- 2026-05-05T01:37:20Z planner: PRD.md 신규 작성
- 2026-05-05T00:00:00Z planner: PRD §12 미해결 6건 자체 결정 반영, PLAN.md(6 마일스톤)와 TASKS.md(T-001/T-002 활성) 신규 작성
- 2026-05-05T10:50:00Z generator: T-001 완료 — src/rss_wiki 패키지 골격, pyproject.toml 의존성(feedparser/httpx/trafilatura/pytest) 선언, pytest src 레이아웃 설정, 스모크 테스트 통과
- 2026-05-05T11:00:00Z evaluator: T-001 PASS (10/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 2, 운영 고려 2; 전체 acceptance 통과, T-002 활성화 권장
- 2026-05-05T11:10:00Z planner: T-001 완료 처리, T-002 단독 활성. REVIEW(T-001) 인계 메모(파이썬 3.12 환경/3개 테스트 케이스/예시 피드 1~2개)를 T-002 참고 항목에 반영. PLAN/PRD 변경 없음.
- 2026-05-05T12:00:00Z generator: T-002 완료 — FeedConfig dataclass + load_feeds 함수 구현(tomllib), feeds.example.toml 2개 피드 수록, 테스트 3케이스(정상/빈파일/URL누락) 모두 PASS (4/4)
- 2026-05-05T12:30:00Z evaluator: T-002 PASS (11/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 2; FileNotFoundError 미래핑 감점, T-003(SQLite 스키마) 활성화 권장
- 2026-05-05T12:45:00Z planner: T-002 완료 처리 후 완료 섹션으로 이동, T-003 단독 활성(SQLite 8개 테이블 DDL + idempotent init_db, 4케이스 테스트). PLAN.md에 "공통 운영 가이드"(파일 IO 에러 래핑) 추가. PRD 변경 없음.
- 2026-05-05T13:00:00Z generator: T-003 완료 — storage/schema.sql(8개 테이블+인덱스), storage/db.py(get_connection/init_db), tests/test_storage_db.py 4케이스 PASS (8/8)
- 2026-05-05T13:30:00Z evaluator: T-003 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 전체 acceptance 통과, T-002 인계(FileNotFoundError 래핑) 반영 확인, T-004 활성화 권장
- 2026-05-05T13:45:00Z planner: T-003 완료 처리, T-004 단독 활성. 원래 "전체 CRUD"였던 T-004를 세션 크기로 분할(T-004=feeds+articles, T-004B=categories/tags/magazines 백로그). repo.py는 sqlite3.Connection 인자 받는 함수형 + url_hash/title_hash 외부 주입 + IntegrityError 가공 없이 전파로 인터페이스 확정. PLAN M2에 슬라이싱 메모 추가, PRD 변경 없음.
- 2026-05-05T14:00:00Z generator: T-004 완료 — storage/repo.py(5개 함수: upsert_feed/get_feed_by_url/insert_article/get_article_by_url_hash/get_article_by_title_hash), tests/test_storage_repo.py 6케이스 PASS (전체 14/14)
- 2026-05-05T14:30:00Z evaluator: T-004 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 전체 acceptance 통과, T-004B(categories/tags/magazines CRUD) 활성화 권장
- 2026-05-05T14:45:00Z planner: T-004 완료 처리 후 완료 섹션으로 이동, T-004B 단독 활성(6개 함수: upsert_category/upsert_tag/link_article_category/link_article_tag/insert_magazine/link_magazine_article + 6 테스트 케이스). 카테고리/태그 정규화는 PRD §12.1에 따라 storage 레이어에서 strip()+lower() 적용으로 자체 결정. PLAN M2 슬라이싱 메모를 T-004 완료/T-004B 활성으로 갱신. PRD 변경 없음.
- 2026-05-05T15:00:00Z generator: T-004B 완료 — storage/repo.py(6개 함수 추가: upsert_category/upsert_tag/link_article_category/link_article_tag/insert_magazine/link_magazine_article), tests/test_storage_repo.py 6케이스 추가 PASS (전체 20/20)
- 2026-05-05T15:30:00Z evaluator: T-004B PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 전체 20 tests 통과, T-005(RSS 수집기) 활성화 권장
- 2026-05-05T15:45:00Z planner: T-004B 완료 처리, T-005(`ingest/fetcher.py`) 단독 활성. M2 마일스톤 완료, M3로 진입. fetcher 인터페이스를 `fetch_feed(url, *, timeout, client) → list[FeedEntry]` + `FetchError(__cause__ 체인)`로 확정, 테스트는 `httpx.MockTransport` 사용해 외부 네트워크 없이 4 케이스(정상/빈피드/HTTP오류/네트워크오류). PLAN.md에 M1/M2 완료 표시 + M3 슬라이싱·인터페이스 원칙(의존성 주입, 도메인 예외 체인, ingest는 DB 미접근) 추가. PRD 변경 없음.
- 2026-05-05T16:00:00Z generator: T-005 완료 — ingest/fetcher.py(FeedEntry+FetchError+fetch_feed), tests/test_ingest_fetcher.py 4케이스 추가 PASS (전체 24/24, 기존 20 회귀 없음)
- 2026-05-05T16:30:00Z evaluator: T-005 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 전체 24 tests 통과, T-006(extractor.py) 활성화 권장
- 2026-05-05T16:45:00Z planner: T-005 완료 처리, T-006(`ingest/extractor.py`) 단독 활성. 인터페이스 `extract_body(entry, *, timeout, client) → str | None` 확정 — trafilatura 본문 추출 → 실패 시 RSS summary 폴백 → 둘 다 실패 시 None(PRD §12.4). `ExtractError`는 클래스 선언만(현재 정상 흐름 raise 없음). 테스트 4케이스(정상/trafilatura None 폴백/HTTP 오류 폴백/둘 다 실패), 합계 28/28. PLAN M3 슬라이싱을 T-005 완료/T-006 활성으로 갱신, PRD 변경 없음.
- 2026-05-05T17:00:00Z generator: T-006 완료 — ingest/extractor.py(ExtractError+extract_body), tests/test_ingest_extractor.py 4케이스 추가 PASS (전체 28/28, 기존 24 회귀 없음)
- 2026-05-05T17:30:00Z evaluator: T-006 PASS (11/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 2; 28/28 PASS, 예외 침묵 패턴 운영 고려 감점, T-007(dedupe.py) 활성화 권장
- 2026-05-05T17:45:00Z planner: T-006 완료 처리, T-007(`ingest/dedupe.py`) 단독 활성. 인터페이스 `normalize_url(url) → str` + `url_hash(url) → str (sha256 hex)` + `title_hash(title) → str (sha256 hex)`로 확정 — UTM prefix 제거 + 알려진 추적 파라미터(fbclid/gclid/ref/ref_src/mc_cid/mc_eid) 제거 + scheme/host lowercase + fragment 제거 + query key 정렬. stdlib만 사용(`urllib.parse`, `hashlib`), DB/네트워크 미접근. 테스트 6 케이스(UTM/추적/대소문자+fragment/key 정렬/해시 동치/제목 해시 정규화), 합계 34/34. T-006 REVIEW 인계(파이프라인 로깅 보완)는 T-015 백로그에 명시. PLAN M3 슬라이싱을 T-006 완료/T-007 활성으로 갱신, PRD 변경 없음.
- 2026-05-05T18:00:00Z generator: T-007 완료 — ingest/dedupe.py(TRACKING_PARAM_PREFIXES/TRACKING_PARAM_NAMES/normalize_url/url_hash/title_hash), tests/test_ingest_dedupe.py 6케이스 PASS (전체 34/34, 기존 28 회귀 없음)
- 2026-05-05T18:30:00Z evaluator: T-007 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 34/34 PASS, 순수 함수 모듈 만점, T-008(failures.py) 활성화 권장
- 2026-05-05T18:45:00Z planner: T-007 완료 처리 후 완료 섹션으로 이동, T-008 단독 활성. 인터페이스를 (i) `ingest/failures.py`=`FAILURE_THRESHOLD=5` 상수 + `is_failing(count, *, threshold)` 순수 함수, (ii) `storage/repo.py`에 `record_feed_success`/`record_feed_failure`/`list_failing_feeds` 3 함수 추가로 분할 — M3 "ingest는 DB 미접근" 원칙 + 레이어 단방향 의존성 유지(repo는 ingest 미임포트, 임계값 기본값 `5`는 양쪽에 중복 표기). 총 6 신규 테스트(failures 3 + repo 3), 합계 40/40 목표. PLAN M3 슬라이싱·레이어 의존성 메모 갱신, PRD 변경 없음.
- 2026-05-05T19:00:00Z generator: T-008 완료 — ingest/failures.py(FAILURE_THRESHOLD=5+is_failing 순수 함수), storage/repo.py(record_feed_success/record_feed_failure/list_failing_feeds 3 함수 추가), tests/test_ingest_failures.py 3케이스 + tests/test_storage_repo.py 3케이스 추가, 전체 40/40 PASS, 기존 34 케이스 회귀 없음
- 2026-05-05T19:30:00Z evaluator: T-008 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 40/40 PASS, M3 전체 완료, T-009(llm/client.py) 활성화 권장
- 2026-05-05T19:45:00Z planner: T-008 완료 처리 후 완료 섹션으로 이동, T-009(`llm/client.py`) 단독 활성. M3 완료/M4 진입 표시. 인터페이스를 `call_claude(prompt, *, timeout=60.0, runner=None) -> str` + `LLMError`/`LLMTimeoutError` 도메인 예외로 확정 — `runner` 주입 패턴으로 외부 바이너리 비의존 단위 테스트 4 케이스(정상/non-zero exit/timeout/empty output), 합계 44/44 목표. JSON 파싱은 본 태스크 범위 외(T-010 `prompts.py` 책임). PLAN.md M3→완료/M4→진행 중 표시 + M4 공통 원칙(runner 주입, 도메인 예외 체인, llm은 DB/네트워크 미접근) 추가. 백로그에서 T-009 제거. PRD 변경 없음.
- 2026-05-05T20:00:00Z generator: T-009 완료 — llm/__init__.py(빈 파일), llm/client.py(LLMError/LLMTimeoutError/DEFAULT_TIMEOUT/call_claude, stdlib subprocess만 사용), tests/test_llm_client.py 4케이스 PASS, 전체 44/44 PASS, 기존 40 회귀 없음
- 2026-05-05T20:30:00Z evaluator: T-009 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 44/44 PASS, M4 첫 슬라이스 완료, T-010(prompts.py) 활성화 권장
- 2026-05-05T20:45:00Z planner: T-009 완료 처리 후 완료 섹션으로 이동, T-010(`llm/prompts.py`) 단독 활성. 인터페이스를 `build_article_prompt(*, title, body, existing_categories) -> str` + `parse_article_response(text) -> ArticleAnalysis(summary, category, tags)` + `PromptParseError` 도메인 예외로 확정 — 순수 함수 모듈(`call_claude`/DB/네트워크/`subprocess` 미접근), 본문 자르기 금지(PRD §10), 카테고리·태그 lower 정규화는 storage 레이어 위임(M2 인계 패턴 유지). 6 신규 테스트(프롬프트 3 + 파싱 3), 합계 50/50 목표. 코드 펜스 처리는 라인 단위 단순 매칭만(정규식 없음)으로 자체 결정. PLAN M4 슬라이싱을 T-009 완료/T-010 활성으로 갱신, 백로그에서 T-010 제거(T-011은 트렌드 프롬프트로 유지). PRD 변경 없음.
- 2026-05-05T21:00:00Z generator: T-010 완료 — llm/prompts.py(PromptParseError/ArticleAnalysis/build_article_prompt/parse_article_response, stdlib only), tests/test_llm_prompts.py 6케이스 PASS, 전체 50/50 PASS, 기존 44 회귀 없음
- 2026-05-05T21:30:00Z evaluator: T-010 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 50/50 PASS, M4 두 번째 슬라이스 완료, T-011(트렌드 프롬프트) 활성화 권장
- 2026-05-05T21:45:00Z planner: T-010 완료 처리 후 완료 섹션으로 이동, T-011(`llm/prompts.py` 확장) 단독 활성. 인터페이스 `build_trend_prompt(*, category, articles: Sequence[Mapping[str, str]]) -> str` + `parse_trend_response(text) -> str`로 확정 — 응답이 자연어 단락이라 JSON 파싱·dataclass 없음, `PromptParseError` 재사용, 빈 articles는 `ValueError`(도메인 위반과 응답 파싱 오류 분리). 같은 파일 작업이므로 REVIEW T-010 인계 #2(dead code 2줄 제거) + #3(빈 summary/category 거부 테스트 2개 보강)을 본 task에 함께 포함, 합계 7 신규 케이스 → 57/57 목표. 백로그에서 T-011 제거, T-017(주월간 통합 요약 프롬프트, M4 마지막 슬라이스) 신규 추가. PLAN M4 슬라이싱을 T-010 완료/T-011 활성으로 갱신, PRD 변경 없음.
- 2026-05-05T22:00:00Z generator: T-011 완료 — llm/prompts.py(build_trend_prompt/parse_trend_response 추가, dead code 2줄 제거, Mapping import 추가), tests/test_llm_prompts.py 7케이스 추가(트렌드 5+빈요약/카테고리 2), 전체 57/57 PASS, 기존 50 회귀 없음
- 2026-05-05T22:30:00Z evaluator: T-011 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 57/57 PASS, M4 세 번째 슬라이스(트렌드 프롬프트) 완료, T-017(주월간 통합 요약 프롬프트) 또는 T-012(일간 매거진 빌더) 활성화 권장
- 2026-05-05T22:45:00Z planner: T-011 완료 처리 후 완료 섹션으로 이동, T-017(`llm/prompts.py` 확장) 단독 활성. 인터페이스를 `build_weekly_prompt(*, articles: Sequence[Mapping[str, str]]) -> str` + `build_monthly_prompt(*, articles: Sequence[Mapping[str, str]]) -> str`로 확정 — 응답 파서는 신규 추가 없이 기존 `parse_trend_response`를 호출자가 재사용(strip+코드펜스+빈 거부 로직 동일, 단순함 우선). 입력은 (title, summary) 페어만 받고 본문(body)은 받지 않음 — 일간 단계의 요약 산출물을 다시 통합하는 PRD §4 흐름이 자연스럽고 컨텍스트 비용 절감(자체 결정). 빈 articles는 `ValueError`(T-011 패턴 유지). 5 신규 케이스(weekly 2 + monthly 2 + distinguishable 1), 합계 62/62 목표. 본 task 완료 시 M4 마일스톤 전체 완료, 다음 사이클 M5 진입(T-012). PLAN M4 슬라이싱을 T-011 완료/T-017 활성으로 갱신, 백로그에서 T-017 제거. PRD 변경 없음.
- 2026-05-05T23:00:00Z generator: T-017 완료 — llm/prompts.py(`_build_period_prompt` 헬퍼 + `build_weekly_prompt`/`build_monthly_prompt` 추가), tests/test_llm_prompts.py 5케이스 추가, 전체 62/62 PASS, M4 마일스톤 완료
- 2026-05-05T23:30:00Z evaluator: T-017 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 62/62 PASS, M4 마일스톤 전체 완료, T-012(일간 매거진 빌더) 활성화 권장
- 2026-05-05T23:45:00Z planner: T-017 완료 처리 후 완료 섹션으로 이동, T-012(`publish/daily.py`) 단독 활성. M4→완료 / M5→진행 중 표시. 인터페이스를 dataclass 3개(`ArticleCard`/`CategorySection`/`FailingFeed`) + `build_daily_magazine(*, date, sections, failing_feeds=()) -> str` 순수 함수로 확정 — publish는 DB/파일IO/네트워크/subprocess 미접근, 자체 dataclass만 입력으로 수용(레이어 단방향 의존성: `sqlite3.Row` → dataclass 변환은 호출자 책임). 빈 sections는 `ValueError`(M4 패턴 유지). 빈 tags/failing_feeds는 해당 줄·섹션 자체 누락(빈 섹션 비노출). 7 신규 케이스(date/순서/카드/태그누락/장애피드/장애피드비노출/ValueError), 합계 69/69 목표. 공통 헬퍼(`renderer.py`) 추출은 T-013/T-014 진행 중 중복 발견 시 검토(YAGNI). PLAN M4→완료/M5→진행 중 갱신 + M5 슬라이싱·인터페이스 원칙 추가. 백로그에서 T-012 제거, T-013/T-014 메모 보강. PRD 변경 없음.
- 2026-05-05T23:59:00Z generator: T-012 완료 — publish/__init__.py(빈 파일), publish/daily.py(ArticleCard/CategorySection/FailingFeed dataclass 3개 + build_daily_magazine 순수 함수, stdlib only), tests/test_publish_daily.py 7케이스 PASS, 전체 69/69 PASS, 기존 62 회귀 없음
- 2026-05-05T23:59:59Z evaluator: T-012 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 69/69 PASS, M5 첫 슬라이스 완료, T-013(주간/월간 빌더) 활성화 권장
- 2026-05-06T00:15:00Z planner: T-012 완료 처리 후 완료 섹션으로 이동, T-013(`publish/{weekly,monthly}.py`) 단독 활성. 주간/월간은 일간과 다른 정보 모델(통합 요약 본문 + 출처 링크 목록)이라 `CategorySection`/`ArticleCard` 재사용 안 함, 자체 `SourceArticle(title,url)` dataclass 신설. `FailingFeed`는 `daily`에서 sibling import로 재사용(dataclass 정의 중복 방지). 시그니처 `build_weekly_magazine(*, period_label, summary, articles, failing_feeds=()) -> str` + `build_monthly_magazine(...)` 동일 구조. 빈 summary(strip 후)·빈 articles는 `ValueError`(M4/M5 패턴 유지). 출력 구조: H1(주간/월간 + period_label) → H2 통합 요약(평문 단락, 일간 트렌드와 달리 블록인용 없음) → H2 출처(`- [{title}]({url})`) → 선택적 `---` + H2 장애 피드. 8 신규 케이스(주간 5 + 월간 3), 합계 77/77 목표. 렌더링 코드 3중 중복(daily/weekly/monthly)은 허용 — T-014 4중 중복 시점에 `publish/renderer.py`로 한꺼번에 추출(YAGNI). PLAN M5 슬라이싱 갱신(T-012 완료 마킹, T-013 활성, M5 인터페이스 패턴에 frozen/splitlines/sibling import 추가). 백로그에서 T-013 제거, T-014에 `field` import 정리/renderer.py 추출 인계 메모 보강. PRD 변경 없음.
- 2026-05-06T00:30:00Z generator: T-013 완료 — publish/weekly.py(SourceArticle dataclass + build_weekly_magazine), publish/monthly.py(build_monthly_magazine, SourceArticle/FailingFeed sibling import), tests/test_publish_weekly.py 5케이스 + tests/test_publish_monthly.py 3케이스 PASS, 전체 77/77 PASS, 기존 69 회귀 없음
- 2026-05-06T00:45:00Z evaluator: T-013 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 77/77 PASS, M5 두 번째 슬라이스 완료, T-014(카테고리/태그 인덱스 빌더) 활성화 권장
- 2026-05-06T01:00:00Z planner: T-013 완료 처리 후 완료 섹션으로 이동, T-014(`publish/indexes.py`) 단독 활성(M5 마지막 슬라이스). 인터페이스를 `IndexEntry(title,url,summary,published_date)` dataclass + `build_index(*, kind, name, entries) -> str`로 확정 — `kind`는 `"category"`/`"tag"` 둘 중 하나, 그 외 `ValueError`. 빈 `name.strip()`·빈 `entries`는 `ValueError`(M4/M5 패턴 유지). 정렬은 빌더 미수행, 호출자 책임(권장: `published_date` 역순). `IndexEntry`에 `tags` 미포함은 자체 결정 — 카테고리/태그 인덱스에서는 같은 카테고리·태그 글이 모이므로 표기 중복(정보 모델 단순화). `failing_feeds` 인자 미수용은 자체 결정 — PRD §4 후미 "매거진 하단"의 "매거진"은 일간/주간/월간 한정, 인덱스 페이지는 PRD §3·§7에서 별도 표현. 따라서 failing_feeds 렌더링은 daily/weekly/monthly 3중 중복 유지, `publish/renderer.py` 추출은 4번째 사용처 등장 시까지 보류(YAGNI). T-012 REVIEW 인계 사항인 `daily.py:3` 미사용 `field` import 제거를 본 task에 동반 포함(한 줄 수정). 7 신규 케이스(category/tag 구분 2 + entries 렌더링 1 + 순서 보존 1 + 멀티라인 summary 1 + invalid kind 1 + 빈 입력 1), 합계 84/84 목표. T-014 완료 시 M5 마일스톤 전체 완료 — 다음 사이클 M6(CLI/파이프라인 결선) 진입. PLAN.md M5 슬라이싱(T-013 완료 마킹, T-014 활성), M5 인터페이스 패턴에 sibling import 재사용 명시·인덱스 페이지 failing_feeds 미수용 결정 추가. 백로그에서 T-014 제거, T-015 백로그에 인덱스 호출 흐름(`build_index`로 카테고리/태그별 누적 entries 전달, 결과로 파일 덮어쓰기) 메모 추가. PRD 변경 없음.
- 2026-05-05T01:37:20Z generator: T-014 완료 — publish/indexes.py(IndexEntry dataclass + build_index 순수 함수, stdlib only), daily.py 미사용 `field` import 제거, tests/test_publish_indexes.py 7케이스 PASS, 전체 84/84 PASS, M5 마일스톤 전체 완료
- 2026-05-05T02:00:00Z evaluator: T-014 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 84/84 PASS, M5 마일스톤 전체 완료, T-015(CLI/파이프라인 결선) 활성화 권장
- 2026-05-05T02:15:00Z planner: T-014 완료 처리 후 완료 섹션으로 이동, M5 마일스톤 ✅완료 / M6 🟢진행 중 표시. T-015(CLI/파이프라인 결선)를 6 슬라이스(T-015A~F)로 분할 — 한 세션 10~20분 기준. T-015A(`pipeline/ingest.py` `process_entry` 단일 entry 처리) 단독 활성. 인터페이스를 `process_entry(*, conn, feed_id, entry, extractor=None, logger=None) -> int | None`로 확정 — 결선층은 `storage`/`ingest` import 허용(M3 "ingest는 DB 미접근" 원칙은 도메인 모듈 한정), `extract_body` `None` 반환 시 stdlib `logging` WARNING으로 URL+이유 기록 후 None 반환(T-006 REVIEW 인계 통합), URL hash → title hash 순으로 중복 판정 후 신규면 `insert_article` 호출 article id 반환, 트랜잭션은 호출자 책임(M2 패턴 유지). 6 신규 케이스 → 합계 90/90 목표. PLAN.md 모듈 구조에 `pipeline/` 패키지 추가, M5 ✅ / M6 🟢 표시, M6 인터페이스 원칙(결선층 import 허용, 콜러블 주입 패턴 유지, commit 미수행, stdlib `logging` 표준) 추가, 공통 운영 가이드에 logging 표준 한 줄 추가. 백로그에 T-015B~F + T-016 정렬, T-016은 T-015F 흡수 검토 메모. PRD 변경 없음.
- 2026-05-05T02:30:00Z generator: T-015A 완료 — pipeline/__init__.py(빈 파일), pipeline/ingest.py(process_entry 함수: extractor 주입 + None 반환 시 WARNING 로깅 + URL/title 해시 중복 판정 + insert_article), tests/test_pipeline_ingest.py 6케이스 PASS, 전체 90/90 PASS, 기존 84 회귀 없음
- 2026-05-05T03:00:00Z evaluator: T-015A PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 90/90 PASS, T-006 인계(extract_body None 로깅) 해소, T-015B(run_daily_ingest 다피드 순회) 활성화 권장
- 2026-05-05T03:15:00Z planner: T-015A 완료 처리 후 완료 섹션으로 이동, T-015B(`pipeline/ingest.py` `run_daily_ingest` 다피드 순회 + 통계 + 실패 격리) 단독 활성. 인터페이스를 `run_daily_ingest(*, conn, feeds: Sequence[FeedConfig], fetcher=None, extractor=None, logger=None) -> IngestStats`로 확정 — `IngestStats` frozen dataclass(feeds_total/feeds_success/feeds_failed/articles_inserted/articles_skipped), `upsert_feed`로 feed_id 확보, fetcher try/except로 `FetchError`만 격리(비-FetchError는 전파해 정합성 문제 가리지 않음), 성공/실패에 따라 `record_feed_success`/`record_feed_failure` 갱신, 각 entry는 기존 `process_entry` 위임. T-006 REVIEW 인계(extract_body timeout/client 노출)는 호출자가 lambda로 주입하는 패턴으로 충족 — 함수 시그니처에 별도 인자 추가 안 함(단순함 우선). 6 신규 케이스 → 합계 96/96 목표. PLAN.md M6 슬라이싱 표시 갱신(T-015A ✅, T-015B 🟢). 백로그에서 T-015B 제거. PRD 변경 없음.
- 2026-05-05T03:30:00Z generator: T-015B 완료 — pipeline/ingest.py(IngestStats dataclass + run_daily_ingest 함수 추가: upsert_feed/FetchError 격리/record_feed_{success,failure}/process_entry 위임/IngestStats 반환), tests/test_pipeline_ingest.py 6케이스 추가 PASS, 전체 96/96 PASS, 기존 90 회귀 없음
- 2026-05-05T04:00:00Z evaluator: T-015B PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 96/96 PASS, FetchError 피드 단위 격리 및 IngestStats 집계 완전 구현, T-015C(pipeline/llm.py LLM 분석 결선) 활성화 권장
- 2026-05-05T04:15:00Z planner: T-015B 완료 처리 후 완료 섹션으로 이동, T-015C(`pipeline/llm.py` 신규 `analyze_articles` + storage repo 3 함수 추가) 단독 활성. 인터페이스를 `analyze_articles(*, conn, article_ids, runner=None, logger=None) -> AnalyzeResult(stats=AnalyzeStats, trends=dict[str,str], analyzed_article_ids=tuple[int,...])`로 확정 — `runner: Callable[[str], str]` 단일화(timeout/runner 키워드 미노출, 단순함 우선), 글 단위 try/except로 `LLMError`/`PromptParseError`만 격리(비-도메인 예외는 전파해 정합성 보호), 트렌드 그룹화 키는 `category.strip().lower()`(저장 정규화는 `upsert_category`가 단일 진실 원천), `update_article_summary`로 분석된 요약을 articles 행에 갱신, `analyzed_article_ids`는 정렬 튜플로 후속 T-015D의 magazine_articles 링크를 결정적으로 만듦. storage repo 신규 3 함수(`list_categories`/`list_articles_by_ids`/`update_article_summary`) 추가. 7+3 신규 케이스 → 합계 106/106 목표. PLAN M6 슬라이싱 표시 갱신(T-015B ✅, T-015C 🟢) + M6 인터페이스 원칙에 (a) 도메인 예외 한정 격리(IntegrityError 캐치 금지), (b) `runner` 시그니처 단순화, (c) 카테고리 정규화 단일 진실 원천 명시 추가. 백로그에서 T-015C 제거, T-015D 메모에 `AnalyzeResult` 입력 흐름 보강. PRD 변경 없음.
- 2026-05-05T05:00:00Z generator: T-015C 완료 — pipeline/llm.py(AnalyzeStats/AnalyzeResult dataclass + analyze_articles 함수: list_articles_by_ids/list_categories 조회 → 글 단위 LLM 분석 + LLMError/PromptParseError 격리 → 트렌드 산출), storage/repo.py(list_categories/list_articles_by_ids/update_article_summary 3 함수 + Sequence import 추가), tests/test_pipeline_llm.py 7케이스 + tests/test_storage_repo.py 3케이스 추가, 전체 106/106 PASS, 기존 96 회귀 없음
- 2026-05-05T05:30:00Z evaluator: T-015C PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 106/106 PASS, runner 시그니처 단순화·예외 격리 정책·정렬 튜플 반환 모두 만점, T-015D(pipeline/publish.py 일간 매거진 빌드) 활성화 권장
- 2026-05-05T06:00:00Z planner: T-015C 완료 처리 후 완료 섹션으로 이동, T-015D(`pipeline/publish.py` 신규 일간 매거진 발행 결선) 단독 활성. 원 PLAN의 T-015D가 "매거진 발행 + 인덱스 갱신"을 한 슬라이스로 묶었으나 한 세션(10~20분) 범위를 넘는 사이즈로 판단해 자체 결정으로 매거진 발행과 인덱스 갱신을 두 슬라이스로 분리(M6 슬라이스 6→7개로 확장: D 매거진, E 인덱스, F 주간/월간, G CLI). 인터페이스를 `publish_daily(*, conn, result: AnalyzeResult, output_dir: Path, date: str, logger=None) -> Path`로 확정 — `analyzed_article_ids`로 article row 조회 → `article_categories`/`article_tags` JOIN으로 카테고리·태그 조회 → name ASC 정렬상 첫 카테고리에만 글 배치(매거진 중복 노출 방지, 인덱스에서는 모든 카테고리 노출은 T-015E 책임) → trends dict로 CategorySection 매핑 → `list_failing_feeds`로 장애 피드 조회 → `build_daily_magazine` 호출 → `output_dir/daily-{date}.md` 파일 쓰기 → `magazines` INSERT + `magazine_articles` 링크. analyzed_ids 비어 있으면 ValueError(빈 매거진 비발행). 카테고리 미할당 글은 WARNING 후 매거진 스킵(magazine_articles 미링크). storage repo 신규 2 함수(`list_categories_for_article`/`list_tags_for_article`) 추가 — name ASC 결정적 정렬. 7+2 신규 케이스 → 합계 115/115 목표. PLAN M6 슬라이싱 표시 갱신(T-015C ✅, T-015D 🟢, 7 슬라이스화 사유 명시) + 백로그를 T-015E(인덱스)/T-015F(주간·월간)/T-015G(CLI)로 재배치. PRD 변경 없음.
- 2026-05-05T07:00:00Z generator: T-015D 완료 — pipeline/publish.py(publish_daily: 빈 analyzed_ids/카테고리 미할당 시 ValueError, 카테고리별 그룹화+매거진 빌드+파일 쓰기+magazines INSERT+magazine_articles 링크), storage/repo.py(list_categories_for_article/list_tags_for_article 2 함수 추가), tests/test_pipeline_publish.py 7케이스 + tests/test_storage_repo.py 2케이스 추가, 전체 115/115 PASS, 기존 106 회귀 없음
- 2026-05-05T08:00:00Z evaluator: T-015D PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 115/115 PASS, publish_daily 결선층 패턴(commit 미수행·WARNING 격리·파일 IO 안전성) 완전 준수, T-015E(인덱스 갱신) 활성화 권장
- 2026-05-05T08:30:00Z planner: T-015D 완료 처리 후 완료 섹션으로 이동, T-015E(`pipeline/publish.py` `publish_indexes` 추가) 단독 활성. 인터페이스를 `publish_indexes(*, conn, output_dir: Path, logger=None) -> list[Path]`로 확정 — 모든 카테고리/태그 일괄 처리(개별 함수 미분리, 단순함 우선), 글 0개 카테고리·태그는 WARNING 후 스킵(`build_index`의 빈 entries ValueError 사전 회피), `output_dir/index-{kind}-{name}.md` 파일 **덮어쓰기**(append 아님 — PRD §8 재생성성 보장 자체 결정), 슬래시만 치환(나머지 특수문자 원문 유지, macOS/Linux 파일명 호환만 고려). storage repo 신규 3 함수(`list_tags`/`list_articles_by_category`/`list_articles_by_tag`) — `published_at DESC NULLS LAST, id DESC` 결정적 정렬. 6+3 신규 케이스 → 합계 124/124 목표. T-015D REVIEW 인계 `Callable` 미사용 import는 본 슬라이스에서 사용처 없으면 generator가 동반 정리(소수점 관찰). PLAN M6 슬라이싱 표시 갱신(T-015D ✅, T-015E 🟢) + 백로그에서 T-015E 제거. PRD 변경 없음.
- 2026-05-05T09:00:00Z generator: T-015E 완료 — storage/repo.py(list_tags/list_articles_by_category/list_articles_by_tag 3 함수 추가), pipeline/publish.py(publish_indexes 함수 + import 5개 추가), tests/test_storage_repo.py 3케이스 + tests/test_pipeline_publish.py 6케이스 추가, 전체 124/124 PASS, 기존 115 회귀 없음
- 2026-05-05T09:30:00Z evaluator: T-015E PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 124/124 PASS, publish_indexes 결선층 패턴 완전 준수, T-015F(주간/월간 트리거+발행) 활성화 권장
- 2026-05-05T10:00:00Z planner: T-015E 완료 처리 후 완료 섹션으로 이동. 원 T-015F("주간/월간 트리거 + 발행")가 한 세션 범위 초과 사이즈로 판단되어 자체 결정으로 3분할(M6 슬라이스 7→8개): T-015F=주간 발행 단독, T-015G=월간 발행 단독, T-015H=CLI+트리거+README. T-015F(`pipeline/publish.py` `publish_weekly` 추가) 단독 활성 — 인터페이스 `publish_weekly(*, conn, end_date: str, output_dir: Path, runner=None, logger=None) -> Path | None`로 확정. 직전 7일(`end_date - 6일 ~ end_date`, 양 끝 포함) 글 조회 → LLM 통합 요약 1회 호출(`build_weekly_prompt` + `parse_trend_response` 재사용, T-017 인계) → `build_weekly_magazine` → 파일 쓰기(`weekly-{period_label}.md`, period_label=ISO 주차 `YYYY-Www`) → `magazines` INSERT(kind="weekly") + 시간순 `magazine_articles` 링크. 빈 기간 → WARNING + None(부수 효과 0). LLM 호출 1회는 격리 단위가 매거진 발행 전체이므로 `LLMError`/`PromptParseError` 결선층 캐치 금지(호출자=T-015H CLI에 전파). storage repo 신규 1 함수(`list_articles_published_between`, `published_at IS NOT NULL` 가드 + ASC 시간순 정렬, NULL은 매거진 누적 제외). 내부 헬퍼 `_iso_week_label(date_str)` stdlib `date.isocalendar()` 기반 zero-pad. 1+4 신규 케이스 → 합계 129/129 목표. T-015D 인계 `Callable` 미사용 import는 본 슬라이스에서 `runner: Callable[[str], str] | None`로 사용 전환되어 자연 해소. PLAN M6 슬라이싱 표시 갱신(T-015E ✅, T-015F 🟢, 8 슬라이스화 사유 명시) + 백로그를 T-015G(월간)/T-015H(CLI+트리거)로 재배치. PRD 변경 없음. **자체 결정 기록:** (a) start_date는 양 끝 포함(`end_date - 6일`), 주간 = ISO 7일. (b) period_label은 발행일 기준 ISO 주차(한국 정서상 그 주의 마지막 금요일이 자연 표기). (c) `articles.summary=""`인 글도 LLM 입력에 포함(빈 요약 필터링하지 않음, PRD §9 단순함 우선). (d) 월간 기간 산출은 T-015G에서 결정하되 잠정적으로 "발행일이 속한 달의 1일 ~ 발행일"(달력월 기준) 사용 권장.
- 2026-05-05T10:30:00Z generator: T-015F 완료 — storage/repo.py(list_articles_published_between 추가), pipeline/publish.py(_iso_week_label 헬퍼 + publish_weekly 함수 + import 4종 추가), tests/test_storage_repo.py 1케이스 + tests/test_pipeline_publish.py 4케이스 추가, 전체 129/129 PASS, 기존 124 회귀 없음
- 2026-05-05T11:00:00Z evaluator: T-015F PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 129/129 PASS, publish_weekly 20단계 순서·LLMError 캐치 금지·commit 미호출 완전 준수, Callable import 미사용 이슈 자연 해소, T-015G(월간 매거진 발행 결선) 활성화 권장
- 2026-05-05T11:30:00Z planner: T-015F 완료 처리 후 완료 섹션으로 이동, T-015G(`pipeline/publish.py` `publish_monthly` 추가) 단독 활성.
- 2026-05-05T12:00:00Z generator: T-015G 완료 — pipeline/publish.py(import 2종 추가: build_monthly_prompt/build_monthly_magazine, publish_monthly 함수 추가: 달력월 1일~end_date 조회→LLM 통합 요약→build_monthly_magazine→파일 쓰기→magazines INSERT/magazine_articles 링크), tests/test_pipeline_publish.py 4케이스 추가(empty/write/period_label/exclude), 전체 133/133 PASS, 기존 129 회귀 없음 인터페이스 `publish_monthly(*, conn, end_date: str, output_dir: Path, runner=None, logger=None) -> Path | None`로 확정 — 발행일이 속한 달의 1일 ~ 발행일(양 끝 포함, 달력월 기준) 글 조회 → `build_monthly_prompt` + `parse_trend_response` 재사용 LLM 통합 요약 1회 → `build_monthly_magazine` → `monthly-{period_label}.md` 파일 쓰기 → `magazines` INSERT(kind="monthly") + 시간순 `magazine_articles` 링크. 빈 기간 → WARNING + None(부수 효과 0). period_label = `end_date[:7]`(YYYY-MM, ISO 슬라이스로 zero-pad 자체 보장). start_date = `date.fromisoformat(end_date).replace(day=1).isoformat()`. `list_articles_published_between`(T-015F 신설) 재사용 — storage repo 신규 함수 0개. import 2종만 추가(`build_monthly_prompt`, `build_monthly_magazine`). 4 신규 케이스(empty/write/period_label/exclude) → 합계 133/133 목표. `LLMError`/`PromptParseError` 결선층 캐치 금지(T-015F 일관, 호출자=T-015H CLI 전파). PLAN M6 슬라이싱 표시 갱신(T-015F ✅, T-015G 🟢) + T-015G 자체 결정 5개(달력월 기간, YYYY-MM 라벨, replace(day=1) 산출, 헬퍼 미생성, repo 함수 재사용) 추가. 백로그에서 T-015G 제거. PRD 변경 없음. **자체 결정 기록:** (a) 달력월 기준(1일~발행일) — PRD §4 "매월 마지막 주 금요일" 의미 일관, 직전 30일 슬라이딩 윈도우는 캘린더 월 경계와 어긋나 운영자 직관 위배. (b) period_label은 `end_date[:7]` 슬라이스 — `_iso_week_label` 같은 별도 헬퍼 미생성(YAGNI). (c) 발행일 이후 며칠은 다음 매거진에 자연 포함되지 않을 수 있으나(누락 가능성) 단순함 우선; 운영자가 회귀를 원하면 후속 슬라이스에서 결정.
- 2026-05-05T13:00:00Z evaluator: T-015G PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 133/133 PASS, publish_monthly 20단계 순서·달력월 기간 산출·LLMError 캐치 금지·commit 미호출 완전 준수, T-015H(CLI 엔트리포인트 + 트리거 판정) 활성화 권장
- 2026-05-05T14:00:00Z generator: T-015H 완료 — cli.py(is_friday/is_last_friday_of_month/run_daily/run_weekly/run_monthly/main 신규), storage/repo.py(list_unanalyzed_article_ids 추가), tests/test_cli.py 12케이스 + tests/test_storage_repo.py 1케이스 추가, 전체 146/146 PASS, 기존 133 회귀 없음
- 2026-05-05T13:30:00Z planner: T-015G 완료 처리 후 완료 섹션으로 이동, T-015H(`cli.py` 신규 — argparse 엔트리포인트 + 트리거 판정 함수 2개(`is_friday`/`is_last_friday_of_month`) + 서브커맨드 결선 함수 3개(`run_daily`/`run_weekly`/`run_monthly`)) 단독 활성. 원 T-015H("CLI + main.py + pyproject + README")가 한 세션 범위 초과 사이즈로 판단되어 자체 결정으로 2분할(M6 슬라이스 8→9개): T-015H=CLI 결선·테스트 단독, T-015I=운영 안내(main.py + pyproject scripts + README cron/launchd 사용법). 인터페이스 확정 — `is_last_friday_of_month`는 stdlib `calendar.monthrange`로 그 달 말일 조회 후 `d.day + 7 > last_day` 판정(윤년·짧은 달 자연 대응). `run_daily`는 미분석 글 회수 패턴(`list_unanalyzed_article_ids` storage repo 신규 함수 1개)으로 멱등성 보장 — 어제 LLM 실패한 글이 오늘 분석 회복(PRD §9 일관). daily 흐름에서 트리거 충족 시 `publish_weekly`/`publish_monthly` 자동 호출(PRD §5 그대로 구현, 옵트아웃 옵션 미도입), weekly/monthly 단독 서브커맨드는 트리거 우회. `now: date | None` 인자 주입 패턴(콜러블 시계 미도입, YAGNI). DB/feeds/output 기본값 `data/rss-wiki.db`/`feeds.toml`/`output`. `cli.main`은 `ValueError`만 캐치(exit 1), `sqlite3.IntegrityError`/`OSError`는 traceback 전파, `LLMError`/`PromptParseError`는 운영자 알림 차원에서 traceback 노출. `conn.commit()`은 `cli.py`의 서브커맨드 결선 함수가 책임(M6 인터페이스 원칙). 12+1 신규 케이스 → 합계 146/146 목표. PLAN M6 슬라이싱 표시 갱신(T-015G ✅, T-015H 🟢, 9 슬라이스화 사유 명시) + T-015H 자체 결정 10개 추가(트리거 함수 cli.py 내부 위치, 결선 함수 cli.py 내부, argparse 시그니처, 자동 트리거 PRD §5 그대로, `now` 인자 주입, 기본 경로, basicConfig, 예외 정책, commit 책임, weekly/monthly 트리거 우회). 백로그에서 T-015H 제거, T-015I 신규 추가(T-016 흡수). PRD 변경 없음.
- 2026-05-05T15:00:00Z evaluator: T-015H PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 146/146 PASS, run_daily 13단계 순서·트리거 판정·argparse 라우팅·commit 책임 완전 준수, T-015I(운영 안내 — main.py/pyproject/README) 활성화 권장
- 2026-05-05T15:30:00Z planner: T-015H 완료 처리 후 완료 섹션으로 이동, T-015I(`main.py` 진입점 정리 + `pyproject.toml [project.scripts]` 등록 + `README.md` 신규 작성) 단독 활성 — **M6 마지막 슬라이스**.
- 2026-05-05T16:00:00Z generator: T-015I 완료 — main.py(얇은 진입점: from rss_wiki.cli import main as cli_main + raise SystemExit), pyproject.toml([project.scripts] rss-wiki = "rss_wiki.cli:main" 추가), README.md(한국어 9 섹션 운영 가이드 신규 작성 약 160줄), 기존 146/146 PASS 회귀 없음, M6 마지막 슬라이스 완료 — 전체 마일스톤 완료 인터페이스 확정 — `main.py`는 `from rss_wiki.cli import main as cli_main` + `if __name__ == "__main__": raise SystemExit(cli_main())` 3~5줄 얇은 진입점(exit code 전파). `pyproject.toml`에 `[project.scripts]` 테이블 + `rss-wiki = "rss_wiki.cli:main"` 단일 엔트리 추가(서브커맨드별 분리 미도입). `README.md`는 빈 파일을 한국어 9 섹션(개요/요구사항/설치/feeds.toml/사용법/자동 트리거 동작/cron·launchd 등록/트러블슈팅/디렉터리 구조)으로 신규 작성 — 약 100~180줄. cron 예시는 매일 12:00(PRD §5 일관), launchd plist는 `StartCalendarInterval` Hour=12 Minute=0. 트러블슈팅 5종(Claude CLI 미인증/feeds.toml 미존재/SQLite 권한/연속 실패 피드/빈 분석 결과). 신규 테스트 추가 없음 — 기존 146/146 PASS 회귀 0 유지가 검증 기준이고, main.py·pyproject·README는 수동 검증(`uv run rss-wiki --help`/임포트 확인). PLAN M6 슬라이싱 표시 갱신(T-015H ✅, T-015I 🟢, "M6 마지막 슬라이스" 명시) + T-015I 자체 결정 9개 추가(main.py 얇은 진입점, single scripts 엔트리, 한국어 README, cron 12:00, launchd plist, 트러블슈팅 5종, 분량 100~180줄, 테스트 미추가, M6 종료 신호). 백로그에서 T-015I/T-016 제거(T-016은 T-015I에 흡수, 백로그 빈 상태). PRD 변경 없음. **다음 사이클(T-015I 평가 PASS 시)에는 모든 항목 완료 확인 후 `docs/DONE` 빈 파일 생성으로 종료 신호.**
- 2026-05-05T16:30:00Z evaluator: T-015I PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 146/146 PASS, rss-wiki --help 동작 확인, README 9섹션 완비, M6 전체 완료
- 2026-05-05T17:00:00Z planner: T-015I PASS 확인(146/146), M6 마지막 슬라이스 완료 = 전체 마일스톤(M1~M6) 완료. PLAN.md 헤더에 "전체 상태: 모든 마일스톤 완료" 한 줄 추가, M6 표기를 "✅ 완료(T-015A~I)"로 갱신. TASKS.md 현재 사이클 섹션에 종료 신호 메모 추가, 백로그 메모를 "비어 있음. 모든 마일스톤 완료" 갱신. `docs/DONE` 빈 파일 생성으로 Planner→Generator→Evaluator 사이클 종료 신호 발신(REVIEW.md "다음 사이클로 넘기는 메모"의 절차 그대로). PRD 변경 없음. 활성 슬라이스 없음. 다음 사이클 호출 시 `docs/DONE` 존재로 즉시 종료.
- 2026-05-05T17:30:00Z planner: docs/DONE 빈 파일 실제 생성 완료(직전 17:00 사이클이 PLAN/TASKS 헤더와 JOURNAL에는 발행 표기를 했으나 실제 파일은 미생성). 모든 마일스톤(M1~M6) 완료, T-015I PASS(146/146), 백로그 빈 상태 재확인. PRD/PLAN/TASKS 변경 없음. 다음 사이클 호출 시 docs/DONE 존재로 즉시 종료.
- 2026-05-05T19:30:00Z planner: PRD §13(FastAPI 웹 인터페이스) 신설(PRD.md mtime 18:56)을 코드/문서 점검에서 발견 — `src/rss_wiki/web/` 부재, `tests/test_web*` 부재, `cli.py` `daily` 흐름이 `feeds.toml`을 SoT로 사용(PRD §11 line 107 "DB feeds 테이블이 SoT" 미반영). 종료 조건 (b) 미충족 → `docs/DONE` 미생성(이전 사이클 17:30 발행 표기에도 불구하고 현재 파일 시스템에 부재 확인). M7 신규 마일스톤(웹 인터페이스, 7 슬라이스 T-018A~G) PLAN.md 추가, 모듈 구조 트리에 `web/` 패키지 도입. T-018A(feeds 스키마 확장 — `enabled`/`last_fetched_at`/`updated_at` 컬럼 + `init_db` 멱등 마이그레이션 + storage repo 4 함수: `list_feeds`/`update_feed`/`set_feed_enabled`/`reset_feed_failures`) 단독 활성. PLAN.md 헤더 "전체 상태"를 "M1~M6 완료, M7 진행 중. docs/DONE 발행 보류"로 갱신. TASKS.md 현재 사이클 헤더 갱신 + 백로그에 T-018B~G 6 항목 추가. PRD 변경 없음. **자체 결정 기록:** (a) PRD §11 표의 `title`은 기존 `name` 컬럼으로 매핑(컬럼 이중화 회피, 호환 유지). (b) `delete_feed`는 본 슬라이스 범위 외 — 외래키 정합성 결정과 함께 T-018B에서 처리(자체 결정으로 메타 스냅샷 컬럼 추가 채택). (c) M7 슬라이스 7개 분할 사유: 한 세션(10~20분) 기준으로 storage 확장 / 부트스트랩+FK / 패키지 골격 / 매거진 GET / 피드 GET / 피드 POST / CLI+README. (d) 의존성(fastapi/uvicorn/jinja2/markdown-it-py) 추가는 T-018C(패키지 골격)에서 일괄 처리 — T-018A는 stdlib only 유지로 회귀 위험 최소화. (e) 부트스트랩 시점은 T-018B `cli.run_daily` 진입 시 1회(`bootstrap_feeds_from_toml`) — `web` 서브커맨드는 DB만 조회. (f) 단위 테스트는 기존 `tests/test_storage_repo.py`에 추가(파일 분산 방지).
- 2026-05-05T15:00:00Z generator: T-018A 완료 — feeds 테이블 컬럼 3개(enabled/last_fetched_at/updated_at) 추가, init_db 멱등 마이그레이션(PRAGMA table_info→ALTER TABLE), repo 4함수(list_feeds/update_feed/set_feed_enabled/reset_feed_failures) 신설, 단위 테스트 7케이스 PASS (153/153)
- 2026-05-05T20:00:00Z evaluator: T-018A PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 153/153 PASS, feeds 스키마 멱등 마이그레이션·repo 4함수 완전 충족, T-018B(TOML→DB 부트스트랩+delete_feed) 활성화 권장
- 2026-05-05T20:30:00Z planner: T-018A 완료 처리 후 완료 섹션으로 이동, T-018B(TOML→DB 부트스트랩 + cli.run_daily 전환) 단독 활성. 원 T-018B(부트스트랩 + cli 전환 + 외래키 nullable 마이그레이션 + 스냅샷 컬럼 + delete_feed)가 한 세션 범위 초과 사이즈로 판단되어 자체 결정으로 2분할(M7 슬라이스 7→8개): T-018B=부트스트랩+cli 전환(pipeline/cli 변경, articles 미터치), T-018B2=articles 외래키/스냅샷/delete_feed(storage 마이그레이션 단일 책임). 인터페이스 확정 — `pipeline/bootstrap.py` 신규(`bootstrap_feeds_from_toml(conn: sqlite3.Connection, path: str | Path) -> int` 단일 함수, `config.load_feeds` + `repo.upsert_feed` 결선, `conn.commit()` 미호출, `INSERT OR IGNORE`로 운영자 수동 비활성/메타 보존), `cli.main` daily 분기 본문 교체(부트스트랩 호출 → `list_feeds(conn, enabled_only=True)` → `FeedConfig` 변환 → `run_daily` 인자 주입), `cli.run_daily` 시그니처 변경 금지(테스트 격리 유지), weekly/monthly 분기 미변경. 단위 테스트 4 케이스(`tests/test_pipeline_bootstrap.py` 신규: inserts_all/idempotent/returns_count/preserves_existing_state) + cli 통합 테스트 1 케이스 → 합계 158/158 목표. PLAN.md M7 슬라이싱 7→8 갱신(T-018A ✅, T-018B 🟢, T-018B2 백로그 추가) + T-018B 자체 결정 9개 추가 + 백로그에서 T-018B 제거하고 T-018B2 신규 추가. PRD 변경 없음. **자체 결정 기록:** (a) `bootstrap_feeds_from_toml` 위치는 `pipeline/`(결선층 패턴, M6 일관) — `storage/`는 데이터 액세스 단일 책임 유지. (b) `upsert_feed` 동작은 `INSERT OR IGNORE` 그대로 — 운영자가 웹/SQL로 비활성화한 피드가 부트스트랩으로 다시 활성화되지 않도록 한다(PRD §11 "운영 SoT는 DB" 의도와 일치). (c) `cli.run_daily` 시그니처 미변경 — `feeds` 인자를 그대로 받아 단위 테스트의 fake fetcher/extractor/runner 주입 패턴 유지. DB 조회는 `cli.main`에서 수행. (d) weekly/monthly 부트스트랩 미수행 — 발행만 수행(피드 변경 영향 없음). (e) `from rss_wiki.config import load_feeds` cli 미사용 import는 자체 결정으로 제거(린트 클린, generator가 동반 정리). (f) bootstrap의 commit 미수행 — 첫 commit은 `run_daily` 내 commit 시점에 함께 반영(부트스트랩만 수행하고 fetcher 실패해도 피드 행은 commit 안 됨, 다음 daily 호출 시 회복).
- 2026-05-05T21:00:00Z generator: T-018B 완료 — pipeline/bootstrap.py 신규(bootstrap_feeds_from_toml), cli.py daily 분기 교체(부트스트랩→list_feeds→run_daily 주입), test_pipeline_bootstrap.py 4케이스 + test_cli.py 1케이스 추가, 158/158 PASS
- 2026-05-05T21:30:00Z evaluator: T-018B PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 158/158 PASS, INSERT OR IGNORE로 운영자 비활성 피드 보존·결선층 패턴 완전 준수, T-018B2(articles 외래키/스냅샷/delete_feed) 활성화 권장
- 2026-05-05T22:00:00Z planner: T-018B 완료 처리 후 완료 섹션으로 이동, T-018B2(articles 외래키 nullable + 스냅샷 컬럼 + delete_feed) 단독 활성(M7 세 번째 슬라이스).
- 2026-05-05T22:30:00Z generator: T-018B2 완료 — schema.sql(feed_id nullable + 스냅샷 2 컬럼), db.py(articles 재생성 마이그레이션 + 스냅샷 컬럼 멱등 추가), repo.py(delete_feed 신설), tests/test_storage_repo.py(6 케이스 추가), 164/164 PASS 인터페이스 확정 — `schema.sql`의 `articles.feed_id` NOT NULL 제거 + `feed_url_snapshot TEXT`/`feed_name_snapshot TEXT` 2 컬럼 추가, `init_db`는 `PRAGMA table_info(articles)`로 `feed_id.notnull == 1`이면 SQLite 테이블 재생성 패턴(PRAGMA foreign_keys OFF → articles_new 생성 → INSERT SELECT 9컬럼 → DROP → RENAME → 인덱스 2개 재생성 → PRAGMA foreign_keys ON), 스냅샷 컬럼은 항상 PRAGMA table_info 재조회 후 누락 시 멱등 ALTER ADD(T-018A 패턴), `delete_feed(conn, feed_id) -> None`은 (1) 스냅샷 채움 UPDATE(서브쿼리), (2) feed_id NULL UPDATE, (3) DELETE feeds 3 SQL(commit 미호출, M2 패턴). 6 신규 케이스(nullable 마이그/스냅샷 마이그/데이터 보존/이중 호출 멱등/delete_feed 채움+NULL+삭제/articles 0건 delete) → 합계 164/164 목표. PLAN.md M7 슬라이싱 갱신(T-018B ✅, T-018B2 🟢) + T-018B2 자체 결정 11개 추가, TASKS.md 현재 사이클 헤더 갱신 + T-018B2 활성 항목 추가 + 백로그에서 T-018B2 제거. PRD 변경 없음. **자체 결정:** (a) ON DELETE SET NULL 미채택(명시적 NULL UPDATE 흐름 유지, 운영자 추적성 우선). (b) 마이그레이션 버전 테이블 미도입(PRAGMA table_info로 충분, YAGNI). (c) 별도 마이그레이션 스크립트 파일 미도입(`init_db` 단일 진입점). (d) 테스트는 `tests/test_storage_repo.py` 확장(파일 분산 방지). (e) 미존재 feed_id에 대한 delete_feed는 silent no-op(호출자 검증 책임).
- 2026-05-05T23:00:00Z evaluator: T-018B2 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 164/164 PASS, articles 재생성 마이그레이션·스냅샷 컬럼 멱등·delete_feed 3-SQL 순서 완전 충족, T-018C(web/ 패키지 골격) 활성화 권장
- 2026-05-05T23:30:00Z planner: T-018B2 완료 처리 후 완료 섹션으로 이동, T-018C(web/ 패키지 골격 + fastapi/uvicorn/jinja2/markdown-it-py 의존성 + create_app 팩토리 + lifespan에서 init_db + WAL 모드 + GET /healthz, GET / 라우트 + render_markdown 래퍼) 단독 활성(M7 네 번째 슬라이스). PLAN.md M7 슬라이싱 갱신(T-018B2 ✅, T-018C 🟢) + T-018C 자체 결정 16개 추가(의존성 미핀, create_app 팩토리, WAL은 lifespan에서 별도 커넥션으로 활성화, get_db 요청 단위, render_markdown 단일 export, 4 테스트 케이스). TASKS.md 현재 사이클 헤더 갱신 + T-018C 활성 항목 추가. PRD 변경 없음. 자체 결정: (a) WAL 활성화 책임은 web/app.py(결선층)에 두고 storage/db.py(도메인) 미터치 — 기존 storage 테스트 회귀 보호. (b) get_db는 본 슬라이스 라우트 미사용이지만 T-018D~F가 즉시 import할 수 있도록 미리 정의. (c) markdown-it-py 채택(markdown 패키지 대신, PRD §13 옵션 중 활발한 유지보수 + CommonMark 표준).
- 2026-05-05T23:45:00Z generator: T-018C 완료 — pyproject.toml(fastapi/uvicorn[standard]/jinja2/markdown-it-py 4 의존성 추가), src/rss_wiki/web/__init__.py(신규 빈 파일), src/rss_wiki/web/app.py(create_app 팩토리 + lifespan에서 init_db + WAL 활성화 + GET /healthz + GET / + get_db 의존성), src/rss_wiki/web/markdown.py(render_markdown 단일 함수), tests/test_web_app.py(4 케이스: healthz/index/WAL/render_markdown), 168/168 PASS
- 2026-05-05T23:59:00Z evaluator: T-018C PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 168/168 PASS(신규 4 + 기존 164 회귀 0), create_app 팩토리·lifespan WAL 활성화·get_db 요청 단위 커넥션·render_markdown 싱글턴 완전 충족, T-018D(매거진/카테고리/태그 GET 라우트) 활성화 권장
- 2026-05-05T23:59:00Z generator: T-018D 완료 — repo.py(list_magazines/get_magazine_by_id 추가), web/app.py(Jinja2Templates 모듈 레벨 + include_router + get_db Request 타입 힌트), routes_magazines.py(신규 3라우트: /·/magazines·/magazines/{id}), templates/{base,list,magazine}.html(신규 3개), tests/test_web_app.py(5 케이스 추가), 173/173 PASS
- 2026-05-06T00:30:00Z planner: T-018C 완료 처리 후 완료 섹션으로 이동, T-018D(매거진 GET 라우트 + 템플릿 토대) 단독 활성(M7 다섯 번째 슬라이스).
- 2026-05-06T01:00:00Z evaluator: T-018D PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 173/173 PASS(신규 5 + 기존 168 회귀 0), repo 2함수·템플릿 3종·routes_magazines.py 3라우트·404 이중 방어 완전 충족, T-018D2(카테고리/태그 GET 라우트) 활성화 권장 원 T-018D("매거진/카테고리/태그 GET 라우트")가 5 라우트 + 3 템플릿 + 5+ 테스트 + 라우트 모듈/Jinja2 토대를 한 슬라이스에 묶었으나 한 세션(10~20분) 범위 초과로 자체 결정 2분할(M7 슬라이스 8→9개): T-018D=매거진 단독(라우트 모듈/Jinja2 토대 + 매거진 2 라우트 + 3 템플릿), T-018D2=카테고리/태그(기존 list.html 재사용). 인터페이스 확정 — `repo.list_magazines(conn) -> list[sqlite3.Row]`(SELECT id/kind/published_at/file_path ORDER BY published_at DESC, id DESC), `repo.get_magazine_by_id(conn, magazine_id)` 2 함수 신규. `routes_magazines.py` 신규(APIRouter 기반 `/`+`/magazines`+`/magazines/{magazine_id}` 3 라우트). `web/app.py`에 `Jinja2Templates(directory=Path(__file__).parent/"templates")` 모듈 레벨 인스턴스 + `app.state.templates = templates` + `app.include_router(magazines_router)` 추가, 기존 `/` 임시 HTML 라우트는 routes_magazines.py로 이전·교체. 매거진 슬러그는 `magazines.id` 정수 사용(별도 슬러그 컬럼 미추가, YAGNI). 단건 라우트는 file_path를 `Path.read_text(encoding="utf-8")`→`render_markdown`→`magazine.html` 렌더, 미존재 id/file 둘 다 HTTPException(404). 템플릿 3종 — `base.html`(골격 + 인라인 CSS 1줄), `magazine.html`(`{{ magazine_html | safe }}`), `list.html`(`heading`+`items=[{title, href, subtitle?}]` 표준 — 카테고리/태그 재사용). `/`와 `/magazines`는 동일한 list.html 렌더(`/`=최근 매거진 헤딩, `/magazines`=매거진 인덱스 헤딩) — "최신 일간 단건 + 최근 목록 분리"는 후속 사이클 검토(YAGNI). T-018C `test_index_returns_200` 갱신(검증은 "RSS Wiki" 유지) + 5 신규 케이스(목록 빈/2건/단건 마크다운/404 미존재 id/404 file 누락) → 합계 173/173 목표. PLAN.md M7 슬라이싱 갱신(T-018C ✅ 168/168, T-018D 🟢, T-018D2 백로그 추가) + T-018D 자체 결정 16개 추가, TASKS.md 현재 사이클 헤더 갱신("M7 9 슬라이스") + T-018D 활성 항목 추가 + 백로그에서 T-018C 제거하고 T-018D2 신규 추가. PRD 변경 없음. **자체 결정 기록:** (a) T-018D 2분할 사유는 한 세션 범위 초과(라우트 5+템플릿 3+테스트 5+토대). (b) 매거진 슬러그는 정수 id 사용 — slug 컬럼 추가는 schema.sql 마이그레이션 필요(회귀 위험 회피). (c) `/` 와 `/magazines`는 본 슬라이스에서 동일 list.html — 분리는 PRD §13 표 충족 후 후속 검토. (d) python-multipart 의존성 미추가(GET 라우트만, T-018F 책임). (e) Jinja2Templates 단일 인스턴스 모듈 레벨 — 별도 dependencies.py 미도입.
- 2026-05-06T01:30:00Z planner: T-018D PASS(12/12, 173/173) 확인 후 완료 섹션으로 이동, T-018D2(카테고리/태그 GET 라우트) 단독 활성(M7 여섯 번째 슬라이스). 인터페이스 확정 — `repo.get_category_by_name(conn, name) -> Row | None`/`repo.get_tag_by_name(conn, name) -> Row | None` 2 함수 신규(자체 결정으로 PLAN 메모 "repo 변경 미필요" 갱신 — 이름 정규화 책임을 storage 레이어 M2 패턴 일관으로 두기 위해 thin lookup 헬퍼 도입, 내부 `name.strip().lower()` 처리). `routes_magazines.py` 확장(별도 `routes_browse.py` 분리하지 않음, YAGNI — 라우트 6개로 단일 모듈 충분). 3 라우트 추가: `GET /categories`(인덱스, list.html), `GET /categories/{name}`(글 목록, list.html), `GET /tags/{name}`(글 목록, list.html). `/tags` 인덱스 라우트 미도입(PRD §13 표 strict — 태그 인덱스 항목 없음). 비공개 헬퍼 3종(`_category_items`/`_tag_items`/`_article_items`) 추가. 글 카드 `href`는 원문 URL(`articles.url`) 직접 링크 — 별도 글 상세 라우트 미도입(PRD §13 표에 없음, YAGNI). 글 카드 `title`은 NULL/빈 시 `articles.url` 폴백(빈 링크 텍스트 방지). `web/app.py`/`templates/*.html` 변경 없음(include_router로 자동 노출 + list.html 재사용). 5 신규 케이스(`test_categories_index_empty`/`test_categories_index_with_entries`/`test_category_articles_renders_articles`/`test_category_articles_404_for_missing_name`/`test_tag_articles_404_for_missing_name`) → 합계 178/178 PASS 목표. 태그 200 케이스 의도적 생략(카테고리 200이 동일 코드 경로 충분히 커버, 회귀 위험 낮음). PLAN.md M7 슬라이싱 갱신(T-018D ✅ 173/173, T-018D2 🟢) + T-018D2 자체 결정 10개 추가. TASKS.md 현재 사이클 헤더 갱신("T-018D2 활성") + T-018D2 활성 항목 추가 + 백로그에서 T-018D2 제거. PRD 변경 없음. **자체 결정 기록:** (a) repo 신규 2 함수 — 이름 정규화 책임을 storage에 둠(M2 일관). (b) 라우트 모듈 분리 미수행 — YAGNI, T-018E·F의 routes_feeds.py 신설 시 그쪽이 별도 모듈. (c) `/tags` 인덱스 미도입 — PRD strict. (d) 글 카드는 원문 URL 직접 링크 — 글 상세 라우트 미도입(YAGNI). (e) 글 카드 title 폴백(NULL/빈 → url) — 운영 안전. (f) heading은 정규화된 이름 그대로(`"카테고리: ai"`) — 정규화 투명성 우선. (g) 태그 200 케이스 생략 — 카테고리 200이 동일 코드 경로 커버. (h) python-multipart 의존성 미추가(T-018F 책임).
- 2026-05-05T10:00:00Z generator: T-018D2 완료 — repo.py(get_category_by_name/get_tag_by_name 추가), routes_magazines.py(_category_items/_tag_items/_article_items 헬퍼 + /categories·/categories/{name}·/tags/{name} 3 라우트 추가), tests/test_web_app.py(5 케이스 추가), 178/178 PASS
- 2026-05-05T10:30:00Z evaluator: T-018D2 PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 178/178 PASS(신규 5 + 기존 173 회귀 0), get_category_by_name/get_tag_by_name storage 레이어 정규화·routes_magazines.py 단일 모듈 확장 6라우트·변경 금지 파일 미변경 완전 충족, T-018E(피드 GET 라우트 + routes_feeds.py) 활성화 권장
- 2026-05-05T11:00:00Z planner: T-018D2 PASS(12/12, 178/178) 확인 후 완료 섹션으로 이동, T-018E(피드 GET 라우트 + `routes_feeds.py` 신설) 단독 활성(M7 일곱 번째 슬라이스). 인터페이스 확정 — `routes_feeds.py` 신규 모듈(APIRouter, 매거진 라우트 6 + 피드 라우트 7=GET2+POST5 = 단일 모듈 13 라우트 회피로 자연스러운 분리 시점), `repo.get_feed_by_id(conn, feed_id) -> Row | None` 1 함수 신설(M2 thin lookup, `get_*_by_*` 명명 일관 — 기존 `get_feed_by_url`은 부트스트랩/수집의 URL 기반 lookup 전용), `web/app.py`는 import 1줄 + `include_router(feeds_router)` 1줄만 추가(create_app 시그니처/lifespan 미변경), 2 라우트(`GET /feeds` `feeds_index` → `repo.list_feeds(conn)` 전체 표시(`enabled_only=False` — 비활성도 토글 복원 가능하도록 노출), `GET /feeds/{feed_id}/edit` `feed_edit_form` → `get_feed_by_id` 미존재 시 404), `templates/feeds.html` 신규(다중 컬럼 테이블 — name/url/enabled/연속실패/마지막수집/수정링크, `list.html` 미재사용 사유: heading+items 모델로 표현 불가), `templates/feed_edit.html` 신규(`<form method="post" action="/feeds/{id}">` — name input + enabled checkbox + url readonly + "삭제 후 재추가 안내", action은 T-018F 시점에 활성화 — 본 슬라이스 405지만 후속 즉시이므로 템플릿 한 번에 작성). POST 라우트 5종/`python-multipart` 의존성/CLI `web` 서브커맨드는 T-018F~G 책임. 4 신규 케이스(`test_feeds_index_empty`/`test_feeds_index_with_entries`/`test_feed_edit_form_renders`/`test_feed_edit_404_for_missing_id`) → 합계 182/182 목표. PLAN.md M7 슬라이싱 갱신(T-018D2 ✅ 178/178, T-018E 🟢) + T-018E 자체 결정 12개 추가, TASKS.md 현재 사이클 헤더 갱신("T-018E 활성") + T-018E 활성 항목 추가. PRD 변경 없음. **자체 결정 기록:** (a) `routes_feeds.py` 신규 분리 — 매거진 6 + 피드 7 단일 모듈 회피, M7 인터페이스 원칙 "라우트 모듈 3 이상이면 분리 고려" 충족 시점. (b) `get_feed_by_id` 신설 — id 기반 edit lookup 필요. (c) `enabled_only=False` — 비활성 피드 노출. (d) `list.html` 미재사용 — 다중 컬럼/form 표현 명료성. (e) `feed_edit.html` form action을 T-018F POST 엔드포인트로 한 번에 작성 — 회귀 안전(GET만 테스트), T-018F에서 템플릿 재수정 회피. (f) checkbox checked/unchecked 텍스트 검증 생략 — Jinja2 출력 형태 의존, T-018F POST 토글 테스트가 동작 커버. (g) `python-multipart` 미추가(T-018F 책임). (h) 파일 분리 미수행(`test_web_feeds.py` 신설 X) — 178+4=182 단일 파일 가독성 유지 가능. (i) `consecutive_failures`/`last_fetched_at` 표시 검증 생략 — 부수 컬럼 회귀 위험 낮음, 핵심은 name/url/edit 링크/404.
- 2026-05-05T12:00:00Z generator: T-018E 완료 — repo.py(get_feed_by_id 추가), web/routes_feeds.py 신규(GET /feeds + GET /feeds/{id}/edit 2 라우트), templates/feeds.html 신규(다중 컬럼 테이블), templates/feed_edit.html 신규(수정 폼), web/app.py(feeds_router include_router 등록), tests/test_web_app.py(4 케이스 추가), 182/182 PASS
- 2026-05-05T13:00:00Z evaluator: T-018E PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 182/182 PASS(신규 4 + 기존 178 회귀 0), routes_feeds.py 신규 모듈(GET /feeds + GET /feeds/{id}/edit)·get_feed_by_id·feeds.html/feed_edit.html 2 템플릿 완전 충족, app.py 2줄 추가·변경 금지 파일 미변경 모듈 경계 완전 준수, T-018F(피드 POST 라우트 5종 + python-multipart) 활성화 권장
- 2026-05-05T13:30:00Z planner: T-018E PASS(12/12, 182/182) 확인 후 완료 섹션으로 이동, T-018F(피드 POST 라우트 5종 + `python-multipart` 의존성) 단독 활성(M7 여덟 번째 슬라이스). 인터페이스 확정 — `pyproject.toml dependencies`에 `python-multipart` 1줄 추가(FastAPI `Form(...)` 의존성, T-018E에서 의도적 보류분 도입), `routes_feeds.py`에 5 POST 라우트 추가(`feeds_create`/`feed_update`/`feed_delete`/`feed_toggle`/`feed_reset`) — 모두 `RedirectResponse(url="/feeds", status_code=303)` 반환 + 함수 끝에서 `conn.commit()` 호출(M7 인터페이스 원칙 "쓰기 라우트만 commit"). `Form`/`RedirectResponse`/`normalize_url` import 추가. URL 정규화는 `ingest.dedupe.normalize_url` 재사용(M3 단일 진실 원천). UNIQUE 충돌은 기존 `upsert_feed`의 `INSERT OR IGNORE`로 silent 멱등. 미존재 feed_id는 4 라우트(`feed_update`/`feed_delete`/`feed_toggle`/`feed_reset`)에서 명시적 404. `POST /feeds` 빈 url은 명시적 400(누락은 FastAPI 자동 422). `feeds.html` 갱신 — 테이블 위 추가 폼 1개(`url` required + `name` 선택, `enabled` 미포함=기본 활성) + 행별 토글/실패 리셋/삭제 3 form 버튼(인라인 CSS `display:inline` 1줄 + 삭제 confirm 다이얼로그 1줄 표준 브라우저 JS). `feed_edit.html` 변경 없음(T-018E에서 `action="/feeds/{id}"`로 사전 작성). 10 신규 케이스(`test_post_feeds_creates`/`test_post_feeds_normalizes_url`/`test_post_feeds_duplicate_url_idempotent`/`test_post_feeds_rejects_empty_url`/`test_post_feed_update`/`test_post_feed_update_disables_when_unchecked`/`test_post_feed_delete`/`test_post_feed_toggle`/`test_post_feed_reset`/`test_post_feed_404_for_missing_id`) → 합계 192/192 PASS 목표. PLAN.md M7 슬라이싱 갱신(T-018E ✅ 182/182, T-018F 🟢) + T-018F 자체 결정 20개 추가. TASKS.md 현재 사이클 헤더 갱신("T-018F 활성") + T-018F 활성 항목 추가, 백로그에서 T-018F 제거(T-018E 백로그 항목도 함께 정리). PRD 변경 없음. **자체 결정 기록:** (a) `python-multipart` 의존성 추가 — `Form(...)` 필수. (b) `POST /feeds`에 `enabled` form 미수용 → 기본 활성 추가, 운영자가 추가 직후 토글로 처리(단순함). (c) 미존재 feed_id silent no-op 회피 → 명시적 404로 운영자 즉시 인지. (d) `name` 추가 폼은 빈 입력 시 normalized url로 폴백, 수정 폼은 `name` required. (e) URL 정규화는 dedupe 재사용. (f) `feeds.html`에 추가 폼 동시 추가 — 운영자가 피드 등록 수단 부재 회피(PRD §13 "피드 관리 UI" 충족). (g) 인라인 CSS 1줄 + 삭제 confirm JS 1줄만 사용(PRD §13 strict). (h) CSRF 미도입(PRD §13 "개인용·로컬 전용, 인증 없음"). (i) 트랜잭션은 라우트 단일 commit. (j) repo 신규 함수 미추가(기존 함수 재사용).
- 2026-05-05T14:00:00Z generator: T-018F 완료 — pyproject.toml(python-multipart 추가), routes_feeds.py(POST 5 라우트 + Form/RedirectResponse/normalize_url import), feeds.html(추가 폼 + 행별 토글/리셋/삭제 버튼), tests/test_web_app.py(10 케이스 추가), 192/192 PASS
- 2026-05-05T14:30:00Z evaluator: T-018F PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 192/192 PASS(신규 10 + 기존 182 회귀 0), POST 5 라우트·303 리다이렉트·URL 정규화·404·빈URL 400·commit 명시 완전 충족, T-018G(CLI web 서브커맨드 + README §13) 활성화 권장
- 2026-05-05T15:00:00Z planner: T-018F PASS(12/12, 192/192) 확인 후 완료 섹션으로 이동, T-018G(CLI `rss-wiki web` 서브커맨드 + uvicorn 인스턴스 직접 전달 + README §7 웹 인터페이스 섹션 신설 + 트러블슈팅 포트 점유 항목 + tests/test_cli.py 4 케이스) 단독 활성(M7 마지막 슬라이스). 인터페이스 확정 — `cli.py` 모듈 상단 `import uvicorn` + `from rss_wiki.web.app import create_app` 2 import 추가, `run_web(*, db_path, host, port, run_uvicorn=None, logger=None) -> int` 결선 함수 신설(`run_uvicorn` 콜러블 주입 패턴 — M3·M4 일관, 단위 테스트 격리), `argparse`에 `web` 서브커맨드(`--host` 기본 `127.0.0.1`, `--port` 기본 `8765`), `cli.main`의 `web` 분기에서 `return run_web(db_path=..., host=args.host, port=args.port, logger=logger)`. uvicorn은 인스턴스 직접 전달(`uvicorn.run(create_app(db_path), host=..., port=..., log_level="info")`)로 `--db` 옵션 정합. README는 기존 §7~9를 §8~10으로 시프트하고 §7 "웹 인터페이스" 신설(시작 명령/호스트·포트 옵션/보안 경고/라우트 요약 표/동시 실행 안내 ~50~80줄), 트러블슈팅에 "포트 점유" 항목 1개 추가. 4 신규 케이스(`test_run_web_invokes_uvicorn_with_create_app`/`test_run_web_passes_custom_host_port`/`test_main_web_subcommand_routes_to_run_web`/`test_main_web_subcommand_honors_host_port_args`) → 합계 196/196 PASS 목표. PLAN.md M7 슬라이싱 갱신(T-018F ✅ 192/192, T-018G 🟢 마지막 슬라이스) + T-018G 자체 결정 19개 추가, TASKS.md 현재 사이클 헤더 갱신("T-018G 활성, M7 마지막 슬라이스") + T-018G 활성 항목 추가, 백로그에서 T-018G 제거(빈 백로그). PRD 변경 없음. **자체 결정 기록:** (a) `import uvicorn` 모듈 레벨(테스트 monkeypatch 격리). (b) `run_uvicorn` 콜러블 주입 패턴(M3·M4 일관). (c) uvicorn 인스턴스 직접 전달(`--db` 정합, reload/workers 미지원이지만 PRD §13 단일 프로세스 strict). (d) `init_db` 중복 호출 회피하지 않음(멱등, 일관성 우선). (e) uvicorn 인프라 예외(포트 점유 등) traceback 전파(M6 인터페이스 원칙). (f) PRD 본문 import 경로 표기(`rss_wiki.web:app`) 변경 없음 — CLI 우회 import로 운영자 노출 면 차이 없음. (g) README 섹션 번호 시프트(§7~9 → §8~10) — 단순 헤더 변경. (h) 한국어 작성. (i) FastAPI 앱 인스턴스 검증은 `isinstance(app, FastAPI)` 단순 확인. (j) `monkeypatch.setattr(rss_wiki.cli.uvicorn, "run", fake)` 패턴(모듈 레벨 import 덕분).
- 2026-05-05T15:30:00Z generator: T-018G 완료 — cli.py(import uvicorn/create_app + run_web 함수 + web 서브커맨드 + main 분기), README.md(§7 웹 인터페이스 신설 + §7~9 → §8~10 시프트 + 포트 점유 트러블슈팅 추가), tests/test_cli.py(4 케이스 추가), 196/196 PASS
- 2026-05-05T16:00:00Z evaluator: T-018G PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 196/196 PASS(신규 4 + 기존 192 회귀 0), cli.py run_web 함수·web 서브커맨드·main 분기·README §7 신설·섹션 시프트·포트 점유 트러블슈팅 완전 충족, M7 마지막 슬라이스 완료·PRD 전 섹션 충족·docs/DONE 발행 권장
- 2026-05-05T16:30:00Z planner: PRD §1~13 전 섹션 코드/문서/테스트 매핑 최종 점검 완료 — §1~3(목적/환경/산출물) 일간 파이프라인+web/+마크다운 산출물, §4~5(매거진 구성/흐름) publish/{daily,weekly,monthly}.py+cli.run_daily 트리거, §6 카테고리/태그 LLM 자동생성+기존카테고리 주입(llm/prompts.py), §7 마크다운 인덱스(publish/indexes.py), §8 SQLite+마크다운 하이브리드(storage/), §9 수집/실패/중복 ingest/{fetcher,extractor,dedupe,failures}.py, §10 Claude CLI subprocess(llm/client.py), §11 feeds 테이블 SoT+TOML 부트스트랩(pipeline/bootstrap.py), §12 결정사항(N=5/TOML/FastAPI/summary 폴백) 모두 반영, §13 web/app.py+routes_{magazines,feeds}.py 13 라우트+WAL+127.0.0.1 보안 기본값. TASKS.md 35개 항목 모두 [x] 완료(196/196 PASS), 신규 간극 없음. DONE 발행 조건 (a) 전 항목 [x] · (b) PRD 전 섹션 충족 모두 만족. **`docs/DONE` 빈 파일 발행으로 프로젝트 종료 신호 발행.** TASKS.md 현재 사이클 헤더를 "M1~M7 전 슬라이스 완료(196/196). PRD §1~13 전 섹션이 코드·문서·테스트에 반영됨. 신규 활성 항목 없음. docs/DONE 발행으로 프로젝트 종료(2026-05-05)."로 갱신. PRD/PLAN.md 변경 없음.
- 2026-05-05T23:35:00Z planner: PRD §13 갱신(23:32Z) 점검 — 모던 디자인·디자인 토큰 단일 CSS·라이트/다크 자동·반응형·상단 GNB(매거진/카테고리/태그/피드 관리)·토스트·배너 플래시(쿼리스트링)·`url`도 수정 가능·`GET /feeds/new` 별도 페이지 등 신규 UI/UX 요구가 코드/템플릿/CSS 미반영 확인(`base.html` 1줄 인라인 CSS·`feed_edit.html`의 "URL 변경 불가" 안내가 PRD와 직접 충돌). 직전 사이클의 `docs/DONE` 신호는 갱신 이전 PRD 기준이었으므로 재개 — `docs/DONE` 파일 미존재 확인, 발행 보류. M8(웹 UI 모던화 + PRD §13 CRUD 충돌 해소) 신설, 3 슬라이스 분할: T-019A(디자인 토큰+단일 CSS+base.html GNB+다크모드+반응형+StaticFiles 마운트), T-019B(피드 CRUD URL 편집 허용+토스트/배너 플래시+`GET /feeds/new`), T-019C(컴포넌트 클래스 부착+`GET /tags` 인덱스+active GNB 강조+매거진 본문 스타일). T-019A 단독 활성, T-019B·T-019C 백로그. 인터페이스 확정 — `web/static/style.css` 신규(약 200~300줄, 토큰 5축[컬러/간격/폰트/라운드/섀도우] + 라이트/다크 `@media (prefers-color-scheme: dark)` + 반응형 `@media (max-width: 768px)` 가로 스크롤 탭바 + 컴포넌트 `.gnb`/`.container`/`.card`/`.badge`/`.btn`/`.btn-primary`/`.btn-danger` + 폼/표 element 스타일), `web/app.py`에 `from fastapi.staticfiles import StaticFiles` import 1줄 + `app.mount("/static", StaticFiles(directory=Path(__file__).parent / "static"), name="static")` 1줄 추가, `templates/base.html` 18줄 내외로 재작성(viewport meta + stylesheet link + `<header class="gnb">` 4 메뉴 GNB + `<main class="container">`, `active_nav` Jinja2 조건부 클래스는 본 슬라이스 미주입=미강조 노출, T-019C에서 컨텍스트 주입). 4 신규 케이스(`test_static_style_css_served`/`test_base_html_includes_stylesheet_link`/`test_base_html_renders_gnb_with_four_links`/`test_base_html_includes_viewport_meta`) → 합계 200/200 PASS 목표. PLAN.md 상태 문구·M7 완료 표기·M8 마일스톤 신설(슬라이싱 표 + T-019A 자체 결정 16개 + 모듈 트리에 `web/static/` 추가), TASKS.md 현재 사이클 헤더·T-019A 활성 항목·T-019B/C 백로그 추가. PRD 변경 없음. **자체 결정 기록:** (a) 햄버거 메뉴 대신 가로 스크롤 탭바 채택(PRD §13 "JS 없이 동작" strict 준수, PRD가 둘 중 택일 허용). (b) active 강조 컨텍스트 주입은 T-019C로 분리(라우트 5+ 곳 일괄 수정 비용 분산, 본 슬라이스는 마크업 + CSS 정의만). (c) 컴포넌트 클래스 부착(`feeds.html`/`list.html`/`magazine.html`)은 T-019B·C 책임으로 분산 — 본 슬라이스 4개 변경 파일에 한정. (d) `fastapi.staticfiles.StaticFiles`는 fastapi 본체 동봉으로 의존성 추가 없음. (e) CSS 빌드 도구(Tailwind 등) 미도입(PRD §13 "빌드 단계 없이"). (f) 다크모드 토글 UI 미도입(PRD가 자동 적용만 명시). (g) `name="static"` 라우트 등록 — 향후 `url_for("static", path="...")` 호환. (h) `position: sticky` GNB(PRD §13 "모든 페이지에 고정 배치"). (i) `_flash.html` 부분 템플릿은 T-019B 책임(본 슬라이스 미포함, 회귀 surface 최소화). (j) `feed_edit.html` URL readonly 제거는 T-019B 책임(M8 두 번째 슬라이스).
- 2026-05-05T23:50:00Z generator: T-019A 완료 — web/static/style.css 신규(디자인 토큰 5축+라이트/다크+반응형+GNB+컴포넌트, 약 270줄), app.py StaticFiles 마운트(2줄 추가), base.html GNB 재작성(18줄), test_web_app.py 4 케이스 추가, 202/202 PASS
- 2026-05-06T00:10:00Z evaluator: T-019A PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 202/202 PASS(신규 4 + 기존 198 회귀 0), style.css 5축 토큰·다크모드·반응형·GNB 컴포넌트 완전 충족, base.html 18줄 재작성·StaticFiles 마운트 모듈 경계 준수, T-019B(피드 URL 편집+플래시+GET /feeds/new) 활성화 권장
- 2026-05-06T00:30:00Z planner: T-019A PASS(12/12, 202/202) 확인 후 완료 섹션으로 이동, T-019B(피드 CRUD URL 편집 허용 + 토스트/배너 플래시 + `GET /feeds/new` 별도 페이지) 단독 활성(M8 두 번째 슬라이스). PRD §13(2026-05-05 갱신본) "수정: `url`도 수정 가능(정규화 후 UNIQUE 검증, 충돌 시 폼 에러)" + "토스트/배너로 처리 결과 노출(쿼리스트링 플래시, JS 없이 동작)" + 라우트 표 `GET /feeds/new`(별도 페이지) 3 신규 요구를 한 슬라이스에 충족. 인터페이스 확정 — `repo.update_feed(conn, feed_id, *, name, url=None)` 시그니처 확장(url=None 미변경, 명시 시 `UPDATE SET name=?, url=?, updated_at=...` SQL 분기 — UNIQUE 검증은 SQLite 제약 + `IntegrityError` 전파 위임으로 별도 SELECT 미수행, 자기 자신 갱신은 동일 url set 시 SQLite 동작상 위반 안 함). `routes_feeds.py` `feed_update`에 `url: str = Form("")` 추가 + `url.strip()` 비면 `url_arg=None`, 비지 않으면 `normalize_url()` 후 기존 url과 동일하면 `url_arg=None`(불필요 SQL 회피), `try`/`except sqlite3.IntegrityError` 캐치 시 `conn.rollback()` + `RedirectResponse(url=f"/feeds/{feed_id}/edit?error=duplicate", status_code=303)`, 성공 시 `?ok=updated`. `feeds_create`도 `?ok=created` redirect(silent 멱등 유지 — duplicate URL 추가는 PRD "방지"로 해석). `GET /feeds/new` 라우트 신설(`feed_new_form` — DB 미주입, 폼 렌더만) + `templates/feed_new.html` 신규(20줄). `templates/_flash.html` 신규(부분 템플릿 18줄, `request.query_params.get("ok"/"error")` 코드를 한국어 메시지로 매핑 — `created`/`updated`/`duplicate` 4종, `role="status"`/`role="alert"` ARIA). `feeds.html`/`feed_edit.html`/`feed_new.html`에서 `{% include "_flash.html" %}` 한 줄로 사용. `style.css`에 `.flash`/`.flash-success`/`.flash-danger` 3 클래스 추가(약 12줄, 토큰 재사용 — 닫기 버튼 미도입 PRD "JS 없이"). `feed_edit.html` URL input readonly 제거 + 안내 문구 교체. `feeds.html` 인라인 추가 폼은 그대로 유지(`/feeds/new`는 보조 진입점). 9 신규 케이스(storage 2: `update_feed_url_changes_url`/`update_feed_url_duplicate_raises`; web 7: `get_feeds_new_returns_form`/`post_feed_update_changes_url`/`post_feed_update_duplicate_url_redirects_to_edit_with_error`/`post_feed_update_empty_url_keeps_existing`/`feeds_create_redirects_with_ok`/`feeds_index_renders_flash_on_ok_query`/`feed_edit_renders_flash_on_error_query`) → 합계 211/211 PASS 목표. PLAN.md 상태 문구 갱신 + M8 진행 상태(T-019A ✅ 202/202, T-019B 🟢) 갱신 + T-019B 자체 결정 18개 추가. TASKS.md 현재 사이클 헤더 갱신 + T-019B 활성 항목 추가 + T-019A 완료 섹션으로 이동(`[x]`, PASS 202/202) + 백로그에서 T-019B 제거(T-019C만 잔여). PRD 변경 없음. **자체 결정 기록:** (a) UNIQUE 검증은 SQLite IntegrityError 전파로 위임(M2 일관, 별도 SELECT 미수행). (b) 정규화 후 기존 url과 동일하면 url_arg=None(불필요 SQL 회피). (c) IntegrityError 캐치 시 rollback 호출(set_feed_enabled 부분 적용 차단). (d) feeds_create는 silent 멱등 유지 + `?ok=created` 통일(중복 메시지 분리 안 함, PRD "방지" 해석). (e) `feeds.html` 인라인 폼 유지 + `/feeds/new` 별도 페이지 추가(두 진입점 — 빠른 등록 + 안내 페이지). (f) `_flash.html` partial은 `<h2>` 직후 1줄 include. (g) request 컨텍스트 자동 주입(FastAPI 0.110+ `templates.TemplateResponse(request, ...)` 활용). (h) 닫기 버튼 미도입(PRD "JS 없이" strict — 새로고침/이동 시 자동 사라짐). (i) 3 단순 액션 라우트(delete/toggle/reset) 플래시 미추가(회귀 surface 최소화, T-019C에서 추가 검토). (j) 빈 url Form 입력은 미변경 처리(라우트 책임 — storage는 패스스루). (k) repo의 url=None 미변경 동작 검증 케이스 생략(라우트 통합 테스트로 커버, YAGNI).

- 2026-05-06T00:45:00Z generator: T-019B 완료 — update_feed url 인자 추가, routes_feeds.py(GET /feeds/new·feeds_create ?ok=created·feed_update url Form+IntegrityError 처리), _flash.html 신규, feed_new.html 신규, feed_edit.html readonly 제거+플래시, feeds.html 플래시, style.css .flash 3클래스 추가, tests 9케이스 추가, 211/211 PASS
- 2026-05-06T01:00:00Z evaluator: T-019B PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 211/211 PASS(신규 9 + 기존 202 회귀 0), update_feed url 분기·IntegrityError 전파(M2 일관)·정규화 라우트 책임(M3 일관)·rollback 명시·ARIA 접근성·JS 없이 동작 전항 충족, 변경 금지 파일 미수정, T-019C(컴포넌트 클래스 부착+GET /tags 인덱스+active GNB+매거진 본문 스타일) 활성화 권장
- 2026-05-06T01:30:00Z planner: T-019B PASS(12/12, 211/211) 확인 후 완료 섹션으로 이동, T-019C(컴포넌트 클래스 부착 + `GET /tags` 인덱스 + active GNB 강조 + 매거진 본문 스타일) 단독 활성(M8 마지막 슬라이스). PRD §13(2026-05-05 갱신본) 잔여 4 요구를 한 슬라이스에 충족: (1) "카드/리스트/배지/버튼/폼 등 공통 컴포넌트의 스타일을 통일" — `.card`/`.badge`/`.btn`/`.btn-primary`/`.btn-danger` 클래스를 5 템플릿(`feeds.html`/`list.html`/`magazine.html`/`feed_edit.html`/`feed_new.html`)에 부착, (2) "본문 영역은 가독성 있는 폭 + 코드/인용/표/링크 스타일 정리" — `style.css`에 `.magazine-body` 스코프로 H1~H4 계층 + `pre`/`code` + `blockquote` 인용 + `<a>` 밑줄 약 50줄 추가(폭은 T-019A `.container` 720px 단일 진실 원천), (3) "현재 활성 메뉴는 시각적으로 강조" — `routes_magazines.py` 7 GET 라우트(`index`/`magazines_list`/`magazine_detail`/`categories_index`/`category_articles`/`tag_articles`/신규 `tags_index`) + `routes_feeds.py` 3 GET 라우트(`feeds_index`/`feed_new_form`/`feed_edit_form`)의 `TemplateResponse` 컨텍스트에 `active_nav` 4값(magazines/categories/tags/feeds) 주입(`base.html` Jinja2 조건부 클래스는 T-019A에서 작성됨), (4) GNB "태그" 메뉴 동작 활성화 — `GET /tags` 인덱스 라우트 신설(`routes_magazines.py`, `repo.list_tags` 재사용, `_tag_items` 헬퍼 재사용, `list.html` 재사용, heading "태그"). 인터페이스 확정 — `magazine.html` `<article class="magazine-body">`, `list.html` 항목별 `<div class="card">` 래퍼(`<ul><li>` → `<div>`), `feeds.html` 추가 폼 `.card` + 활성/비활성 `.badge-success`/`.badge-danger` + 5+ 버튼 `.btn`/`.btn-primary`/`.btn-danger`(인라인 form `style="display:inline"` 유지), `feed_edit.html`/`feed_new.html` 제출/취소 `.btn`/`.btn-primary`. 7 신규 케이스(`tags_index_empty`/`tags_index_with_entries`/`active_nav_marks_feeds_link_when_on_feeds_page`/`active_nav_marks_magazines_link_when_on_magazines_page`/`active_nav_marks_tags_link_when_on_tags_page`/`feeds_html_uses_btn_class`/`magazine_body_styles_in_css`) → 합계 218/218 PASS 목표. PLAN.md 상태 문구·M8 슬라이싱(T-019B ✅ 211/211 + T-019C 🟢 마지막 슬라이스) 갱신 + T-019C 자체 결정 16개 추가. TASKS.md 현재 사이클 헤더 갱신 + T-019C 활성 항목 추가 + T-019B 완료 섹션으로 이동(`[x]`, 🟢 마커 제거) + 백로그에서 T-019C 제거(빈 백로그). PRD 변경 없음. **자체 결정 기록:** (a) `/tags` 인덱스는 `routes_magazines.py`에 추가(별도 모듈 분리 미수행, T-018D2 일관). (b) `active_nav` 매핑은 4값 string(`/` 라우트는 `magazines`로 — 랜딩 페이지가 매거진 목록 역할). (c) POST 라우트 active_nav 미주입(redirect만 반환, base.html 미렌더). (d) 매거진 본문 스타일은 `.magazine-body` 클래스 스코프(글로벌 element 영향 차단). (e) 폭 720px는 `.container` 단일 진실 원천(`.magazine-body` 폭 재정의 안 함). (f) `<table>` 매거진 본문 스타일 미추가(T-019A element 정의 재사용, YAGNI). (g) `<a>` 매거진 본문은 `text-decoration: underline`(GNB/카드 링크는 밑줄 없는 디자인 유지). (h) `<pre><code>` 중첩은 padding/border/background 제거(이중 박스 회피). (i) 카테고리 active 케이스 생략(magazines/feeds와 동일 코드 경로, YAGNI). (j) `list.html` `.card` 래퍼는 `tags_index_with_entries`에서 함께 검증(별도 케이스 미추가). (k) `/tags` 빈 목록 메시지는 `list.html`의 공용 "아직 항목이 없습니다." 분기 사용(메시지 통일성). (l) 인라인 form `style="display:inline"` 유지(`.btn` `inline-flex`이지만 부모 inline form으로 행 내 정렬 정상, 작은 변경 surface 우선). (m) repo 변경 없음(`list_tags`/`list_feeds` 등 모두 기존 함수 재사용). (n) DONE 발행 시점은 T-019C PASS 후 다음 사이클 Planner가 PRD §1~13 전 섹션 점검 → `docs/DONE` 발행(T-019C가 PRD §13 갱신본 마지막 미반영 4 항목을 모두 충족하므로 다음 사이클이 종료 사이클).
- 2026-05-06T02:00:00Z generator: T-019C 완료 — GET /tags 라우트 신설, routes_magazines.py 7 GET 라우트 + routes_feeds.py 3 GET 라우트 active_nav 주입, style.css .magazine-body 스타일 50줄 추가, list.html .card 래퍼, magazine.html .magazine-body 클래스, feeds.html .btn/.badge 클래스, feed_edit.html/feed_new.html .btn 클래스, 신규 7 케이스, 218/218 PASS
- 2026-05-06T02:30:00Z evaluator: T-019C PASS (12/12) — 사양 충족 3, 모듈 경계 3, 테스트 충실도 3, 운영 고려 3; 218/218 PASS(신규 7 + 기존 211 회귀 0), GET /tags 라우트·10 GET 라우트 active_nav 주입·.magazine-body CSS·list.html .card·magazine.html .magazine-body·feeds.html 컴포넌트 클래스 전항 충족, M8 전 슬라이스 완료(T-019A/B/C 모두 PASS), 다음 사이클 Planner PRD §1~13 전 섹션 최종 점검 후 docs/DONE 발행 권고
- 2026-05-06T02:45:00Z planner: T-019C PASS 확인(218/218, REVIEW.md 12/12) → M8 종결, 모든 마일스톤(M1~M8) 완료. PRD §1~13 전 섹션이 코드/문서/테스트에 반영됨을 점검: §3·§13(FastAPI 웹 + GNB + 디자인 토큰 + 다크모드 + 반응형 + 컴포넌트 클래스 + 매거진 본문 스타일 + active 강조 + `/tags` 인덱스 + 플래시 메시지 + URL 수정 허용 + `/feeds/new` 별도 페이지)는 web/ 패키지·style.css·templates/*에 반영, §11(feeds.toml 시드 + DB feeds 테이블 SoT)은 T-018A·B에서 충족, §1~12 핵심 흐름은 M1~M6(config/storage/ingest/llm/publish/pipeline/cli)에서 충족. 모든 TASKS [x], 미반영 PRD 요구 없음 → 종료 조건 (a)+(b) 모두 충족. `docs/DONE` 빈 파일 생성으로 Planner→Generator→Evaluator 사이클 종료 신호 발신. PLAN.md 헤더(전체 상태)와 M8 표기를 ✅ 완료로 갱신, TASKS.md 현재 사이클 헤더를 종료 메모로 갱신. PRD 변경 없음. 활성 슬라이스 없음. 다음 사이클 호출 시 `docs/DONE` 존재로 즉시 종료.
