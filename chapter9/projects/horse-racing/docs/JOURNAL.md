- 2026-07-06T16:19:17Z planner: PRD.md 신규 작성
- 2026-07-07T00:00:00Z planner: 미해결 의사결정 3건 PRD→PLAN 이관 및 decisions 개시(렌더링 A/탭처리 B/그래픽 A), PLAN.md 마일스톤 6개·TASKS.md T1(스캐폴딩)·T2(도메인 순수 로직) 신규 작성
- 2026-07-07T01:30:00Z generator: T1(프로젝트 스캐폴딩) 완료. Vite+React18+TS+vitest 구성, npm install/tsc/build/test 모두 통과
- 2026-07-07T01:32:00Z evaluator: T1 PASS(11/12). acceptance 4개 직접 재실행 전부 통과, touch 범위 준수. decisions 3건 evaluator 독립 선택 기록(canvas:A, horse-graphics:A, tab-inactive:B)
- 2026-07-07T01:34:00Z planner: 누락됐던 decisions .planner.md 3건 재선택 기록(canvas:A, tab-inactive:B, horse-graphics:A)으로 합의 비교 재개. T1 PASS 반영해 PLAN M1 진행상태 갱신, TASKS 다음 작업은 T2(도메인 타입·스탯 변동·배당 순수 모듈) 유지. DONE 미생성(T2 미완료, M2~M6 미구현)
- 2026-07-07T01:40:00Z generator: T2(도메인 타입 & 스탯 변동·배당 순수 모듈) 완료. src/domain/{types,stats,odds}.ts 및 테스트 작성, npm run test(13 tests)·tsc --noEmit 모두 통과
- 2026-07-07T01:42:00Z evaluator: T2 PASS(10/12). test 13개·tsc 직접 재실행 전부 통과, acceptance 3개 핵심을 테스트가 실제 검증, touch 범위 준수. 테스트 충실도·운영 고려 각 2점(컨디션 중간 밴드/0-스탯 실패경로 미검증). decisions 3건 evaluator 독립 선택 기록(canvas:A, horse-graphics:A, tab-inactive:B)
- 2026-07-07T01:44:00Z planner: decisions 3건 합의 완료(canvas:A, horse-graphics:A, tab-inactive:B) PRD 8번·6번에 (자체 결정: 합의) 반영, PLAN 미해결 섹션 정리. M2 착수 위해 TASKS에 T3(저장 계층)·T4(스토어·상태 머신+말 카탈로그) 신규 정의, REVIEW 메모2(0-스탯 계약)를 T4 acceptance에 반영. DONE 미생성(M2~M6 미구현)
- 2026-07-07T01:47:53Z generator: T3(저장 계층/persistence) 완료. src/persistence/{schema,storage}.ts 및 storage.test.ts(6개) 작성, npm run test(19 tests)·tsc --noEmit 모두 통과
- 2026-07-07T01:50:00Z evaluator: T3 PASS(11/12). storage.test.ts 6개·전체 19 tests·tsc 직접 재실행 전부 통과, acceptance 3개(라운드트립/손상 2종/미가용 폴백)를 테스트가 실제 검증, touch 범위 준수. 테스트 충실도 2점(save 단독 실패·records 항목 검증 분기 미커버). decisions 3건은 단일 파일 합의 종결로 신규 독립 선택 불필요
- 2026-07-07T01:52:00Z planner: T3 PASS 반영해 PLAN M2 진행상태 갱신(persistence [x], 스토어·상태머신 T4 잔여). 다음 작업 T4로 확정하며 T3 REVIEW 메모 3(말 id↔records 키 정합)을 T4 acceptance에 반영. 비차단 메모 1·2(save 단독 실패·schema 분기 테스트)는 T5로 신규 정의(T4 여유 시 흡수). DONE 미생성(M3~M6 미구현, T4·T5 미완료)
- 2026-07-07T01:58:00Z generator: T4(게임 상태 스토어·상태 머신+말 카탈로그) 완료. src/store/{machine,gameStore}.ts·테스트, src/domain/horses.ts·테스트 작성, npm run test(36 tests)·tsc --noEmit 모두 통과. T3 REVIEW 메모 3(id↔records 정합)·T2 REVIEW 메모 2(0-스탯 NaN 경로) 해소. T5(저장 계층 방어 케이스)는 미흡수, 다음 세션으로 이월
- 2026-07-07T02:02:00Z evaluator: T4 PASS(10/12). test 36개·tsc 직접 재실행 전부 통과, acceptance 5개(전이 순서·미정의 거부/pause·resume 범위/파산 재충전/팩토리 스탯 하한·NaN 부재/id 고유·records 정합)를 테스트가 실제 검증, touch 범위 준수. 테스트 충실도·운영 고려 각 2점(파산 경계값·adjustBalance 알림·방어 복사 미검증). decisions 3건은 단일 파일 합의 종결로 신규 독립 선택 불필요
- 2026-07-07T02:05:00Z planner: T4 PASS 반영해 PLAN M2에 테스트 완결 항목 추가. 다음 작업 T5를 "M2 테스트 완결"로 확장 — T3 REVIEW 메모 1·2(저장 계층 방어)와 T4 REVIEW 메모 1·2·3(gameStore 파산 경계값·adjustBalance 알림·방어 복사)을 한 세션으로 묶어 프로덕션 코드 변경 없이 테스트만 보강. DONE 미생성(M3~M6 미구현: 시뮬레이션·Canvas·React UI·효과음)
- 2026-07-07T02:10:00Z generator: T5(M2 테스트 완결) 완료. storage.test.ts에 save 단독 실패·schema 분기(records 항목 손상, horseCount 범위 위반) 3개, gameStore.test.ts에 파산 경계값·adjustBalance 알림·방어 복사 4개 테스트를 프로덕션 코드 변경 없이 추가. npm run test(43 tests)·tsc --noEmit 모두 통과. M2 테스트 부채 정리 완료, 다음은 M3(경주 시뮬레이션 엔진)
- 2026-07-07T02:12:00Z evaluator: T5 PASS(11/12). test 43개·tsc --noEmit 직접 재실행 전부 통과, acceptance 6개(save 단독 실패·records 손상·horseCount 범위·파산 경계 100/99·adjustBalance 알림·방어 복사)를 테스트가 실제 프로덕션 분기로 검증. mtime으로 touch 두 테스트 파일만 변경·프로덕션 코드 무변경 확인. 테스트 충실도 3점(방어 복사 중첩 깊이는 비차단 메모). M2 테스트 부채 정리 완료
- 2026-07-07T02:15:00Z planner: T5 PASS 반영해 PLAN M2 완료 표시, M3(경주 시뮬레이션 엔진)를 T6(delta-time 적분·순위·완주 판정)와 T7(스킬 5종·발동 확률·피니시 근접·포토 피니시)로 분해. 이번 세션은 T6 진행으로 정의(순수 sim 엔진, 프레임레이트 독립성·결정론·순위·완주·후반 감속 acceptance). T5 REVIEW 비차단 메모 2건(방어 복사 중첩 깊이→M5, save 정상경로 disabled 확인→M4/M5)은 후속 마일스톤 반영으로 이관. DONE 미생성(M3~M6 미구현: 시뮬레이션·Canvas·React UI·효과음, PRD 4.2·4.5·4.6·4.8 등 코드 미대응)
- 2026-07-07T02:20:00Z generator: T6(경주 시뮬레이션 코어) 완료. src/sim/types.ts·engine.ts·engine.test.ts 신규 작성 — delta-time 적분 기반 step(rng는 초기화 시 1회만 소비), 프레임레이트 독립성·결정론·순위 산출(동률 처리)·완주 판정·stamina 후반 감속 8개 테스트. npm run test(51 tests)·tsc --noEmit 모두 통과. 스킬·피니시 근접·포토 피니시는 T7로 분리
- 2026-07-07T02:30:00Z planner: T6 PASS 반영해 PLAN M3 T6 완료 표시. 원래 T7(스킬+피니시)이 한 세션 크기 초과라 T7(스킬 시스템 5종·발동 확률 luck·하위권 보정·경주당 1회 발동 엔진 통합)과 T8(피니시 근접 슬로모션 트리거·포토 피니시 판정)로 분해. 이번 세션은 T7. T6 REVIEW 메모 1(burst 효과 직접 검증)을 T7 acceptance로 흡수, 메모 2·3(프레임레이트 독립성 전체 구간·연출 상수 실측)은 M4로 이관. DONE 미생성(M3 후반~M6 미구현: 스킬·Canvas·React UI·효과음, PRD 4.2·4.5·4.6·4.8 코드 미대응)
- 2026-07-07T02:25:00Z evaluator: T6 PASS(11/12). vitest(전체 51개·sim 8개)·tsc --noEmit 직접 재실행 전부 통과. acceptance 5개(프레임레이트 독립성·결정론·순위·완주·후반 감속)를 테스트가 실제 검증, 변경 파일 모두 touch(src/sim/) 범위 내·순수 함수로 렌더 분리 준수. 테스트 충실도 2점: burst 효과 직접 검증 부재, 프레임레이트 독립성 테스트가 stamina 감속 미발동 구간(진행률 ~0.056)에만 적용됨(비차단 메모 1·2). decisions 3건 evaluator 선택 독립 기록(A/A/B, 기존 합의와 일치)
- 2026-07-07T02:35:00Z generator: T7(스킬 시스템 5종+발동 확률+경주당 1회 발동) 완료. src/sim/skills.ts(발동 조건·확률·효과 순수 모듈) 신규, engine.ts의 step에 스킬 발동 판정·효과(본인/타인 속도 배수, stamina 면제) 통합, types.ts에 skillId·skillActivated·skillActivatedAt를 선택적 필드로 확장(engine.test.ts 하위 호환 유지). skills.test.ts 16개(하위권 보정·luck 영향·경주당 1회 제한·대표 스킬 효과·burst 진폭 직접 검증·결정론) 신규. npx vitest run(9 files, 67 tests)·tsc --noEmit 모두 통과. T6 REVIEW 비차단 메모 1(burst 직접 검증) 해소. 다음은 T8(피니시 근접·포토 피니시 판정)
- 2026-07-07T02:45:00Z planner: T7 PASS(12/12) 반영해 PLAN M3 T7 완료 표시. 이번 세션 작업을 T8(피니시 근접 슬로모션 트리거·포토 피니시 판정, src/sim/finish.ts 순수 함수)로 확정 — T8은 이미 TASKS 진행 중 항목으로 잘 정의돼 있어 유지. T7 REVIEW 비차단 메모 3건(zone·slipstream 엔진 위치 반영 통합 테스트, 발동 확률 계수 실측, 흔들기 범위 제한)을 PLAN M4 "T7 REVIEW 메모 흡수"로 이관. DONE 미생성(T8 미완료, M4~M6 미구현: Canvas 연출·React UI·효과음, PRD 4.5·4.6·4.7·4.8·4.9 코드 미대응)
- 2026-07-07T02:41:00Z evaluator: T7 PASS(12/12). vitest(skills 16개·전체 67개)·tsc --noEmit 직접 재실행 전부 통과, engine.test.ts 8개 회귀 없음(선택적 필드 하위 호환 유지 확인). acceptance 전 항목(스킬 5종 구분 효과·발동 이력 노출·하위권 보정·luck 영향·경주당 1회 제한·대표 스킬 효과·burst 직접 검증·결정론)을 테스트가 실제 검증. 판정(skills.ts 순수)/상태(engine.ts 1회 제한 필터) 책임 분리, 변경 파일 모두 touch 범위 내. 비차단 메모 3건(zone·slipstream 엔진 위치 반영 통합 테스트 부재, 발동 확률 계수 실측 조정 M4/M5 이관, 흔들기 범위 제한 M4 재검토). decisions 3주제 evaluator.md 기존재로 신규 기록 없음
- 2026-07-07T02:50:00Z generator: T8(피니시 근접 슬로모션 트리거+포토 피니시 판정) 완료. src/sim/finish.ts(신규) — isSlowMotionTriggered(선두 진행률 0.9 이상)·isPhotoFinish(완주 시 1·2위 위치 차 트랙 길이의 2% 이내, 미완주면 항상 false) 순수 함수, 임계값은 상수+함수 인자로 노출. finish.test.ts 8개(경계값 포함) 신규. types.ts 확장은 불필요(기존 position·finished 필드로 충분). npx vitest run(10 files, 75 tests)·tsc --noEmit 모두 통과. M3의 마지막 태스크 완료, 다음은 M4(Canvas 렌더링 & 연출)
- 2026-07-07T03:00:00Z planner: T8 PASS(12/12) 반영해 PLAN M3 완결 표시. M4(Canvas 렌더링 & 연출)가 한 세션 크기 초과라 T9~T12로 분해(T9 시뮬레이션 전체 구동기·연출 상수 실측 / T10 Canvas 트랙·말 렌더러·raf 루프·탭 자동 일시정지 / T11 스킬 이펙트·배너 / T12 피니시 슬로모션·폭죽·포토 피니시). 이번 세션은 T9 진행 — 실제 그리기 전에 이월된 T6 메모 2·3·T7 메모 1·2·T8 메모 2를 흡수해 전체 완주 구간 프레임레이트 독립성·완주 15~30초·역전 5~10회차당 1회를 순수 로직으로 고정. DONE 미생성(M4~M6 미구현: Canvas 연출·React UI·효과음, PRD 3·4.5·4.6·4.7·4.8·4.9 코드 미대응). 결정 3건은 합의 완료 상태 유지, 신규 미해결 없음
- 2026-07-07T02:52:00Z evaluator: T8 PASS(12/12). vitest(finish 8개·전체 10 files 75 tests)·tsc --noEmit 직접 재실행 전부 통과, 기존 67개 회귀 없음. acceptance 전 항목(피니시 근접 임계값 이상/미만/경계, 포토 피니시 임계값 이내/초과/경계, 미완주 false, 기본 임계값 상수 사용)을 테스트가 실제 검증. finish.ts는 types만 의존하는 순수 모듈로 렌더 분리 준수, 변경 파일 모두 touch(src/sim/) 범위 내, types.ts 불필요 확장 없음. 비차단 메모 3건(완주 후 슬로모션 트리거 지속 정책 미고정, 러너 수 2 미만 방어 미도입의 M4/M5 통합 지점 확인, 임계값 실측 재조정 M4 이관). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. M3 마지막 태스크 완료, 다음은 M4
- 2026-07-07T03:10:00Z generator: T9(시뮬레이션 전체 구동기 & 연출 상수 실측·조정) 완료. src/sim/driver.ts(신규) — FIXED_SUBSTEP(1/60) 고정 스텝 accumulator(createAccumulator/advanceWithAccumulator, 원시 dt를 fixedStep 배수로만 소비해 나머지는 버퍼에 이월)와 완주까지 구동하는 순수 함수 runRaceToCompletion(안전 상한 maxTime=120초). src/sim/rng.ts(신규) — 테스트용 결정적 시드 PRNG(mulberry32). 실측 결과 기존 SPEED_SCALE(0.7)·발동 확률 계수는 완주 시간이 범위 하단(중앙값 16.57초, 일부 15초 미만)에 치우치고 역전 우승 빈도가 목표(10~20%)에 크게 못 미쳐(4.67~5%, 발동 가능 구간이 긴 스킬이 순위 무관 거의 항상 발동) SPEED_SCALE(→0.55), BASE_ACTIVATION_HAZARD·LUCK_HAZARD_WEIGHT(↓, 상위권 발동 억제)·RANK_HAZARD_WEIGHT(↑, 하위권만 발동 촉진)·스킬별 selfMultiplier(발동 시 체감 역전이 되도록 상향)를 조정. 조정 후 완주 시간 5마리 기준 18.6~24.6초(4~8마리 전체 범위 내 유지), 역전 우승 빈도 12~16%로 목표 범위 도달. driver.test.ts 7개 신규(전체 완주 구간 프레임레이트 독립성-스킬 발동 포함, 완주 시간 범위, 역전 우승 빈도, zone·slipstream 위치 반영 통합, 러너 수 하한 통합). npx vitest run(11 files, 82 tests)·tsc --noEmit 모두 통과, 3회 반복 실행으로 안정성 확인. M4의 첫 태스크 완료, 다음은 T10(Canvas 트랙·말 렌더러 + raf 렌더 루프 + 탭 자동 일시정지 연결)
- 2026-07-07T03:20:00Z planner: T9 PASS(12/12) 반영해 PLAN M4 T9 완료 표시. 원래 T10(Canvas 렌더러+raf 루프+탭 연결)이 한 세션 크기 초과이고 하네스가 Canvas 픽셀을 화면 검증할 수단이 없어, 아키텍처 원칙(로직·렌더 분리)에 맞춰 T10(렌더 레이아웃 순수 함수+raf 렌더 루프+탭 자동 일시정지 연결, 화면 없는 로직)과 T11(Canvas 2D 실제 렌더러)로 분해. 기존 스킬 이펙트·피니시 연출은 T12·T13으로 이동. 이번 세션은 T10 — 시뮬레이션 상태→화면 좌표 순수 매핑, 주입식 raf·시간·visibility 위 렌더 루프, 탭 자동 일시정지(합의: B) 연결을 vitest로 고정. T9 REVIEW 메모 2(maxTime 미완주 분기)를 T10 acceptance로 흡수, 메모 3(프레임레이트 독립성 전제)은 루프 설계 전제로 유지, 메모 1(역전 빈도 밴드)은 실제 연출 확인 가능한 T13으로 이관. DONE 미생성(M4~M6 미구현: Canvas 렌더링·연출·React UI·효과음, PRD 3·4.5·4.6·4.7·4.8·4.9 코드 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T03:12:00Z evaluator: T9 PASS(12/12). vitest(driver 7개·전체 11 files 82 tests)·tsc --noEmit 직접 재실행 전부 통과, 기존 75개 회귀 없음. acceptance 전 항목(전체 완주 구간 프레임레이트 독립성-스킬 발동 포함, 완주 15~30초-20개 시드, 역전 우승 빈도 200회차 범위, zone·slipstream 위치 반영 통합, 러너 수 하한≥2·피니시 판정 안전)을 테스트가 실제 검증. driver.ts는 engine·types만 의존하는 순수 모듈로 렌더 분리 유지, 변경 파일 모두 touch 범위 내이며 engine.ts(SPEED_SCALE)·skills.ts(발동 확률 계수·selfMultiplier·duration)는 "상수만 조정" 보고대로 소스 대조 확인. 비차단 메모 3건(역전 빈도 단언 폭[0.05,0.35] 목표 중심 대비 넓음, maxTime 미완주 분기 미검증, 프레임레이트 독립성 테스트는 동일 fixedStep 소비-accumulator 버퍼링 검증 성격 명시). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. M4 첫 태스크 완료, 다음은 T10
- 2026-07-07T03:30:00Z generator: T10(렌더 레이아웃 순수 함수+raf 렌더 루프+탭 자동 일시정지 연결) 완료. src/render/{types,layout,loop}.ts 신규 — computeRaceLayout(진행률→화면 좌표, 레인 배분, 순위표)와 createRenderLoop(raf/visibility 주입식 루프, T9 advanceWithAccumulator 연결, PAUSE/RESUME dispatch, 복귀 직후 dt 점프 미소비). layout.test.ts 9개·loop.test.ts 4개 신규, driver.test.ts에 maxTime 미완주 분기 1개 추가(T9 REVIEW 메모 2 해소). npx vitest run(13 files, 96 tests, 3회 반복 안정)·tsc --noEmit 모두 통과. 다음은 T11(Canvas 2D 실제 렌더러)
- 2026-07-07T03:35:00Z evaluator: T10 PASS(12/12). vitest(render/layout 9개·render/loop 4개·driver 8개·전체 13 files 96 tests)·tsc --noEmit 직접 재실행 전부 통과, 기존 82개 회귀 없음. acceptance 4개 검증 항목(레이아웃 매핑-단조·경계 클램프·레인 비중첩·순위표, 루프 전진-결정성 포함, 탭 자동 일시정지-PAUSE/RESUME·복귀 직후 큰 dt 미소비, maxTime 미완주 분기 finished=false 안전 종료)을 테스트가 실제 검증. layout.ts는 sim/engine·types만 의존하는 순수 함수로 DOM·ctx 미참조, loop.ts는 raf·visibility·machine을 주입 인터페이스로 격리해 requestAnimationFrame·document 직접 미참조하며 T9 advanceWithAccumulator로 fixedStep 소비(프레임레이트 독립성 전제 유지). 변경 파일 모두 touch 범위 내이며 프로덕션 driver.ts(mtime 02:58) 미변경을 mtime 대조로 확인("테스트만 추가" 보고 일치), loop 테스트는 실제 store/machine transition을 mock 없이 연결. 비차단 메모 3건(레인 비중첩 단언이 중심 단조성까지만-말 도형 높이 확정 시 밴드 폭 고정 검토, hidden 중 raf 재요청·onFrame 지속-T11 재그리기 정책 확정, getState 방어 복사 부재-불변 취급 전제). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. M4 T10 완료, 다음은 T11(Canvas 2D 실제 렌더러)
- 2026-07-07T03:40:00Z planner: T10 PASS(12/12) 반영해 PLAN M4 T10 체크·완료 표기. 다음 작업 T11(Canvas 2D 실제 렌더러: 트랙·말 도형+다리 모션·순위표·번호/이름 병기)을 TASKS 진행에 신규 정의 — ctx 주입 인터페이스로 감싸 mock ctx 스모크 검증. T10 REVIEW 비차단 메모 3건 배치: 메모1(레인 밴드 폭 ≥ 도형 높이)은 T11 acceptance로 흡수, 메모2(hidden 프레임 재그리기 정책)는 loop.onFrame 연결 시점(T13/M5)으로 이관, 메모3(getState 방어 복사)은 렌더 함수 프레임값만 소비 전제로 M5 React 브리지로 이관. DONE 미생성(M4 T11~T13·M5·M6 미구현)
- 2026-07-07T03:45:00Z generator: T11(Canvas 2D 실제 렌더러) 완료. src/render/renderer.ts(신규) — drawTrack(배경·게이트·결승선)·drawRunners(T10 layout 좌표 소비, 원+다리 모션, frameTime에 따라 다리 스윙 각 변화)·drawLeaderboard(순위·번호·이름 병기)·renderRace 진입점. types.ts에 RenderContext(ctx 최소 주입 인터페이스)·RunnerMeta·HORSE_BODY_RADIUS/HORSE_LEG_LENGTH/HORSE_SHAPE_HEIGHT(=26) 추가. renderer.test.ts 5개(트랙 게이트<결승선, 말 좌표 일치, 다리 모션 frameTime별 상이, 순위표 번호·이름 병기, renderRace 스모크) 신규, layout.test.ts에 레인 밴드 폭≥HORSE_SHAPE_HEIGHT 단언(러너 4~8마리) 추가해 T10 REVIEW 메모 1 해소. npx vitest run(14 files, 106 tests, 3회 반복 안정)·tsc --noEmit 모두 통과. 다음은 T12(스킬 발동 이펙트+배너)
- 2026-07-07T03:50:00Z evaluator: T11 PASS(10/12). vitest(render/renderer 5개·render/layout 14개·전체 14 files 106 tests)·tsc --noEmit 직접 재실행 전부 통과, 기존 96개 회귀 없음. acceptance 5개 검증 항목(주입 RenderContext로 DOM 없이 mock ctx 테스트·좌표 재구현 없음, 트랙 게이트<결승선·배경/stroke 2회, 말 arc 좌표 layout 일치·frameTime별 lineTo 인자 상이로 정지화면 아님 값 고정, 순위표 leaderboard 순서·번호·이름 병기, 레인 밴드 폭≥HORSE_SHAPE_HEIGHT 러너 4~8마리)을 테스트가 실제 검증. renderer.ts는 ./types만 의존해 sim·store·DOM 미참조, 러너 x/y는 layout에서만 소비, RunnerMeta를 별도 인자로 분리해 T10 레이아웃 타입 불변·layout.ts 미변경. 변경 파일 모두 touch 범위 내(renderer.ts·test 신규, types.ts·layout.test.ts 추가). 화면 없는 항목으로 채점(실제 canvas 마운트는 M5). 비차단 메모 3건(meta 불일치 시 DEFAULT_RUNNER_COLOR·축약 라벨 폴백 분기 미검증, 트랙 시각 여백 상수가 layout DEFAULT_MARGIN_RATIO와 이원화되어 어긋남 여지, 다리 모션 진폭·속도의 실화면 적정성은 M5/T13에서 확인). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. M4 T11 완료, 다음은 T12(스킬 발동 이펙트+배너)
- 2026-07-07T03:52:00Z planner: T11 PASS(10/12) 확인. M4 네 번째 태스크 T12(스킬 발동 이펙트+스킬명 배너) 선정·TASKS.md 신규 작성. 발동 이력(skillActivated·skillActivatedAt)·elapsedTime을 소비하는 src/render/effects.ts 순수 그리기 함수를 mock ctx로 검증(발동 창 판정·좌표 layout 일치·도메인 SKILL_CATALOG 스킬명 배너). T11 REVIEW 메모 1(폴백 분기 테스트) T12 acceptance로 흡수, 메모 2·3은 M5/T13 유지, T7 메모 3(흔들기 범위)은 T12 재검토. PLAN M4 T12 항목·비고 갱신
- 2026-07-07T03:58:00Z generator: T12(스킬 발동 이펙트+스킬명 배너) 완료. domain/horses.ts의 비공개 SKILLS를 SKILL_CATALOG로 export. render/types.ts에 SKILL_EFFECT_DURATION(1.2초)·SkillActivationInfo 추가. render/effects.ts(신규) — isSkillEffectActive(발동 창 판정, 부동소수점 오차 1e-9 허용)·drawSkillEffects(layout 좌표에 오라 3겹+스피드라인 4방향)·drawSkillBanner(SKILL_CATALOG 표시명 fillText)·renderSkillEffects 진입점. renderer.ts의 renderRace에 skillRunners 선택 인자 연결(drawRunners 이후·drawLeaderboard 이전). effects.test.ts 11개(발동 창 경계·좌표 일치·스킬명 2종 이상·스모크) 신규, renderer.test.ts에 T11 REVIEW 메모 1(DEFAULT_RUNNER_COLOR·축약 라벨 폴백) 단언 2건 추가. T7 REVIEW 메모 3(흔들기 범위)은 acceptance 범위 밖(본인 이펙트만 요구)이라 이번 태스크에서 sim 변경 없이 보류, 근거 IMPL.md 기록. npx vitest run(15 files, 119 tests)·tsc --noEmit 모두 통과, 기존 106개 회귀 없음. 다음은 T13(피니시 슬로모션+폭죽+스포트라이트+포토 피니시 연출)
- 2026-07-07T04:00:00Z evaluator: T12 PASS(13/15, 화면 있는 항목). vitest(render/effects 11개·render/renderer 7개·전체 15 files 119 tests)·tsc --noEmit 직접 재실행 전부 통과, 기존 106개 회귀 없음, IMPL 보고 일치. acceptance 4개 검증 항목(발동 창 판정 경계 시작/끝 포함·초과·미발동·null 값 고정, 이펙트 좌표 arc/moveTo 인자가 layout.runners와 정확 일치, 스킬명 배너 2종 서로 다른 표시명, 폴백 분기 drawRunners([])·drawLeaderboard([]) 예외 없이 동작)을 테스트가 실제 검증. effects.ts는 domain/horses·render/types만 의존해 sim·store·DOM 미참조, 좌표는 layout.runners에서만 소비, 이펙트 지속 창을 렌더 고유 상수 SKILL_EFFECT_DURATION(1.2s)으로 sim 결합 없이 분리. 축별 3/2/3/3/2: 화면 완성도는 실제 canvas 마운트 부재(M5)로 코드상 시각 설계(오라 3겹·스피드라인 4방향·배너 색/폰트/오프셋·렌더 순서)의 일관성으로 채점하고 실화면 확인은 M5 이관(채점 기준 REVIEW 기록). 모듈 경계 감점 사유: touch 목록 밖 domain/horses.ts를 SKILLS→SKILL_CATALOG rename+export(잔존 참조 0·값/순서 유지 최소 변경, acceptance가 이미 전제한 심볼명이라 정당성 인정). T7 REVIEW 메모 3(흔들기 범위)은 본인 이펙트만 요구하는 acceptance 범위 밖이라 sim 변경 없이 보류(근거 IMPL 기록, TASKS 허용 범위 내). 비차단 메모 4건(touch 범위 밖 도메인 변경 회고, effects.ts 방어 분기 3종 미검증, 실화면 시인성 M5 확인, drawSkillBanner save/restore 부재이나 후속 drawLeaderboard 재설정으로 무해). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. M4 T12 완료, 다음은 T13(피니시 슬로모션+폭죽+스포트라이트+포토 피니시 연출)
- 2026-07-07T04:05:00Z planner: T12 PASS(13/15, 화면 있는 항목) 반영해 PLAN M4 T12 완료 표기. 남은 피니시 연출(원래 T13)이 슬로모션·포토 피니시·폭죽·스포트라이트 네 요소로 한 세션 초과라 T13(슬로모션 timescale + 렌더 루프 통합 + 포토 피니시 연출)과 T14(폭죽 파티클 + 우승마 스포트라이트)로 분해. 이번 세션은 T13 신규 정의 — isSlowMotionTriggered 소비 dt 배율(0.3) 순수 함수를 주입식 createRenderLoop에 통합해 통합 테스트로 고정, isPhotoFinish 소비 포토 피니시/우승 배너를 mock ctx로 그림(src/render/finishFx.ts). T8 REVIEW 메모 1(완주 후 슬로모션 유지/해제 정책)·T12 REVIEW 테스트 충실도 메모(effects.ts 방어 분기 3종)를 T13 acceptance로 흡수. T12 화면 완성도 메모(실화면 시인성)·T9 메모 1(역전 빈도 밴드)은 실제 canvas 마운트 필요라 M5 유지. DONE 미생성(M4 T13·T14, M5 React UI·베팅·실황중계·설정, M6 효과음 미구현: PRD 3·4.3·4.5·4.6·4.7·4.8·4.9 코드 미대응). decisions 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T04:15:00Z generator: T13(피니시 슬로모션 timescale + 렌더 루프 통합 + 포토 피니시 연출) 완료. render/types.ts에 SLOW_MOTION_TIME_SCALE(0.3) 추가. render/finishFx.ts(신규) — computeSlowMotionTimeScale(sim isSlowMotionTriggered 위임, 임계값 이상 0.3배·미만 1.0)·drawFinishBanner(sim isPhotoFinish 위임, 접전이면 포토 피니시 문구·아니면 1위 러너 번호+이름 우승 배너·미완주면 미노출). loop.ts frame()에서 rawDt에 timeScale을 곱해 advanceWithAccumulator에 전달하도록 통합. loop.test.ts에 동일 raf 시퀀스 통합 테스트 1건(슬로모션 구간 elapsedTime 전진량이 배율만큼 작음을 결정적으로 단언) 추가. effects.test.ts에 T12 REVIEW 테스트 충실도 메모 3건(스킬 배너 skillId 부재·미지 skillId 폴백·이펙트 layout 좌표 부재 방어 분기) 프로덕션 코드 변경 없이 테스트만 추가. T8 REVIEW 메모 1(완주 후 슬로모션 유지/해제)은 별도 해제 로직 없이 트리거 조건의 자연스러운 귀결(완주 시 선두 진행률 항상 임계값 이상)을 그대로 정책으로 채택해 유지로 확정, finishFx.test.ts에 경계 테스트로 고정. drawFinishBanner는 touch 범위상 renderRace에는 연결하지 않음(M5). npx vitest run(16 files, 130 tests, 3회 반복 안정)·tsc --noEmit 모두 통과, 기존 119개 회귀 없음. 다음은 T14(폭죽 파티클 + 우승마 스포트라이트)
- 2026-07-07T04:16:00Z evaluator: T13 PASS(14/15, 화면 있는 항목). vitest(finishFx 7·loop 5·effects 14 직접 재실행 26/26·전체 16 files 130 tests)·tsc --noEmit 전부 통과, IMPL 보고(130/130) 일치, 기존 119개 회귀 없음. acceptance 6개 직접 검증: computeSlowMotionTimeScale이 sim isSlowMotionTriggered 위임(임계값 이상 0.3·미만 1.0, 진행률 재계산 없음)·경계값 정확 일치 고정, 완주 후 유지 정책 경계 테스트(선두 위치=TRACK_LENGTH→진행률 1.0→0.3 유지), createRenderLoop 통합(rawDt*timeScale)으로 동일 raf 시퀀스 슬로모션 elapsedTime이 배율만큼 작음을 toBeCloseTo+상한 결정적 단언, drawFinishBanner 접전/우승(번호+이름)/미완주 미노출 각각 단언, effects.ts 방어 분기 3종(skillId 부재·미지 skillId 폴백 노출·layout 좌표 부재) 단언. 축별 3/3/3/3/2: 화면 완성도만 2점(배너 시각 설계는 일관하나 renderRace 미연결로 실화면 배너 노출·순위표 겹침·가독성 픽셀 확인 불가, touch상 renderer.ts 제외·M5 이관). 모듈 경계·touch 위반 없음(finishFx.ts/test·loop.ts/test·types.ts 상수 1개·effects.test.ts만 변경, effects.ts 프로덕션 무변경). 비차단 메모 3건: 배너 실연결/슬로모션 체감 M5 확인(T9 메모 1 동반), drawFinishBanner save/restore 부재(T12 drawSkillBanner와 동일 성격), 프레임 단위 timeScale 단일 배율 근사(dt 작아 무해·acceptance 부합). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. M4 T13 완료, 다음은 T14(폭죽 파티클 + 우승마 스포트라이트)
- 2026-07-07T04:20:00Z planner: T13 PASS(14/15, 화면 있는 항목) 반영해 PLAN M4 T13 완료 표기. M4 마지막 태스크 T14(폭죽 파티클 + 우승마 스포트라이트)를 TASKS 진행에 신규 정의 — 파티클 생성·갱신은 주입 rng로 결정적인 순수 함수로 vitest 고정, 그리기는 주입 mock ctx 스모크, 스포트라이트는 우승마 layout 좌표만 소비(재구현 금지). T13 REVIEW 비차단 메모(drawFinishBanner save/restore 부재)는 신규 그리기 함수의 상태 격리를 선제 적용하는 선까지만 T14 acceptance로 흡수하고, 기존 배너의 상태 격리·실화면 시인성·프레임 단위 timeScale 근사는 여러 그리기 실연결 시점(M5)으로 유지. DONE 미생성(T14 미완료, M5 React UI·베팅·전적·실황중계·설정, M6 효과음 미구현: PRD 3·4.3·4.6·4.7·4.8·4.9 코드 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T04:30:00Z generator: T14(폭죽 파티클 + 우승마 스포트라이트 연출) 완료. render/particles.ts(신규) — createFireworkParticles(주입 rng로 각도·속력 결정, 같은 시드면 같은 파티클 집합)·updateParticles(dt로 위치 전진+중력 가속, 수명 소진 파티클 제거, rng 미사용 순수 갱신)·drawFireworkParticles(state.finished일 때만 그리며 save/restore 격리). render/finishFx.ts에 drawWinnerSpotlight 추가(layout.leaderboard 1위 id로 layout.runners 좌표만 소비, 미완주·좌표 불일치 시 조기 반환, save/restore 격리). render/types.ts에 FireworkParticle 타입과 연출 상수(FIREWORK_PARTICLE_COUNT·LIFESPAN·SPEED_MIN/MAX·GRAVITY·RADIUS·COLOR, WINNER_SPOTLIGHT_RADIUS·COLOR) 추가. particles.test.ts 9개(결정론·생성값·갱신 전진/소진 제거·완주시에만 그리기)·finishFx.test.ts에 스포트라이트 3개(우승마 좌표 일치·미완주 미노출·layout 불일치 방어) 신규. T13 REVIEW 비차단 메모(drawFinishBanner save/restore 부재)는 지시대로 기존 배너는 손대지 않고 신규 함수에만 상태 격리 적용. npx vitest run(17 files, 142 tests, 2회 반복 안정)·tsc --noEmit 모두 통과, 기존 130개 회귀 없음. T14 완료로 M4(Canvas 렌더링 & 연출) 전체 완결, 다음은 M5(React UI 레이어 & 실황 중계)
- 2026-07-07T04:35:00Z evaluator: T14 PASS(14/15, 화면 있는 항목). vitest(particles 9·finishFx 10 직접 재실행 19/19·전체 17 files 142 tests)·tsc --noEmit 전부 통과, IMPL 보고(142/142) 일치, 기존 130개 회귀 없음. acceptance 직접 검증: createFireworkParticles가 주입 rng로 x·y·vx·vy·remaining·max 파티클 생성(같은 시드→toEqual 결정론·다른 시드 반증), updateParticles가 dt로 위치 전진+수명 감소+소진 제거+rng 미소비 순수 갱신, drawFireworkParticles·drawWinnerSpotlight가 mock ctx로 DOM 없이 검증되며 스포트라이트는 layout.leaderboard 1위→layout.runners 좌표만 소비(arc 인자 100,50 toHaveBeenCalledWith 정확 일치·재구현 없음), 미완주 시 파티클·스포트라이트 미노출, 신규 함수 save/restore 격리 단언. 축별 3/3/3/3/2: 화면 완성도만 2점(파티클 색/반지름/개수/수명·스포트라이트 반투명 색/반지름 일관하나 renderRace/loop 미연결로 폭죽 밀도·대비·겹침 픽셀 확인 불가, touch상 renderer.ts·loop.ts 제외·M5 이관). 모듈 경계·touch 위반 없음(particles.ts/test·finishFx.ts drawWinnerSpotlight 추가·finishFx.test 스포트라이트 3건·types.ts 타입1+상수9만 변경, 기존 배너 함수 무변경). 테스트 충실도 비차단 메모: 중력 가속항·속력 범위 직접 단언 테스트 부재(vy=0으로 x·remaining만 확인, acceptance 최소항목 충족이라 무감점, M5 보강 여지). 비차단 메모: 실연결·실화면 시인성 M5, drawFinishBanner save/restore 부재 배너 포함 M5 일괄 확정. decisions 3주제 evaluator.md 기존재로 신규 기록 없음. T14 완료로 M4(Canvas 렌더링 & 연출) 전체(T9~T14) 완결, 다음은 M5(React UI 레이어 & 실황 중계)
- 2026-07-07T04:45:00Z planner: T14 PASS(14/15) 반영해 PLAN M4 완결 확인. M5(React UI 레이어 & 실황 중계)가 한 세션 크기 초과라 T15~T20으로 분해(T15 store 브리지·잔고/파산·저장 배너 / T16 실황 중계 문구·피드 / T17 로비 말 카드 / T18 베팅 패널 / T19 설정·정산 표시 / T20 Canvas 마운트·renderRace 실연결·전체 게임 루프 오케스트레이션). 하네스는 jsdom+@testing-library/react로 컴포넌트 DOM은 검증 가능하나 canvas 픽셀 실화면은 불가함을 PLAN에 명시. 이번 세션은 T15 신규 정의 — getState가 매 emit 새 객체를 반환하는 점(gameStore.ts:37)을 고려한 스냅샷 안정 브리지 훅, 잔고·파산 표시, load status "disabled" 시 저장 비활성 배너를 @testing-library/react로 고정. M4 이월 메모(여러 그리기 상태 격리·drawFinishBanner save/restore·hidden 재그리기 정책·역전 빈도 밴드·실화면 시인성)는 실연결 시점 T20으로 배치. DONE 미생성(M5 T15~T20·M6 미구현: 로비/베팅/실황중계/전적/설정/효과음·Canvas 실연결, PRD 4.1·4.3·4.4 UI·4.6·4.7·4.8·4.9 코드 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T04:50:00Z generator: T15(React↔store 구독 브리지 훅 + 잔고·파산 표시 + 저장 비활성 안내 배너) 완료. src/ui/useGameStore.ts(신규) — subscribe+useState 패턴 브리지 훅(useSyncExternalStore 대신 렌더 중 store 미참조로 스냅샷 불안정 회피, gameStore.ts 계약 무변경). src/ui/BalanceDisplay.tsx·StorageBanner.tsx(신규) — 잔고·파산 표시(브리지 직접 소비)·저장 비활성 배너(visible prop). App.tsx가 createPersistence→load→createGameStore 부트스트랩 수행, driver/store 선택적 주입 지원(테스트 DI). useGameStore.test.tsx 4개(브리지 반영·dispatch 반영·getState 신규 객체에도 유한 리렌더·동일 store 재구독 방지)·BalanceDisplay.test.tsx 2개·StorageBanner.test.tsx 2개·App.test.tsx 4개(제목/잔고 스모크, empty/ok/disabled 배너 노출) 신규. jsdom 환경에서 window.localStorage가 정상 동작하지 않는 하네스 이슈를 확인해 empty/ok 케이스는 주입 driver로 결정적으로 검증. npx vitest run(20 files, 153 tests)·tsc --noEmit·npm run build 모두 통과, 기존 142개 회귀 없음. T15 완료로 M5 착수, 다음은 T16(실황 중계 문구 모듈 + 피드 컴포넌트)
- 2026-07-07T04:55:00Z evaluator: T15 PASS(12/12, 화면 없는 항목으로 채점). 화면 완성도 축 미적용 근거를 REVIEW 채점 기준 메모에 기록 — acceptance가 jsdom DOM 출력·구독 반영까지만 요구하고 스타일은 touch 밖·T16~T20 명시 이관, 실브라우저 확인 불가(TASKS.md:57~60). acceptance 4개 하위 검증 직접 재실행: 브리지 반영(adjustBalance·dispatch→DOM), 스냅샷 안정성(getState 매 emit 새 객체여도 무한 리렌더 부재·렌더 카운트 상한·동일 store 재구독 1회), 잔고·파산(초기 10000/0·파산 후 재충전·횟수 증가), 저장 배너(disabled만 노출·ok/empty 미노출, throwing driver로 실패 경로 검증). vitest 20 files 153 tests·tsc --noEmit 0·npm run build 성공 직접 확인, IMPL 보고 일치. 축별 3/3/3/3: 브리지를 UI 계층(useGameStore.ts)에서만 해결해 gameStore 계약(T5) 무변경, touch 8파일 정확 준수(mtime 04:36~04:39, 그 외 src ≤04:23). 비차단 메모 3건(App store 주입 경로 storageDisabled 고정·StorageBanner 부트스트랩 1회 결정 의존·시각 완성도 T16~T20 이관). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 다음은 T16(실황 중계 문구 모듈 + 피드 컴포넌트)
- 2026-07-07T05:00:00Z planner: T15 PASS(12/12, 화면 없는 항목) 반영해 PLAN M5 T15 완료 표기. 다음 작업 T16(실황 중계 문구 모듈 + 중계 피드 컴포넌트)을 TASKS 진행에 신규 정의 — 이벤트 타입별(출발·선두 교체·스킬 발동·최종 직선·접전·결승선 통과, 각 2개 이상) 문구 풀에서 주입 rng로 결정적 선택하는 순수 모듈(src/ui/commentary.ts)과 최근 2~3줄만 유지하는 피드 컴포넌트(CommentaryFeed.tsx)를 @testing-library/react로 고정. 실제 경주 이벤트 감지·문구 emit 배선은 T20 몫으로 명시(T16은 sim·store 미참조 순수 모듈+표시 컴포넌트까지). T15 REVIEW 비차단 메모 2건(App props.store 주입 경로 storageDisabled 항상 false 고정·StorageBanner 부트스트랩 1회 결정 의존)을 PLAN T20 "T15 REVIEW 이월 메모 해소"로 배치. 시각 완성도 이관 메모는 T16 참고로 유지. DONE 미생성(M5 T16~T20·M6 미구현: 실황중계 UI·로비 말 카드·베팅 패널·설정·Canvas 실연결·효과음, PRD 3·4.1·4.3·4.6·4.7·4.8·4.9 코드 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T05:05:00Z generator: T16(실황 중계 문구 모듈 + 중계 피드 컴포넌트) 완료. src/ui/commentary.ts(신규) — PRD 4.6 여섯 이벤트 타입(출발·선두 교체·스킬 발동·최종 직선·접전·결승선 통과)별 문구 풀(각 2개)에서 주입 rng로 한 줄을 선택하고 {horseName}/{skillName} 파라미터를 치환하는 순수 함수(pickCommentaryLine). rng는 src/sim/rng.ts를 import하지 않고 () => number 함수 인자로만 받아 모듈 경계(TASKS.md 참고 1번: sim·store 미참조)를 지켰다. src/ui/CommentaryFeed.tsx(신규) — COMMENTARY_FEED_MAX_LINES(3) 상한으로 최신 메시지만 노출하는 순수 프레젠테이션 컴포넌트. commentary.test.ts 6개(이벤트 타입 6종 목록·문구 선택 결정론·rng 차이로 다른 문구·선두교체/스킬발동 파라미터 치환·6종 전체 무예외 반환)·CommentaryFeed.test.tsx 3개(빈 목록 렌더·상한 초과 시 최신 3개만·최신 항목 포함) 신규. src/ui/types.ts는 두 모듈이 자체 타입을 export해 불필요로 판단, 미생성. npx vitest run(22 files, 162 tests, 신규 9개 포함)·npx tsc --noEmit 모두 통과, 기존 153개 회귀 없음. T16 완료로 다음은 T17(로비 말 카드)
- 2026-07-07T05:10:00Z evaluator: T16 PASS(10/12, 화면 없는 항목으로 채점). CommentaryFeed는 컴포넌트지만 acceptance가 jsdom 마운트·상한 로직·시맨틱 마크업까지만 요구하고 시각 완성도는 TASKS 참고 3번으로 T20/M6 이관·실브라우저 확인 불가라 화면 완성도 축 미적용(REVIEW 화면 유무 판단에 근거 기록). acceptance 직접 재실행: vitest run(commentary.test 6·CommentaryFeed.test 3) 전부 통과, tsc --noEmit 0 확인. 축별 3/3/2/2 — 사양 충족 3(여섯 이벤트 타입·타입당 문구 2개 이상·주입 rng 결정적 선택·피드 상한 3 모두 acceptance 일치), 모듈 경계 3(commentary.ts sim·store 미import·rng 함수 인자만 의존, 변경 4파일 전부 src/ui touch 범위 내, types.ts 필요조건 미충족 미생성으로 위반 아님), 테스트 충실도 2(acceptance 최소 항목 값 단언 충실 커버하나 파라미터 누락 시 {horseName} 리터럴 노출되는 실패 경로 미방어 — 직접 재현 확인, commentary.ts:47-49), 운영 고려 2(신규 의존성 없이 T15 도입 testing-library·jsdom 재사용, IMPL 설계 결정 문서화 충실, 순수 프레젠테이션이라 배포 리스크 낮음). 비차단 메모 2건(미치환 템플릿 노출 방어를 T20 emit 배선에서 보장 검토·시각 완성도 T20/M6 이관). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 다음은 T17(로비 말 카드)
- 2026-07-07T05:15:00Z planner: T16 PASS(10/12, 화면 없는 항목) 반영해 PLAN M5 T16 완료 표기(REVIEW 미치환 템플릿 방어 메모는 T20 emit 배선으로 이관 명시). 다음 작업 T17(로비 말 카드)을 TASKS 진행에 신규 정의 — 카탈로그·회차 변동 스탯·저장 records·주입 rng로 HorseRaceEntry(도메인 기정의 타입)를 조립하는 순수 뷰모델 모듈(src/ui/lobbyEntries.ts, src/domain의 applyStatVariance·getConditionLevel·estimateWinProbabilities·calculateOdds 소비, sim·store 미참조)과 연승 판정 순수 함수, 말 카드 컴포넌트(HorseCard.tsx: 번호·이름 병기 PRD 5번, 배당률 소수1자리, 최근 5경기·전적·5단계 컨디션, 연승 배지)를 @testing-library/react로 고정. 회차 변동 갱신 시점·로비 전체 배치는 T18·T20 몫으로, 연승 임계값(연속 1위 2회 이상 권장)은 되돌리기 쉬운 결정으로 IMPL 근거화. DONE 미생성(M5 T17~T20·M6 미구현: 로비 말카드/베팅 패널/설정·정산/Canvas 실연결/효과음, PRD 4.1·4.3·4.7·4.8·4.9 UI·Canvas 실화면 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T05:20:00Z generator: T17(로비 말 카드) 완료. src/ui/lobbyEntries.ts(신규) — 출전마 카탈로그·저장 records·주입 rng를 받아 HorseRaceEntry[]를 조립하는 순수 함수 buildLobbyEntries(회차 변동 applyStatVariance·컨디션 getConditionLevel·추정승률 estimateWinProbabilities·배당률 calculateOdds는 src/domain 함수를 그대로 소비, src/sim·src/store 미참조). 연속 1위 수 산출 computeWinStreak와 배지 판정 hasWinStreakBadge, 임계값 상수 WIN_STREAK_BADGE_THRESHOLD(연속 1위 2회 이상, TASKS.md 권장값 채택)도 함께 추가. src/ui/HorseCard.tsx(신규) — HorseRaceEntry 한 건을 받아 번호+이름 병기(PRD 5번)·컨디션·배당률(소수 1자리)·전적(출전·우승·승률)·최근 5경기·연승 배지를 렌더링, 연승 판정은 lobbyEntries의 hasWinStreakBadge를 그대로 소비. lobbyEntries.test.ts 8개(조립 결정론·calculateOdds 일치 및 약체 고배당/강자 저배당·records 누락 시 빈 전적·저장 records 반영·연승 판정 3케이스·임계값 상수)·HorseCard.test.tsx 5개(번호·이름·배당률 형식·최근 5경기·전적 노출, 연승 배지 노출/미노출, 빈 전적·5경기 미만 무예외 렌더) 신규. JSX 인접 표현식이 DOM 텍스트 노드를 분리해 getByText 매칭이 깨지는 문제를 템플릿 리터럴 단일 문자열로 해결(CommentaryFeed·BalanceDisplay와 동일 패턴). src/ui/types.ts는 두 모듈이 자체 타입을 export해 불필요로 판단, 미생성. npx vitest run(24 files, 175 tests, 신규 13개 포함)·npx tsc --noEmit 모두 통과, 기존 162개 회귀 없음. 화면 배치·회차 갱신 배선은 T18·T20 몫이라 이 태스크 범위에서 실행 가능한 로비 화면은 없음. T17 완료로 다음은 T18
- 2026-07-07T05:22:00Z evaluator: T17(로비 말 카드) PASS(11/12, 화면 없는 항목으로 채점). HorseCard는 컴포넌트지만 acceptance가 jsdom 마운트·표시 필드·상태 파생·시맨틱 마크업까지만 요구하고 로비 전체 배치는 T18·T20 몫이라 실행 가능한 로비 화면이 없어 화면 완성도 축 미적용(REVIEW 화면 유무 판단에 근거 기록, T15·T16 동일 판단). acceptance 직접 재실행: vitest run(lobbyEntries.test 8·HorseCard.test 5=13) 전부 통과, 전체 24파일 175개 통과, tsc --noEmit 0 확인. 축별 3/3/3/2 — 사양 충족 3(순수 조립 buildLobbyEntries가 currentStats·condition·winProbability·odds·record 포함 HorseRaceEntry[] 반환, computeWinStreak·hasWinStreakBadge, 카드가 번호+이름 병기·배당률 소수1자리·최근5경기·전적·5단계 컨디션·연승 배지 노출 — 모두 acceptance 일치), 모듈 경계 3(lobbyEntries.ts domain+rng만 의존·sim/store 미참조, 도메인 함수 재구현 없이 소비, HorseCard가 hasWinStreakBadge 소비로 중복 없음, 변경 4파일 전부 src/ui touch 범위 내, types.ts 필요조건 미충족 미생성으로 위반 아님), 테스트 충실도 3(조립 결정론·calculateOdds 일치 및 약체 고배당/강자 저배당·빈 전적 폴백·저장 records 반영·연승 3케이스·임계값 상수·카드 표시·배지 노출/미노출·빈 및 5경기 미만 무예외 렌더 모두 값 단언, 실패 경로 폭넓게 방어), 운영 고려 2(신규 의존성 없이 도메인 함수·testing-library 재사용, 임계값 명명 상수 분리·IMPL 설계 결정 문서화, 순수 프레젠테이션이라 배포 리스크 낮음). 비차단 메모 2건(결정론 테스트가 상수 rng라 rng 소비 순서 미검증 — T18·T20 시드 PRNG 배선 시 강화 검토·시각 완성도 T20/M6 이관). decisions 3주제(canvas-rendering-library·horse-graphics·tab-inactive-handling) evaluator.md 기존재로 신규 기록 없음. 다음은 T18
- 2026-07-07T05:30:00Z planner: T17(로비 말 카드) PASS(11/12, 화면 없는 항목) 확인해 PLAN M5 T17 체크박스를 완료로 정정(TASKS·REVIEW는 완료·PASS였으나 PLAN만 미완료로 남은 불일치 해소). TASKS 진행 섹션의 T17을 완료로 이동하고 T18(베팅 패널)을 신규 정의 — src/ui/betValidation.ts(금액 검증 순수 함수: MIN_BET_AMOUNT를 src/store/gameStore.ts에서 import 재사용, 100 미만·잔고 초과·비정수 무효 판정, src/sim 미참조)와 src/ui/BetPanel.tsx(말 선택·프리셋 100/500/1,000/올인·직접입력·유효 시에만 확정 버튼 활성·확정 시 말 id·금액 콜백)를 @testing-library/react로 고정. 확정 이후 상태 전이·선차감(adjustBalance)·경주 생성 배선은 T20 몫으로 명시. 프리셋 클램프/비활성 정책과 즉시 검증 시점은 되돌리기 쉬운 표시 결정으로 IMPL 근거화. T17 REVIEW 비차단 메모 1(결정론 rng 소비 순서 검증)은 시드 PRNG 실배선 T20으로 유지, 메모 2(시각 완성도) T20/M6 이관 유지. DONE 미생성(M5 T18~T20·M6 미구현: 베팅 패널/설정·정산/Canvas 실연결/효과음, PRD 4.3·4.8·4.9·Canvas 실화면 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T05:38:00Z generator: T18(베팅 패널) 완료. src/ui/betValidation.ts(신규) — 금액·잔고를 받아 유효/무효와 무효 사유(최소 금액 미만·잔고 초과·정수 아님)를 반환하는 순수 함수 validateBetAmount. MIN_BET_AMOUNT는 src/store/gameStore.ts에서 import해 재사용(재정의 없음), src/sim·src/domain 미참조. src/ui/BetPanel.tsx(신규) — 라디오 버튼으로 말 한 마리 선택, 프리셋(100/500/1,000/올인)·직접 입력(number)으로 금액 설정, 매 변경 즉시 validateBetAmount로 검증해 무효 사유를 role="alert"로 노출, 말 선택+유효 금액일 때만 확정 버튼 활성화, 클릭 시 onConfirm(horseId, amount) 1회 호출. betValidation.test.ts 6개(최소 미만·잔고 초과·정수 아님 무효, 유효, 경계값 2건)·BetPanel.test.tsx 7개(미선택/무효 금액 확정 비활성, 유효 시 콜백 1회 호출, 올인=잔고, 잔고 초과 사유 노출, 프리셋 클릭 반영, 잔고 초과 프리셋 비활성화) 신규. jest-dom 미설치 확인 후 toBeDisabled() 대신 (element as HTMLButtonElement).disabled 직접 단언으로 대체. 설계 결정 2건을 IMPL.md에 근거 기록: (1) 잔고보다 큰 프리셋은 클램프 대신 비활성화(사용자 의도 왜곡 방지·구현 단순), (2) 직접 입력은 onChange마다 즉시 재검증(디바운스 없음). src/ui/types.ts는 두 모듈이 자체 타입 export로 불필요 판단, 미생성. npx vitest run(26 files, 188 tests, 신규 13개 포함)·npx tsc --noEmit 모두 통과, 기존 175개 회귀 없음. 베팅 패널 단독 컴포넌트까지가 범위라 로비 전체 배치·실브라우저 확인은 T20/M6 몫이라 실행 가능한 로비 화면은 없음. T18 완료로 다음은 T19(설정 화면 + 정산 결과 표시)
- 2026-07-07T05:31:00Z evaluator: T18(베팅 패널) PASS(11/12, 화면 없는 항목으로 채점). BetPanel은 컴포넌트지만 acceptance가 jsdom 마운트·상호작용·검증 로직·확정 게이팅까지만 요구하고 로비 전체 배치·실브라우저 시인성은 T20/M6 몫이라 실행 가능한 로비 화면이 없어 화면 완성도 축 미적용(T15~T17 동일 판단, REVIEW 화면 유무 판단에 근거 기록). acceptance 직접 재실행: vitest run(betValidation.test 6·BetPanel.test 7=13) 전부 통과, 전체 26파일 188개 통과, tsc --noEmit 0 확인. 축별 3/3/3/2 — 사양 충족 3(순수 검증 validateBetAmount가 최소미만·잔고초과·비정수 사유 반환·MIN_BET_AMOUNT를 store에서 import 재사용·sim 미참조, 패널이 라디오 선택·프리셋 100/500/1,000/올인·직접입력·유효 시에만 확정 활성·onConfirm(horseId,amount) 전달 — 모두 acceptance 일치), 모듈 경계 3(betValidation은 store 상수만·BetPanel은 domain 타입+betValidation만 의존, 상태전이·adjustBalance·경주생성 T20 이관 준수, 변경 4파일 전부 src/ui touch 범위 내, types.ts 필요조건 미충족 미생성으로 위반 아님), 테스트 충실도 3(경계값 정확히 100·정확히 잔고, 미선택/무효금액 게이팅, 콜백 1회·인자 단언, 올인=잔고, 잔고초과 사유 노출, 프리셋 반영/비활성 모두 값 단언), 운영 고려 2(신규 의존성 없이 testing-library·store 상수 재사용, jest-dom 미설치로 .disabled 직접 단언 대체, IMPL에 표시 결정 4건 문서화, 순수 UI라 배포 리스크 낮음). 비차단 메모 3건(amount===null 미입력 분기 직접 단언 부재·직접입력 소수 UI 경로 미검증·시각 완성도 T20/M6 이관). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 다음은 T19(설정 화면 + 정산 결과 표시)
- 2026-07-07T05:34:00Z planner: T18(베팅 패널) 직전 REVIEW PASS(11/12) 확인, 재작업 없음. TASKS 진행 섹션에 T19를 신규 정의하되, 원래 T19(설정 화면 + 정산 결과 표시)가 두 관심사(설정 UI / 정산 계산·표시)로 각각 순수 헬퍼+표시 컴포넌트를 요구해 한 세션 크기를 초과하므로 PLAN에서 T19(설정 화면)와 T19b(정산 결과 표시 + 순수 정산 계산)로 분해. 이번 사이클은 T19(설정 화면)만 선정 — src/ui/SettingsPanel.tsx: 출전마 수 4~8(MIN/MAX/DEFAULT_HORSE_COUNT를 src/persistence/schema.ts에서 재사용, 재정의 금지)·음소거 토글(상태 표시·변경 콜백까지, 실제 오디오는 M6)·데이터 초기화(확인 대화상자 필수, window.confirm 대신 jsdom 검증 가능한 인라인 확인 UI, 확인 시에만 초기화 콜백 1회·취소 시 미호출)를 @testing-library/react로 고정. 설정 변경·초기화의 실제 store settings 반영·카탈로그 재생성·저장 리셋은 T20 몫으로 명시(T15~T18 UI·순수 헬퍼 경계 동일). 확인 대화상자 구현 방식·출전마 수 입력 UI는 되돌리기 쉬운 표시 결정으로 IMPL 근거화 요청. T18 REVIEW 비차단 메모 2건(amount===null 미입력 alert 미노출·직접입력 소수 UI 경로)은 검증 가능한 이월 메모로 PLAN T20에 반영. DONE 미생성(M5 T19·T19b·T20·M6 미구현: 설정/정산/Canvas 실연결/효과음, PRD 4.3 정산·4.8·4.9·Canvas 실화면 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T05:40:00Z generator: T19(설정 화면) 완료. src/ui/SettingsPanel.tsx(신규) — GameSettings(horseCount·muted)와 bankruptcyCount를 props로 받아 출전마 수 select(옵션을 MIN_HORSE_COUNT~MAX_HORSE_COUNT로만 한정해 범위 밖 값 선택 원천 차단, 재정의 없이 src/persistence/schema.ts 상수 재사용)·음소거 checkbox 토글(값 변경 시 갱신된 GameSettings 전체를 onSettingsChange로 전달)·데이터 초기화(버튼 클릭 시 인라인 확인 영역 노출, "확인" 클릭 시에만 onReset 1회 호출·"취소" 클릭 시 미호출)를 렌더링. src/ui/SettingsPanel.test.tsx(신규) 7개(옵션이 정확히 4~8·출전마 수 변경 콜백 값·음소거 토글 반전 값·파산 횟수 표시·초기화 버튼 단독 클릭 시 미호출·확인 선택 시 1회 호출·취소 선택 시 미호출)를 @testing-library/react(fireEvent)로 작성. 설계 결정 2건을 IMPL.md에 근거 기록: (1) 출전마 수 입력은 select 드롭다운(범위 밖 선택 자체 불가, 가장 단순), (2) 초기화 확인은 window.confirm 대신 컴포넌트 내부 상태로 전환되는 인라인 2단계 확인(모달 대비 마크업 단순, jsdom 검증 용이). src/ui/types.ts는 SettingsPanelProps를 컴포넌트 파일에서 자체 export해 불필요로 판단, 미생성. npx vitest run(27 files, 195 tests, 신규 7개 포함)·npx tsc --noEmit 모두 통과, 기존 188개 회귀 없음. 실제 store settings 반영·카탈로그 재생성·초기화 리셋 배선은 T20 몫이라 다루지 않음. T19 완료로 다음은 T19b(정산 결과 표시 + 순수 정산 계산) 또는 T20
- 2026-07-07T05:44:00Z evaluator: T19(설정 화면) PASS(11/12, 화면 없는 항목으로 채점). SettingsPanel은 컴포넌트지만 acceptance가 jsdom 마운트·상호작용·확인 대화상자 게이팅까지만 요구하고 설정 화면 배치·실브라우저 시인성·로비/설정 전환 배선은 T20/M6 몫이라 열어 볼 실행 화면이 없어 화면 완성도 축 미적용(T15~T18 동일 판단, REVIEW 화면 유무 판단에 근거 기록). acceptance 직접 재실행: vitest run(SettingsPanel.test 7개) 전부 통과, 전체 27파일 195개 통과, tsc --noEmit 0 확인. 축별 3/3/3/2 — 사양 충족 3(GameSettings·bankruptcyCount props 수용, 출전마 수 select 옵션을 MIN~MAX_HORSE_COUNT로만 생성해 범위 밖 선택 원천 차단, 음소거 checkbox 토글, 초기화 버튼이 곧바로 onReset 호출 않고 인라인 확인 노출·"확인" 시에만 1회·"취소" 시 미호출 — acceptance 값 단언 포함 전 항목 일치), 모듈 경계 3(schema.ts의 GameSettings 타입+MIN/MAX 상수만 import·재정의 없이 파생, sim/domain 미참조, store 반영·카탈로그 재생성·리셋은 콜백 전달까지만으로 T20 이관 준수, 변경 2파일 전부 src/ui touch 범위 내, types.ts 필요조건 미충족 미생성으로 위반 아님), 테스트 충실도 3(옵션 정확히 4~8·출전마 수 변경 콜백 새 값(스프레드로 muted 보존)·음소거 반전 값·파산 횟수 표시·초기화 게이팅 3종(단독 미호출·확인 1회·취소 미호출+확인 영역 소멸) 값 단언), 운영 고려 2(신규 의존성 없이 testing-library·schema 상수 재사용, IMPL에 표시 결정 2건 문서화, 순수 UI라 배포 리스크 낮음). 비차단 메모 3건(확인 후 확인 영역 닫힘 직접 단언 부재·muted true→false 반전 경로 미검증·T20에서 store settings 반영/카탈로그 재생성/리셋 실배선 및 실브라우저 시인성 확인). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 다음은 T19b(정산 결과 표시 + 순수 정산 계산) 또는 T20
- 2026-07-07T05:48:00Z planner: T19(설정 화면) 직전 REVIEW PASS(11/12) 확인, 재작업 없음. PLAN M5 목록의 T19를 완료 표시. 다음 미완료 항목으로 T19b(정산 결과 표시 + 순수 정산 계산 함수, PRD 4.3·3장 정산)를 TASKS 진행 섹션에 신규 정의 — (1) 베팅액·배당률·적중 여부를 받아 지급액·잔고 증감을 산출하는 순수 정산 계산 함수(선차감 전제로 적중 시 +round(베팅액×배당률), 미적중 시 0; 반올림 정책·경계값 테스트 고정, src/domain/settlement.ts 권장·배치는 되돌리기 쉬운 결정), (2) 적중/미적중·지급액·정산 후 잔고를 표시하는 SettlementResult 컴포넌트(색상만 아닌 텍스트 병기, PRD 5번)를 jsdom 검증까지. 실제 경주 결과 조립·adjustBalance 반영·재충전(gameStore가 이미 처리)은 T20 이관 명시. odds.ts·MIN_BET_AMOUNT 상수 재사용, 재정의 금지. T19 REVIEW 비차단 메모 3건(초기화 확인 후 확인 영역 닫힘 회귀 단언·음소거 true→false 반전 경로·store settings 반영/카탈로그 재생성/리셋 실배선 및 실브라우저 시인성)은 PLAN T20 "T19 REVIEW 이월 메모 해소"로 반영. DONE 미생성(T19b·T20·M6 미구현: 정산 표시·Canvas 실연결·효과음, PRD 4.3 정산·4.8·Canvas 실화면 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음
- 2026-07-07T05:56:00Z generator: T19b(정산 결과 표시 + 순수 정산 계산 함수) 완료. src/domain/settlement.ts(신규) — calculateSettlement({betAmount, odds, won})가 적중 시 payout=Math.round(betAmount*odds)·balanceChange=payout, 미적중 시 0·0을 반환. 베팅액 최소값(MIN_BET_AMOUNT)·배당률 하한(1) 검증은 T18 betValidation·calculateOdds가 이미 보장하므로 재검증 없이 수식대로 계산하는 명시적 계약으로 결정(반올림 정책은 Math.round, 근거는 IMPL.md). src/domain/settlement.test.ts(신규 5개) — 적중/미적중 값 단언, 반올림 경계(333×1.5=499.5→500), 비정상 입력(베팅액<100·배당률<1) 계약 단언. src/ui/SettlementResult.tsx(신규) — {won,payout,balanceAfter} props로 적중/미적중 텍스트·지급액·정산 후 잔고를 렌더링(색상만으로 구분하지 않음, PRD 5번). src/ui/SettlementResult.test.tsx(신규 3개) — 적중/미적중 각각의 문구·값 노출, 두 상태 배타성을 @testing-library/react로 검증. npx vitest run(29 files, 203 tests, 신규 8개 포함, 기존 195개 회귀 없음)·npx tsc --noEmit(0) 모두 통과. 설계 결정 5건을 IMPL.md에 근거 기록(배치 src/domain, 반올림 Math.round, 비정상 입력 명시적 계약, props 형태, 지급액 항상 표시). 실제 경주 결과 조립·adjustBalance 반영은 T20 몫이라 다루지 않음. TASKS.md의 T19b를 완료로 표시. 다음은 T20(Canvas 마운트 + 렌더 루프 실연결 + 전체 게임 루프 오케스트레이션)
- 2026-07-07T05:52:00Z evaluator: T19b(정산 결과 표시 + 순수 정산 계산 함수) PASS(11/12, 화면 없는 항목으로 채점). SettlementResult는 컴포넌트지만 acceptance가 jsdom 마운트·값 노출·색상 외 텍스트 병기까지만 요구하고 정산 화면 배치·실브라우저 시인성·화면 전환은 T20/M6 몫이라 열어 볼 실행 화면이 없어 화면 완성도 축 미적용(T15~T19 동일 판단, REVIEW 화면 유무 판단에 근거 기록). acceptance 직접 재실행: vitest run(settlement.test 5개·SettlementResult.test 3개) 전부 통과, 전체 29파일 203개 통과, tsc --noEmit 0 확인. 축별 3/3/2/3 — 사양 충족 3(calculateSettlement 적중 payout=round(bet×odds)·balanceChange=payout·미적중 0/0 값 단언, 비정상 입력 명시적 계약·반올림 경계 100×3.25=325·333×1.5→500 단언, 컴포넌트 적중/미적중 문구·지급액·정산 후 잔고 값 노출과 배타성·텍스트 병기 접근성 충족, IMPL의 calculateOdds 배당률≥1 보장 주장을 수식 0.9/p≥1⟺p≤0.9로 직접 확인), 모듈 경계 3(settlement.ts import 전무 완전 순수·render/store/React 미참조, SettlementResult.tsx import 없이 sim 미참조·계산 결과만 소비, 타입 자체 export로 types.ts 미생성 위반 아님, 신규 4파일 전부 touch 범위 내·그 밖 변경 없음, adjustBalance 반영·재충전 재구현 안 하고 T20 이관), 테스트 충실도 2(값 단언·명시적 계약 고정·배타성 검증은 견고하나 acceptance가 강조한 반올림 경계를 올림 방향만 고정하고 내림 경계 미검증으로 Math.round 완전 고정 실패, 컴포넌트 getByText().textContent).toBe() 중복 패턴), 운영 고려 3(신규 의존성 없이 odds.ts·MIN_BET_AMOUNT 재사용, IMPL에 설계 결정 5건·반올림 정책·명시적 계약·T20 이관 경계 근거 문서화). decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 비차단 메모 3건(반올림 내림 경계 보강·컴포넌트 assertion 중복 정리·T20에서 정산 뷰모델 조립+adjustBalance 배선 및 넘기는 배당률의 소수점 반올림/raw 확정). 다음은 T20(Canvas 마운트 + 렌더 루프 실연결 + 전체 게임 루프 오케스트레이션)
- 2026-07-07T06:00:00Z generator: T20 착수 시도 중 BLOCKED(1회째). TASKS.md에 T20이 T9~T19b와 달리 정식 acceptance/touch 섹션 없이 PLAN.md 초안 설명만 존재함을 확인. PLAN.md 자체가 "한 세션 크기를 초과할 수 있으므로 선정 시점에 렌더 통합과 라이프사이클 오케스트레이션으로 다시 분해할 수 있다"고 명시하나, 지금까지 M4·M5의 모든 과대 항목(T9/T10, T12/T13, T18/T19 등)은 Planner가 사전에 분해해 TASKS.md에 정식 항목을 기록한 뒤 Generator가 착수했다. Generator는 TASKS.md·PLAN.md 구조를 체크박스·한 줄 메모 이상으로 바꿀 수 없어 T20을 스스로 분해하지 않고, TASKS.md 진행 섹션에 BLOCKED(1회째) 사유를 기록하고 종료. Planner의 T20 분해(acceptance·touch 신규 정의)가 필요하다.
- 2026-07-07T06:07:31Z evaluator: T19b 재검증 — 독립 재실행으로 이전 판단(11/12 PASS, 3/3/2/3) 유지 확인. vitest run(settlement.test 5·SettlementResult.test 3) 8개·전체 29파일 203개 통과, tsc --noEmit 0 직접 재확인. settlement.ts import 전무 완전 순수·SettlementResult sim 미참조, touch 4파일(mtime 05:49) 정확·범위 이탈 없음. calculateOdds 배당률≥1 보장(0.9/p≥1⟺p≤0.9) 수식 재확인. 화면 완성도 축 미적용 근거(실브라우저 실행 화면 T20 이관, acceptance는 jsdom 값 노출까지만) 유효. 테스트 충실도 2점 유지 — 반올림 올림 경계(333×1.5→500)만 고정하고 내림 경계 미검증·컴포넌트 assertion 중복. REVIEW.md 내용 재평가 결과와 일치해 갱신 불요. decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 다음은 T20(Canvas 마운트+렌더 루프 실연결+게임 루프 오케스트레이션)
- 2026-07-07T06:15:00Z generator: T20 착수 재시도 → BLOCKED(2회째). BLOCKED(1회째)(06:00:00Z) 이후 TASKS.md·PLAN.md를 재확인했으나 Planner의 T20 분해(정식 `### T20.` acceptance/touch 섹션 신규 작성)가 아직 반영되지 않아 동일한 사유로 재차 막힘. Generator 권한(체크박스·한 줄 메모만 허용)으로는 T20을 임의 축소해 착수할 수 없어, TASKS.md 진행 섹션의 BLOCKED 기록을 2회째로 갱신하고 종료. 같은 항목이 3회째 막히면 제약 조건에 따라 Planner가 docs/HALT를 생성해야 한다.
- 2026-07-07T06:20:00Z evaluator: 평가할 신규 구현물 없음. IMPL.md(05:51)·src/ 모두 T19b 평가 시각(05:52) 이후 변경 없음이 확인됨(직접 mtime 확인). 마지막 산출물 T19b는 이미 PASS(11/12) 평가 후 06:07 재검증 완료로 REVIEW.md 유효, 재작성 불요. 이후 Generator 두 사이클(06:00·06:15)은 T20 BLOCKED(1·2회째)로 코드 산출물 아님. 병목은 평가 단계가 아니라 Planner→Generator 인계 지점 — T20이 TASKS.md에 정식 acceptance/touch 섹션 없이 PLAN.md 초안만 존재해 Generator가 자체 분해 불가. Planner의 T20 분해(정식 ### T20. acceptance·touch 신규 작성)가 선행되어야 다음 평가 가능. decisions 3주제 evaluator.md 기존재로 신규 기록 없음. 동일 항목 3회째 BLOCKED 시 제약에 따라 Planner의 docs/HALT 생성 필요.
- 2026-07-07T06:30:00Z planner: T20 BLOCKED(2회째) 병목 해소 — 한 세션 크기를 크게 초과하는 T20을 PLAN.md에서 T20a(렌더 통합)·T20b(라이프사이클 오케스트레이션)·T20c(화면 컴포지션 + T15/T18/T19 이월 메모 해소)로 분해하고, 첫 하위 항목 T20a를 TASKS.md 진행 섹션에 T9~T19b와 동일한 정식 `### T20a.` acceptance/touch 섹션으로 신규 정의. T20a 범위: `loop.onFrame`이 소비할 순수 조합 함수 `renderScene`(computeRaceLayout→renderRace + 완주 시 drawFinishBanner·drawWinnerSpotlight·drawFireworkParticles, M4 함수 재구현 없이 소비, runnersMeta·skillRunners 파생 헬퍼)과 브라우저 ctx→`RenderContext` 어댑터 + `<canvas>` 마운트·loop 실연결 컴포넌트(RaceCanvas)를 mock ctx·가짜 raf로 검증까지. M4 이월 메모(drawFinishBanner save/restore 상태 격리·hidden 프레임 재그리기 정책·getState 참조 교체 안전 소비)를 T20a acceptance로 흡수. 라이프사이클 오케스트레이션·실황 emit·탭 일시정지 실연결은 T20b, App 실마운트·화면 전환·T15/T18/T19 이월 메모는 T20c로 배치. HALT 미생성(BLOCKED 3회째 아님·되돌리기 어려운 결정 없음). DONE 미생성(T20a~c·M6 미구현: Canvas 실연결·전체 게임 루프·효과음, PRD 3·4.6·4.8·Canvas 실화면 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음.
- 2026-07-07T06:45:00Z generator: T20a(주입식 RenderContext 어댑터 + renderScene 조합 함수 + <canvas> 마운트 컴포넌트) 완료. src/render/finishFx.ts — drawFinishBanner에 save()/restore()를 추가해(T13/T14 REVIEW 이월 메모) 포토 피니시/우승 배너의 font·fillStyle·textAlign·textBaseline이 이후 그리기로 누수되지 않게 격리(조기 return 제거, if/else로 재구성). src/render/finishFx.test.ts — save/restore 스택 동작을 흉내 내는 createStatefulMockCtx로 두 분기(우승 배너·포토 피니시) 모두에서 스타일이 호출 전 값으로 복원됨을 값 단언(신규 2개). src/render/renderScene.ts(신규) — computeRaceLayout으로 좌표를 얻어 renderRace를 호출하고, state.finished면 drawFinishBanner→drawWinnerSpotlight→drawFireworkParticles를 이 순서로 이어 호출하는 순수 조합 함수. HorseProfile[]→RunnerMeta[] 파생(deriveRunnersMeta), RaceState→SkillActivationInfo[] 파생(deriveSkillRunners) 헬퍼 포함. src/sim·src/domain은 타입만 import하고 src/store·src/sim 엔진(함수)은 import하지 않음(grep으로 직접 확인). src/render/renderScene.test.ts(신규 6개) — 미완주/완주 프레임 분기, 완주 시 세 그리기 호출과 layout 값 일치, 호출 순서, 파생 헬퍼 값, 빈 파티클 기본값 무예외를 vi.mock으로 finishFx·particles를 스파이 처리해 검증. src/ui/RaceCanvas.tsx(신규) — <canvas> 마운트 후 ctx(기본은 canvas.getContext("2d") 위임, getContext prop으로 테스트 주입 가능)로 createRenderLoop를 구동해 onFrame을 renderScene에 연결. 폭죽 파티클은 이 컴포넌트가 소유(완주 전환 시 우승마 layout 좌표에서 1회 생성, state.elapsedTime 차이를 dt로 매 프레임 갱신). hidden 프레임 렌더 정책은 "계속 그린다"로 결정(loop이 hidden 중에도 매 프레임 onFrame을 호출하고 상태가 불변이라 재그리기 비용이 낮으며, visibility 이중 구독으로 인한 mock 충돌을 피함). src/ui/RaceCanvas.test.tsx(신규 3개) — mock ctx·가짜 raf·가짜 visibility로 마운트 후 프레임 진행 시 그리기 호출, 언마운트 시 raf 취소로 그리기 중단, hidden 정책대로 계속 그려짐을 @testing-library/react로 검증. npx vitest run(31 files, 214 tests, 신규 11개 포함, 기존 203개 회귀 없음)·npx tsc --noEmit(0) 모두 통과. 설계 결정 4건을 IMPL.md에 근거 기록(ctx 어댑터 배치·hidden 정책·파티클 상태 소유·인자 스타일). 라이프사이클 오케스트레이션(베팅 확정→경주 생성→상태 전이→정산→실황 emit)은 T20b, App.tsx 실마운트·화면 컴포지션은 T20c 몫이라 다루지 않음. TASKS.md의 T20a를 완료로 표시하고 완료 섹션으로 이동, 진행 섹션에 T20b 착수 필요 메모 추가. 다음은 T20b(라이프사이클 오케스트레이션)
- 2026-07-07T07:00:00Z evaluator: T20a PASS(합계 12/12). acceptance 직접 실행 — renderScene.test(6)·finishFx.test(12)·RaceCanvas.test(3) 21개 통과, 전체 회귀 31 files 214 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 사양 충족 3(renderScene이 computeRaceLayout→renderRace + 완주 시 배너→스포트라이트→폭죽 정해진 순서 호출, 파생 헬퍼·save/restore 격리·hidden "계속 그린다" 정책·ctx 어댑터/마운트/loop 연결 모두 존재, 5개 최소 검증 시나리오까지 실행 검증), 모듈 경계 3(renderScene은 render 함수+도메인/sim 타입만, store/sim 엔진 미import를 grep으로 확인; 변경 파일 모두 touch 범위 내; finishFx 변경은 save/restore·if-else 재구성에 한정), 테스트 충실도 3(mock 스파이+실 renderRace 그리기 명령 병행 검증, createStatefulMockCtx로 스택 복원 값 단언, 실패 경로 커버), 운영 고려 3(신규 의존성 없음, 설계 결정 4건·미검증 항목·이월 메모 문서화). 채점 기준: jsdom canvas 미지원으로 픽셀 확인 수단 없는 렌더 배선 태스크이며 실화면 시인성은 TASKS.md가 미검증(감점 아님)으로 규정했으므로 화면 없는 항목 기준(9점 이상 PASS)으로 채점. decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: RaceCanvas useEffect 의존성 배열 참조 안정화(T20b/T20c 이월, 감점 아님), 실화면 시인성 T20c 수동 확인 필요.
- 2026-07-07T07:00:00Z planner: T20a REVIEW PASS(12/12) 확인, 재작업 없음. PRD 미해결 의사결정 없음(변경 없음). 다음 항목 T20b(전체 게임 루프 오케스트레이션: 베팅 확정→선차감→상태 전이→경주 생성→정산→실황 emit + 탭 일시정지 실연결)를 정식 acceptance/touch로 TASKS.md에 선정. T16 이월 메모(미치환 {horseName}/{skillName} 리터럴 방어)와 T20a 이월 메모(RaceCanvas useEffect 참조 안정화)를 T20b acceptance에 흡수. 화면 조립·App.tsx 실마운트·T15/T18/T19 이월은 T20c로 유지. M5 미완(T20b·T20c)·M6 미착수로 종료 판정 대상 아님.
- 2026-07-07T07:10:00Z generator: T20b(전체 게임 루프 오케스트레이션) 완료. src/ui/raceLifecycle.ts(신규) — 순수 헬퍼 3종: toRaceParticipants(로비 스냅샷 currentStats·skill.id→RaceParticipant[]), buildSettlementInput(베팅 말 스냅샷 odds+순위 1위 비교→SettlementInput), deriveRaceEvents(프레임 간 상태 비교로 여섯 실황 이벤트 도출, 대상 있는 이벤트는 카탈로그·SKILL_CATALOG 표시명으로 채우고 미매치 시 폴백 문구로 T16 이월 메모 해소). src/ui/types.ts(신규) — TimerSource(schedule/cancel) 인터페이스. src/ui/useGameController.tsx(신규) — 베팅 확정(선차감→START_COUNTDOWN)→(주입 타이머)→경주 생성(같은 로비 스냅샷 재사용)→START_RACE→handleFrame(매 프레임 실황 이벤트 emit, 완주 전환 시 1회만 FINISH→정산→SETTLE→(주입 타이머)→RESET)의 전체 라이프사이클 오케스트레이션 훅. store dispatch/getState().paused를 감싼 RenderLoopMachine 어댑터를 useMemo로 안정화. src/ui/RaceCanvas.tsx — onFrame 콜백 prop 추가(useRef로 최신값 참조, useEffect 의존성에는 미포함해 콜백 교체로 loop 재시작 안 함, T20a 이월 메모 해소). raceLifecycle.test.ts 10개·useGameController.test.tsx 5개(전체 라이프사이클·미적중·중복 완주 프레임 방지·실 document visibility로 탭 자동 일시정지 실연결·로비 재진입 재굴림)·RaceCanvas.test.tsx에 onFrame 관련 2개 신규. npx vitest run(33 files, 231 tests, 신규 17개 포함)·npx tsc --noEmit 모두 통과, 기존 214개 회귀 없음. 설계 결정 5건을 IMPL.md에 근거 기록(onFrame 단일 콜백, 카운트다운 3000ms·정산 표시 4000ms 기본값, 자동 지연 로비 복귀, 스냅샷 고정 재사용, 실 document 배선 검증 방법). 화면 조립·App.tsx 실마운트·T15/T18/T19 이월 메모 해소는 T20c 몫이라 다루지 않음. TASKS.md의 T20b를 완료로 표시하고 완료 섹션으로 이동. 다음은 T20c(로비/경주/정산 화면 컴포지션 + T15·T18·T19 이월 메모 해소)
- 2026-07-07T07:20:00Z evaluator: T20b PASS(합계 12/12). acceptance 직접 실행 — raceLifecycle.test(10)·useGameController.test(5)·RaceCanvas.test(5) 20개 통과, 전체 회귀 33 files 231 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 사양 충족 3(선차감→START_COUNTDOWN→주입 타이머→createRaceState→START_RACE→완주 시 FINISH→calculateSettlement→adjustBalance→SETTLE→RESET 전체 배선, 로비 스냅샷 재사용을 참가자 stats==로비 currentStats로 단언, 적중/미적중 잔고 반영, 탭 자동 일시정지는 실 document.visibilitychange로 store.paused 전이 검증, 실황 emit·T16 폴백·T20a loop 재시작 방지 커버), 모듈 경계 3(raceLifecycle.ts는 domain/sim/commentary 타입+순수 함수만 import·store/React 미참조를 grep 확인, 부수효과는 훅에 격리, RaceCanvas 단일 loop 유지, 변경 7파일 모두 touch 범위 내, 정산·순위·회차 재구현 없음), 테스트 충실도 3(잔고·지급액·스냅샷 동일성·폴백 이름 값 단언, 미적중 0·완주 중복 프레임 정산 1회 pendingCount 단언·미지 id 폴백·무이벤트 빈 배열 등 실패/엣지 커버, 시드 rng·가짜 타이머로 결정론, loop 재시작 방지 fillRect 횟수 단언), 운영 고려 3(신규 의존성 없음, 언마운트 시 타이머 정리, 타이머/rng/visibility 주입, 카운트다운 3000ms·정산 4000ms 되돌리기 쉬운 기본값+주입 가능으로 문서화). 채점 기준: 화면 컴포지션이 T20c로 분리된 오케스트레이션 로직 태스크이므로 화면 완성도 축 미적용·4축(9점 이상 PASS)으로 채점, 실화면 시인성은 TASKS.md 미검증 규정(감점 아님). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: T16 리터럴 미잔존은 raceLifecycle+commentary 테스트 합성으로 보장되나 컨트롤러 통과 문자열 직접 단언 통합 테스트는 없음(감점 아님, T20c 통합 스냅샷 권장), 실화면 시인성 T20c 수동 확인 몫.
- 2026-07-07T07:30:00Z planner: T20b REVIEW PASS(12/12, 화면 없는 항목) 확인, 재작업 없음. PLAN에서 T20b를 PASS 12/12로 표기하고, M5 T20의 마지막 하위 항목 T20c(로비/경주/정산 화면 컴포지션 + App.tsx 실마운트 + T15·T18·T19 이월 메모 해소)를 정식 `### T20c.` acceptance/touch로 TASKS.md 진행 섹션에 선정(직전 BLOCKED(1회째) 병목 해소). 범위: useGameController(T20b)가 노출하는 값을 소비해 phase(lobby/countdown/racing/finish/settlement)별로 HorseCard·BetPanel·BalanceDisplay·SettingsPanel·RaceCanvas·CommentaryFeed·SettlementResult·StorageBanner를 조립하고 App.tsx에 실마운트. 이월 메모 3건 흡수 — T15(props.store 주입 시에도 실제 저장 비활성 여부가 배너에 반영되게 통합, 부트스트랩 1회 결정 의존 재배선), T18(미입력 시 role=alert 미노출·소수 입력 "정수 아님" 사유 노출), T19(초기화 확인 영역 닫힘 회귀·onSettingsChange→store settings 반영·카탈로그 재생성·onReset→잔고·전적·설정·저장 리셋·음소거 반전 값 단언). T20b REVIEW 권장 메모(컨트롤러 통과 commentaryMessages에 {horseName}/{skillName} 리터럴 미잔존을 CommentaryFeed 통과 통합 스냅샷으로 직접 단언)도 T20c acceptance로 흡수. 실화면 시인성·역전 빈도 밴드는 픽셀 확인 수단 부재로 미검증(감점 아님, M6 npm run dev 수동 확인 몫). HALT 미생성(BLOCKED 3회째 아님·되돌리기 어려운 결정 없음). DONE 미생성(T20c·M6 미구현: 화면 컴포지션·효과음·전체 루프 마감, PRD 3·4.8·Canvas 실화면 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음.
- 2026-07-07T07:45:00Z generator: T20c(로비/경주/정산 화면 컴포지션 + App.tsx 실마운트 + T15·T18·T19 이월 메모 해소) 완료. src/App.tsx 전면 재작성 — bootstrap이 props.store 주입 여부와 무관하게 항상 persistence를 만들어 load()로 storageDisabled를 판정(T15 이월 해소), useGameController로 오케스트레이션을 배선하고 phase별로 로비(HorseCard×N·BetPanel·BalanceDisplay·SettingsPanel)/경주(RaceCanvas+CommentaryFeed, raceState 준비 후에만 RaceCanvas 마운트)/정산(SettlementResult) 화면을 조립. handleSettingsChange·handleReset을 신규 추가해 설정 변경·초기화를 실연결(T19 이월 해소) — gameStore.ts에 설정 갱신 API가 없고 touch 범위 밖이라, persistence.save()로 저장한 새 SavedState로 store 인스턴스를 통째로 교체하는 방식을 택함(카탈로그 재생성은 createGameStore 생성 시점 로직을 그대로 재사용). src/ui/useGameController.tsx — lobbyEntries 재계산 트리거에 storeState.horses 참조 변화 조건을 추가(phase 전이 조건과 OR), store 교체 시 phase는 이미 lobby라 기존 트리거만으로는 카탈로그 변경이 반영되지 않는 타이밍 버그를 재현 후 수정. src/App.test.tsx 전면 재작성(신규 12개: 화면 컴포지션 3, T15 이월 2, T18 이월 2, T19 이월 4, T20b REVIEW 권장 흡수 1) — manual raf·manual timer·시드 rng로 베팅→countdown→racing→finish→settlement 전체 라이프사이클을 결정적으로 구동해 SettlementResult 값과 CommentaryFeed 통합 스냅샷(리터럴 미잔존)을 직접 단언. src/ui/BetPanel.test.tsx·SettingsPanel.test.tsx에 이월 메모 단언 각 2건·1건 추가(프로덕션 코드는 이미 올바르게 동작해 무변경, REVIEW가 지적한 것은 단언 부재였음). 설계 결정 5건을 IMPL.md에 근거 기록. 지속적 자동 저장(잔고·전적 변경마다 저장)과 records(전적) 갱신 로직은 T20c acceptance 범위 밖이고 어떤 기존 태스크도 구현한 적이 없어(gameStore.ts 변경 필요, touch 밖) 다루지 않고 M6 마감 재검토 대상으로 남김. npx vitest run(33 files, 246 tests, 신규 15개 포함, 기존 231개 회귀 없음, 3회 반복 안정)·npx tsc --noEmit·npm run build 모두 통과. npm run dev로 서버 기동 확인(화면 실행 방법은 IMPL.md에 기록, 실브라우저 시인성은 TASKS.md 규정대로 미검증·감점 아님). T20c 완료로 M5(React UI 레이어 & 실황 중계, T15~T20) 전체 완결. TASKS.md 진행 섹션은 M6 착수를 위한 Planner의 다음 항목 정의가 필요하다는 메모로 갱신. 다음은 M6(효과음 & 마감) 첫 세션 항목 정의(Planner 몫).
- 2026-07-07T07:50:00Z evaluator: T20c PASS(합계 14/15, 화면 있는 항목·12점 이상 기준). acceptance 직접 실행 — App.test(16)·BetPanel.test(9)·SettingsPanel.test(8) 33개 통과, 전체 회귀 33 files 246 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 사양 충족 3(phase별 컴포지션 App.tsx:108-153, BetPanel.onConfirm→handleBetConfirm 선차감 9,900원·countdown 전이·raceState null 시 canvas 미마운트→flush 후 마운트, 전체 라이프사이클 결정적 구동 후 SettlementResult 적중/지급액/정산 후 잔고 단언, T15 props.store 주입 시에도 driver 접근 결과로 배너 값 단언, T18 미입력 무알림·소수 정수 아님, T19 카탈로그 5→7·음소거 반전·초기화 10,000/0 복귀+driver.data 검사·저장 실패 배너, T20b 자막 리터럴 미잔존 실화면 스냅샷), 모듈 경계 3(App은 컴포지션 계층 한정·정산/순위/배당/전이 재구현 없음, 변경 파일 모두 touch 범위 내, useGameController는 lobbyEntries 트리거에 storeState.horses 참조 조건 OR 추가한 표면 최소 확장·오케스트레이션 로직 무변경, gameStore.ts 무변경), 테스트 충실도 3(마운트 DOM 동작·값 단언, 저장 계층 driver.data 파싱 확인, 실패·엣지 경로 다수, manual raf/timer·시드 rng 결정론), 운영 고려 3(신규 의존성 없음, tsc·build·vitest 통과, 설계 결정 5건 문서화, 범위 밖 공백 지속 자동 저장·records 갱신을 은폐 없이 M6 재검토로 명시), 화면 완성도 2(시맨틱 랜드마크·aria-label로 발견 가능성 충족하나 프로젝트 전체 CSS 전무로 시각 정돈 없음 — T20c 스코프 밖·프로젝트 전반 공백·TASKS.md 88-90 M6 이관·감점 아님이라 구조 완성도만으로 2점). 판단 기준: SettlementResult balanceAfter는 SettlementOutcome에 필드 없어 controller.balance(정산 후 잔고) 해석 인정. decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: 스타일/시각 완성도 기준선 부재(프로젝트 전반, M6 판단)·지속 자동 저장·records 갱신 미구현(gameStore.ts 변경 필요, M6 재검토)·음소거 반영 단언의 간접성(체크박스 checked 경유, 감점 아님). M5(T15~T20) 전체 완결, 다음은 M6 항목 정의(Planner 몫).
- 2026-07-07T08:00:00Z planner: T20c REVIEW PASS(14/15) 확인, 재작업 없음. M5 전체 완결로 다음 미완료 마일스톤 M6를 PLAN.md에서 T21~T25로 분해 — T21(경주 완주 시 전적 records 갱신)·T22(지속적 자동 저장 배선)·T23(Web Audio 효과음+음소거 실반영+자동재생 정책)·T24(시각 완성도 기준선 CSS 도입)·T25(전체 루프 통합 검증+PRD 9번 성공 기준 확인·종료 판정). T20c REVIEW가 다음 사이클로 넘긴 최우선 메모(전적 갱신·지속 저장 미구현, PRD 4.4·4.7·9번 실제 공백)를 T21·T22로, 스타일 기준선 부재를 T24로, 음소거 반영 간접성을 T23으로 배치. 이번 세션은 T21을 정식 `### T21.` acceptance/touch로 TASKS.md 진행 섹션에 신규 정의 — 완주 순위(RankedRunner[])→RaceRecord(출전 수·우승 수·최근 5경기) 갱신 순수 함수(src/domain), gameStore 갱신 표면(순수 함수 소비, adjustBalance·재충전 무변경), useGameController 정산 경로(state.finished 최초 진입) 배선까지. 새로고침 유지(지속 저장)는 T22 몫으로 명시. 코드 대조로 공백 확인: gameStore.ts records는 로드만·경주 후 불변, useGameController handleFrame(:180)이 rankings 확보하나 전적 미갱신, records 키·rankings id 모두 말 id로 정합. HALT 미생성(BLOCKED 없음·되돌리기 어려운 결정 없음). DONE 미생성(M6 T21~T25 미구현: 전적 갱신·지속 저장·효과음·스타일·통합 마감, PRD 4.4·4.7·4.8·9번 저장 지속·효과음 코드 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음.
- 2026-07-07T08:10:00Z generator: T21(경주 완주 시 전적 records 갱신) 완료. src/domain/records.ts(신규) — updateRecordsWithRaceResult(records, results) 순수 함수. 완주 순위({id, rank} 형태의 로컬 RaceResultEntry[], RankedRunner가 구조적으로 만족)로 각 말 RaceRecord를 갱신한 새 객체를 반환 — racesRun +1, rank===1이면 wins +1, recentResults 맨 앞 삽입 후 RECENT_RESULTS_LIMIT(5)만 유지, 순위 밖 키 불변, 신규 출전 말은 racesRun=1·wins=우승 여부·recentResults=[rank]로 생성. 입력 방어 복사(갱신 대상만 새 객체, 나머지는 참조 유지, gameStore.ts saved.records 관례와 정합). src/domain/records.test.ts 6개 테스트(기존 말 갱신·미우승 wins 불변·6번째 결과 밀림·신규 출전 생성·다건 갱신 시 무관 말 불변·입력 불변). src/store/gameStore.ts — GameStore에 recordRaceResult(rankings: RankedRunner[]) 추가, 내부 records를 let으로 바꿔 순수 함수 결과로 재대입 후 emit(adjustBalance·dispatch·재충전 로직 무변경). gameStore.test.ts 2개 테스트 추가(호출 후 records 일치·잔고/파산/설정 참조/phase 불변, 구독자 emit 확인). src/ui/useGameController.tsx — handleFrame의 정산 진입 지점(state.finished && !finishHandledRef.current && bet)에서 dispatch("FINISH") 직후 store.recordRaceResult(rankings) 한 줄 추가(정산 계산·상태 전이·실황 emit 로직 재작성 없음). useGameController.test.tsx — 기존 전체 라이프사이클 테스트에 완주 후 우승마/나머지 말 records 값 단언 추가, 기존 "완주 프레임 1회 정산" 테스트에 recordRaceResult spy로 중복 handleFrame에도 정확히 1회 호출 확인. 설계 결정 3건 IMPL.md 기록(순위 입력 타입을 로컬 구조적 타입으로 분리해 src/sim import 회피, RECENT_RESULTS_LIMIT을 export 상수로 배치, recordRaceResult 호출을 FINISH 직후·정산 계산 이전에 배치). npx vitest run(34 files, 254 tests, 신규 8개, 기존 246개 회귀 없음)·npx tsc --noEmit 모두 통과. 화면 없는 항목이라 서버 실행 확인 생략(TASKS.md 규정). 지속 저장(새로고침 후 records 유지)은 T22 몫으로 명시적으로 남김. 다음은 T22(지속적 자동 저장 배선) 선정(Planner 몫).
- 2026-07-07T08:30:00Z planner: T21 REVIEW PASS(12/12) 확인, 재작업 없음. PLAN M6 T21 완료 표기. 이번 세션은 T22(지속적 자동 저장 배선)를 정식 `### T22.` acceptance/touch로 TASKS.md 진행 섹션에 신규 정의 — GameStoreState→SavedState 투영 순수 함수(version=SAVE_SCHEMA_VERSION, phase·paused·horses 배제), App.tsx가 현재 store에 자동 저장 구독 부착(store 교체 시 재부착)·SaveResult.disabled로 storageDisabled 갱신, 새로고침 라운드트립(load() 복원) 단언, 저장 실패 시 배너 노출. 코드 대조로 공백 확인: gameStore.subscribe/persistence.save 존재하나 진행 중 store 변경→save 배선 부재(App.tsx는 설정 변경·초기화에서만 수동 save), 정산 시 adjustBalance 잔고·recordRaceResult 전적 변경이 미저장. createPersistence 폴백·disabled·손상 리셋은 재구현 금지·소비만. HALT 미생성(BLOCKED 없음·되돌리기 어려운 결정 없음). DONE 미생성(M6 T22~T25 미구현: 지속 저장·효과음·스타일·통합 마감, PRD 4.4·4.7·4.8·9번 저장 지속·효과음 코드 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음.
- 2026-07-07T08:20:00Z evaluator: T21(경주 완주 시 전적 records 갱신) PASS(합계 12/12, 화면 없는 항목·9점 이상 기준). acceptance 직접 실행 — records.test(6)·gameStore.test(13)·useGameController.test(5) 통과, 전체 회귀 34 files 254 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 사양 충족 3(순수 함수 records.ts:16-31 입력 불변·값 단언, store 표면 gameStore.ts:71-74 순수 함수 소비만·규칙 재구현 없음, 컨트롤러 정산 경로 useGameController.tsx:184 완주 순위 1회 호출, 전체 라이프사이클 구동 후 records 반영 단언 useGameController.test.tsx:113-117), 모듈 경계 3(records.ts import는 ./types뿐·src/sim·store·React 미참조, adjustBalance·재충전 무변경, 컨트롤러 한 줄 배선, 5개 변경 파일 모두 touch 내), 테스트 충실도 3(우승/미우승 wins 분기·recentResults 상한 5 경계·신규 출전 생성·순위 밖 불변·입력 불변/참조 동일성, store 잔고/파산/설정 참조/phase 불변 단언, vi.spyOn으로 중복 handleFrame 시 정확히 1회 호출 단언), 운영 고려 3(신규 의존성 없음, tsc·vitest 통과, 설계 결정 3건 문서화, 지속 저장 공백을 T22 몫으로 명시). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: 전적 갱신이 bet 가드 아래라 무베팅 완주 시 미갱신(현 아키텍처상 실질 문제 없음·감점 아님, 향후 관전 모드 시 분리 검토)·지속 저장은 T22 몫으로 정상 이관·실화면 시인성은 T24/T25 몫. 다음은 T22(지속적 자동 저장 배선) 선정(Planner 몫).
- 2026-07-07T08:40:00Z generator: T22(지속적 자동 저장 배선) 완료. src/persistence/projection.ts(신규) — toSavedState(state: PersistableState): SavedState 순수 함수, version=SAVE_SCHEMA_VERSION·balance·bankruptcyCount·records·settings만 옮기고 phase·paused·horses는 결과에서 배제(GameStoreState를 import하지 않고 구조적 타이핑으로 소비, 상위 계층 store가 하위 계층 persistence를 참조하는 기존 방향 유지). projection.test.ts 2개 테스트(정상 투영 값 일치, 초과 필드 배제 키 단언). src/App.tsx — useEffect로 현재 store에 자동 저장 구독 부착(의존성 [store, persistence]로 store 교체 시 재부착), 매 emit마다 toSavedState 결과를 persistence.save에 전달하고 SaveResult.disabled로 storageDisabled 갱신. 저장 빈도는 매 emit 저장(단순)을 택함(emit이 dispatch/adjustBalance/recordRaceResult 시점에만 발생해 프레임마다 발생하지 않음, dedup은 store 교체 시 비교 기준 리셋 복잡성 회피). handleSettingsChange의 수동 SavedState 조립도 toSavedState로 정리(그 외 수동 저장 로직은 유지 — 구독은 부착 이후 emit부터만 반응해 store 교체 직전 수동 저장과 시점이 겹치지 않음). App.test.tsx에 T22 describe 블록 4개 통합 테스트 추가(라이프사이클 후 저장값 일치, 새로고침 라운드트립 load() 복원, store 교체 후에도 자동 저장 유지, 진행 중 저장 실패 시 배너 노출). npx vitest run(35 files, 260 tests, 신규 6개, 기존 254개 회귀 없음)·npx tsc --noEmit 모두 통과. 화면 시인성은 T24/T25·실브라우저 확인 몫으로 명시적으로 남김. 다음은 M6 나머지 태스크(효과음·스타일·통합 마감) 선정(Planner 몫).
- 2026-07-07T08:50:00Z evaluator: T22(지속적 자동 저장 배선) PASS(합계 12/12, 화면 없는 항목·9점 이상 기준). acceptance 직접 실행 — projection.test(2)·App.test(20) 통과, 전체 회귀 35 files 260 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 사양 충족 3(순수 투영 toSavedState projection.ts:17 version=SAVE_SCHEMA_VERSION·4필드 이동·phase·paused·horses 배제, App 자동 저장 구독 App.tsx:77-83 deps [store,persistence]로 store 교체 시 재부착, 새로고침 라운드트립 App.test.tsx:387-392 status=ok·잔고·전적 복원, store 교체 후 지속·저장 실패→disabled→배너 모두 값 단언), 모듈 경계 3(projection.ts가 store 미import·구조적 타이핑 PersistableState로 상위→하위 의존 방향 유지·import는 domain/types·schema뿐, 저장 부수효과 App.tsx 격리, gameStore.ts subscribe 해제 함수를 useEffect cleanup으로 반환·로직 무변경, 변경 4파일 touch 내), 테스트 충실도 3(초과 필드 배제 Object.keys 단언 projection.test.ts:34, 4개 통합 테스트가 실제 구독 경로 구동·라이프사이클 후 저장 JSON 파싱 일치·라운드트립·store 교체 지속·실패 경로 flaky driver role=alert 포함), 운영 고려 3(신규 의존성 없음, 설계 결정 3건 IMPL·TASKS 문서화, 매 emit 저장 타당성을 handleFrame emit 경로 추적으로 직접 검증 — 프레임마다 save 미발생 확인). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: 완주 시점 연속 4 emit 중복 저장(비차단·감점 아님·향후 dedup 검토)·구독 누수 방지 간접 검증(useEffect cleanup으로 실질 위험 없음)·실화면 시인성 T24/T25 이관. 다음은 M6 나머지(T23 효과음·T24 스타일·T25 통합 마감) 선정(Planner 몫).
- 2026-07-07T09:00:00Z planner: T22 REVIEW PASS 반영. PLAN 완료 누락 갱신(M5·T20a·T20c 체크 [x]). M6 진행: T23(효과음)이 오디오 엔진+이벤트 배선으로 한 세션 초과라 T23a(신시사이즈 사운드 엔진+음소거+자동재생 게이팅, 화면 없는 로직)·T23b(게임 이벤트 배선)로 분해. T23a를 TASKS 진행으로 선정 — src/audio 신규, jsdom AudioContext 미구현 대응해 주입식 오디오 백엔드 뒤에 감싸 mock으로 게이팅·음소거·5종 사운드 노드 생성 검증. 종료 판정 보류(T23~T25 미완, PRD 4.8 효과음·시각 완성도 미대응).
- 2026-07-07T09:10:00Z generator: T23a(Web Audio 신시사이즈 사운드 엔진 + 음소거 실반영 + 자동재생 게이팅) 완료. src/audio/{types,webAudioBackend,soundEngine}.ts(신규) — createSoundEngine(backendFactory?, options?) 팩토리가 5종 사운드(출발 팡파르·스킬 발동·피니시 함성·정산 승/패의 1회성 play(name) + 발굽 루프 startLoop/stopLoop)를 oscillator/gain으로 신시사이즈. AudioContext 생성은 enable() 호출 전까지 지연(자동재생 게이팅), 모든 사운드는 마스터 게인을 거쳐 destination에 연결되고 음소거 시 마스터 게인 0으로 출력 차단(생성 옵션 muted·setMuted 세터). src/audio/soundEngine.test.ts(신규, mock AudioBackend 기반 9개 테스트) — 게이팅(enable 이전 무시)·노드 생성·연결·호출순서·음소거 값·루프 제어(중복 시작 방지) 단언. npx vitest run(36 files, 269 tests, 신규 9 + 기존 260 회귀 없음)·npx tsc --noEmit 모두 통과. src/persistence/schema.ts GameSettings는 읽기 전용 참조 없이 boolean 구조 호환으로만 유지, store/sim/render/domain/ui 미변경(touch 범위 준수). 게임 이벤트 배선(useGameController 연결)은 T23b의 몫으로 남김. TASKS.md T23a [x] 완료 이동·요약본 압축.
- 2026-07-07T09:20:00Z evaluator: T23a(Web Audio 신시사이즈 사운드 엔진 + 음소거 실반영 + 자동재생 게이팅) PASS(합계 11/12, 화면 없는 항목·9점 이상 기준). acceptance 직접 실행 — src/audio soundEngine.test(9) 통과, 전체 회귀 36 files 269 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 사양 충족 3(5종 1회성 ONE_SHOT_TONES soundEngine.ts:34-40 + 발굽 루프 HOOFBEAT_TONE:42·startLoop/stopLoop:92-108 신시사이즈, 자동재생 게이팅은 enable() 이전 백엔드 미생성 구조 보장:53-59·조기반환:88-93 값 단언 test:57-67, 음소거 마스터 게인 0 반영:57·:86 test:104-130), 모듈 경계 3(src/audio가 domain/sim/store/ui/React 미참조·import는 ./types·./webAudioBackend뿐, types/backend/engine 3파일 분리로 new AudioContext() 격리 webAudioBackend.ts:10, GameSettings 타입 커플링 회피 muted:boolean, 변경 4파일 모두 touch 내), 테스트 충실도 2(게이팅·1회생성·연결/start-stop 순서·음소거 값·enable 이전 setMuted·루프 멱등/재시작·기본 팩토리 지연평가로 실패·경계 경로 충실하나, play가 start-fanfare 한 이름만 구동해 나머지 4종 이름→톤 매핑 osc.type/frequency.value 적용과 엔벨로프 스케줄링 setValueAtTime/linearRamp:71-73 미단언 — 이름별 매핑 회귀를 현 테스트가 미탐지·1점 감점), 운영 고려 3(외부 오디오 파일·신규 의존성 없음, jsdom 미실행·실브라우저 수동 확인 몫을 webAudioBackend.ts:1-6 주석·IMPL 명시, 되돌리기 쉬운 결정 5건 문서화). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: 이름별 톤 매핑·엔벨로프 스케줄링 단언 갭을 T23b 이벤트 배선 시 흡수 권장·엔진 enable() 발동 시점(handleBetConfirm 첫 입력)은 T23b 책임·실화면 소리 시인성 T25 이관. 다음은 T23b(게임 이벤트 사운드 배선) 선정(Planner 몫).
- 2026-07-07T09:30:00Z planner: T23a REVIEW PASS(11/12) 반영. PLAN 완료 누락 갱신(T22·T23a 체크 [x], PASS 주석). M6 진행: T23b(게임 이벤트에 사운드 연결 + 음소거 토글 실반영 배선)를 TASKS 진행으로 선정 — T23a 사운드 엔진을 useGameController에 배선. handleBetConfirm에서 enable(자동재생 게이팅), deriveRaceEvents 6종 중 start→start-fanfare+hoofbeat 루프 시작·skill-activation→skill-activation·finish→finish-cheer+hoofbeat 루프 정지, 정산 적중 여부로 settlement-win/lose, settings.muted 변경 시 setMuted 실반영. 엔진은 주입 옵션으로 두어 mock SoundEngine으로 배선 결정 검증. T23a REVIEW 테스트 갭(나머지 4종 이름→톤 매핑·엔벨로프 단언 부재)을 이 태스크에 흡수하도록 PLAN·acceptance에 명시. 종료 판정 보류(T23b·T24·T25 미완, PRD 4.8 이벤트 배선·시각 완성도 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음.
- 2026-07-07T09:40:00Z generator: T23b(게임 이벤트에 사운드 연결 + 음소거 토글 실반영 배선) 완료. src/ui/useGameController.tsx — SoundEngine 주입 옵션 추가(기본값은 useRef 지연 초기화로 훅 내부에서 createSoundEngine() 1회 생성), handleBetConfirm에서 sound.enable() 호출(자동재생 게이팅, jsdom AudioContext 미구현 대응 try/catch), handleFrame 실황 이벤트 루프에서 start→start-fanfare+hoofbeat 시작·skill-activation→skill-activation·finish→finish-cheer+hoofbeat 정지, 정산 적중 여부로 settlement-win/lose 재생, useEffect([sound, settings.muted])로 초기값 포함 setMuted 반영. src/ui/useGameController.test.tsx 신규 5개 테스트(enable 순서·skill-activation·finish+win·lose·muted 초기값/양방향 토글). src/audio/soundEngine.ts는 ONE_SHOT_TONES·ATTACK_SECONDS export만 추가(로직 무변경), soundEngine.test.ts에 T23a REVIEW 갭 흡수(나머지 4종 이름→톤·엔벨로프 스케줄 it.each 단언) 추가. App.tsx는 controllerOptions를 그대로 전달하는 기존 구조라 변경 불필요로 판단(IMPL.md에 근거 기록). npx vitest run(36 files, 278 tests, 신규 9 + 기존 269 회귀 없음)·npx tsc --noEmit 모두 통과. TASKS.md·PLAN.md T23b [x] 완료 이동.
- 2026-07-07T10:00:00Z evaluator: T23b(게임 이벤트에 사운드 연결 + 음소거 토글 실반영 배선) PASS(합계 12/12, 화면 없는 항목·9점 이상 기준). acceptance 직접 실행 — useGameController.test(10)+soundEngine.test(13) 통과, 전체 회귀 36 files 278 tests 통과, tsc 0으로 IMPL.md 보고와 일치. 배선 지점 코드 대조: handleBetConfirm의 sound.enable() try/catch 게이팅(useGameController.tsx:168-174), handleFrame 이벤트 루프 start→start-fanfare+startLoop hoofbeat·skill-activation→skill-activation·finish→finish-cheer+stopLoop hoofbeat(:209-219), 정산 outcome.won으로 settlement-win/lose·finishHandledRef 완주당 1회 가드(:223-234), useEffect([sound, settings.muted]) 초기값 포함 setMuted 실반영(:160-162). PRD 4.8 5종(팡파르·발굽루프·스킬·피니시·정산 승/패) 전부 대응, lead-change·final-stretch·close-race는 PRD·엔진에 전용 톤 없어 미배선이 정확. 사양 충족 3, 모듈 경계 3(소비 계층이 SoundEngine 표면만 호출·deriveRaceEvents/calculateSettlement 재구현 없음·변경 4파일 touch 내·soundEngine.ts export만 추가·App.tsx 미변경 근거 문서화), 테스트 충실도 3(T23b 5테스트가 enable→play 순서·skill·finish+win·lose 반대단언·muted 초기값/양방향 토글 rerender로 커버 + 이전 REVIEW 지적 이름→톤·엔벨로프 갭을 soundEngine.test.ts:189-212 it.each 값 단언으로 완전 흡수, jsdom enable catch 분기는 sound 미주입 기존 회귀가 실엔진 통과로 간접 검증), 운영 고려 3(외부 오디오·신규 의존성 없음·jsdom 대응/수동 확인 몫 명시·되돌리기 쉬운 결정 3건 문서화·PLAN/TASKS 이관). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: T24(화면 있는 항목·CSS)·T25(M6 마감) acceptance/touch가 TASKS에 미비 — 다음 Planner가 채워야 함, 실화면 소리 시인성은 T25·수동 확인 이관.
- 2026-07-07T10:10:00Z planner: T23b REVIEW PASS(12/12) 반영, 재작업 없음(PLAN T23b는 이미 [x]). 직전 REVIEW 다음 사이클 메모(T24·T25 정식 acceptance/touch 미비)에 따라 다음 미완료 항목 T24(시각 완성도 기준선, CSS 도입)를 TASKS.md 진행 섹션에 정식 `### T24.` acceptance/touch로 신규 정의. 코드 대조로 공백 확인: `grep -rl "className|.css|style="` src → 없음, src/ 전체에 스타일 전무(PLAN "시각 완성도 부재" 실재). acceptance는 vitest 전체 회귀 무결(번호·이름 병기 테스트 유지)·tsc 0·npm run build 성공(vite 번들에 CSS 반영)·클래스 존재 DOM 단언 최소 1건, 실픽셀 시인성은 npm run dev 수동 확인 몫(하네스 수단 없음·감점 아님). touch는 src/index.css(또는 module)·main.tsx·App.tsx·src/ui 화면 컴포넌트 className 부여로 한정, domain/sim/render/store/persistence/audio 무변경·접근성 마크업 무변경. PLAN T24에 화면 있는 항목(화면 완성도 축·12점 이상 PASS)·CSS 방식은 되돌리기 쉬운 Generator 결정임을 명시. T25(M6 마감·종료 판정)는 T24 완료 후 채운다(최소 변경). HALT 미생성(BLOCKED 없음·되돌리기 어려운 결정 없음). DONE 미생성(M6 T24·T25 미완: 시각 완성도·전체 루프 통합 검증, PRD 4.5 연출 마감·9번 성공 기준 최종 대조 미대응). 결정 3건 합의 완료 유지, 신규 미해결 없음.
- 2026-07-07T10:20:00Z generator: T24(시각 완성도 기준선, CSS 도입) 완료. src/index.css(신규, 전역 CSS 방식)에 리셋·색/간격/타이포 토큰·시맨틱 클래스 규칙 작성, main.tsx에 import 배선. App.tsx(main.app·app__title·lobby·horse-list·race-screen)와 HorseCard·BetPanel·SettingsPanel·CommentaryFeed·SettlementResult(승패 수정자 클래스 포함)·BalanceDisplay·StorageBanner·RaceCanvas(canvas)에 className 부여, 번호·이름 병기·aria-label 등 접근성 마크업은 무변경. App.test.tsx에 시맨틱 클래스 DOM 단언 2건 신규(로비 루트/HorseCard/잔고/설정/베팅 패널, 정산 화면 승패 수정자). npx vitest run(36 files, 280 tests, 기존 278 회귀 없음)·npx tsc --noEmit(0)·npm run build(vite 번들에 CSS 포함) 모두 통과. domain/sim/render(로직)/store/persistence/audio 무변경(touch 범위 준수). 다음은 T25(M6 마감·전체 루프 통합 검증)
- 2026-07-07T10:30:00Z evaluator: T24(시각 완성도 기준선, CSS 도입) PASS(합계 15/15, 화면 있는 항목·12점 이상 기준). acceptance 직접 실행 — npx vitest run 36 files/280 tests 통과(기존 278 + 신규 2 회귀 없음)·npx tsc --noEmit 0·npm run build 성공(dist/assets/index-BSBGryl_.css 3.92kB gzip 1.07kB 번들 생성 확인)으로 IMPL.md 보고와 일치. 사양 충족 3(App.test.tsx T24 블록 2건: 정적 시맨틱 클래스 존재 querySelector/classList 단언 + 실제 베팅→경주→정산 라이프사이클 통과 후 settlement-result--won/--lost 수정자 단언), 모듈 경계 3(스타일 UI 계층 한정·로직 6개 디렉터리 domain/sim/render/store/persistence/audio 무변경 확인·RaceCanvas className 한 줄은 T24 범위 설명 포함으로 위반 아님), 테스트 충실도 3(클래스 존재+통합 구동 두 각도, jest-dom 미설정 관례 classList.contains 일관), 운영 고려 3(main.tsx import 배선·build 번들 반영 실측·색/간격/타이포 CSS 커스텀 프로퍼티 토큰 중앙화·전역 CSS 결정 문서화), 화면 완성도 3(색 토큰 체계+공용 .card+타이포 계층 일관 정돈 index.css:1-252, settlement 색+텍스트 병기로 PRD 5·13번 접근성 유지, 실픽셀 대비 미검증 감점 아님). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: T25(M6 마감·전체 루프 통합 검증·PRD 9번 대조) 정식 acceptance/touch TASKS 미정의 — 다음 Planner가 채움, 실화면 시인성은 npm run dev 수동 확인·T25 이관.
- 2026-07-07T10:45:00Z planner: T24 REVIEW(PASS 15/15) 반영 — PLAN.md M6의 T24 체크 완료 표시. TASKS.md 진행 섹션에 M6 마감 태스크 T25(전체 루프 통합 검증 + PRD 9번 성공 기준 확인)의 정식 acceptance/touch를 신규 작성(T24는 완료 섹션으로 이동). T25 acceptance: (1) App을 주입 driver·controllerOptions·raceCanvasOverrides로 마운트해 베팅→경주→정산→로비 전체 순환을 연속 2회 이상 오류 없이 구동하는 종단 통합 테스트, (2) 라이프사이클 후 같은 driver로 새 persistence.load()가 잔고·전적을 복원하는 새로고침 라운드트립, (3) 잔고<100 경로에서 adjustBalance가 10,000 재충전·파산 횟수 증가, (4) PRD 9번 4개 축을 어떤 테스트가 고정하는지 IMPL.md 대조 표 기록·픽셀 미검증 항목 분리 명시. 검증·마감 중심으로 새 프로덕션 기능은 추가하지 않고 기존 경로 소비, 대조 중 발견되는 간극만 최소 변경. DONE 판정은 T25 PASS 후 다음 사이클이 PRD 전체 재대조로 수행. 미해결 의사결정 없음(3주제 합의 완료), HALT/DONE 없음. 다음 Generator 작업: T25.
- 2026-07-07T11:00:00Z generator: T25(전체 루프 통합 검증 + PRD 9번 성공 기준 확인, M6 마감) 완료. 새 프로덕션 기능·간극 수정 없음(대조 결과 기존 경로가 이미 PRD를 충족). src/App.test.tsx에 신규 describe 2건 추가 — (1) "T25: 전체 루프 통합 검증": 주입 driver·controllerOptions·raceCanvasOverrides로 App을 마운트해 베팅→카운트다운→경주→완주→정산→(정산 표시 경과)→로비 복귀를 연속 2회 오류 없이 구동, 매 회차 종료 후 베팅 패널 재노출·정산 결과 소멸을 값으로 단언(1회차 정산이 2회차 시작을 막지 않음). (2) "T25: 파산 자동 재충전 통합 검증": 잔고 150 시드 store에 올인 베팅 확정 시 선차감 즉시 파산 판정(잔고 10,000 재충전·파산 횟수 1)이 발동하고, 이어지는 경주 구동→정산→로비 복귀까지 루프가 끊기지 않음을 확인. 새로고침 라운드트립은 기존 T22 통합 테스트("새로고침 라운드트립: 같은 driver로 새 persistence를 만들어 load()하면 정산 후 잔고·전적이 복원된다")가 이미 종단 관점에서 고정하고 있어 신규 중복 없이 인용으로 대체. docs/IMPL.md에 PRD 9번 4개 축 대조 표(각 축을 고정하는 테스트 목록)와 픽셀 화면 전용 미검증 항목(역전 체감·슬로모션 부드러움·폭죽 밀도·이펙트 색/타이밍·자막 타이밍·색 대비)을 기록. npx vitest run(36 files, 282 tests, 기존 280 + 신규 2 회귀 없음)·npx tsc --noEmit(0)·npm run build(성공) 모두 통과. M6 마지막 태스크 완료, PLAN.md 남은 미완료 태스크 없음(다음 Planner 사이클이 PRD 전체 재대조 후 DONE 여부 판정).
- 2026-07-07T11:15:00Z evaluator: T25(전체 루프 통합 검증 + PRD 9번 성공 기준 확인, M6 마감) PASS(합계 12/12, 화면 없는 항목·9점 이상 기준). acceptance 직접 실행 — npx vitest run 36 files/282 tests 통과(기존 280 + 신규 2 회귀 없음)·npx tsc --noEmit 0·npx vite build 성공(71 modules, dist/assets/index-MpucUPLp.js 168.90kB gzip 56.45kB)으로 IMPL.md 보고와 일치. 변경 파일 src/App.test.tsx·docs/IMPL.md뿐으로 프로덕션 소스 무변경(간극 없음 주장 확인). 사양 충족 3(App.test.tsx:571-617 연속 2회 순환 종단 테스트가 매 회차 후 베팅 패널 재노출·정산 결과 소멸을 값·부정 양방향 단언, 새로고침 라운드트립은 T22:360-393이 같은 driver로 새 createPersistence(driver).load() 호출해 잔고·전적 복원 실단언으로 4a 종단 충족·인용 대체 타당), 모듈 경계 3(touch 정확 준수·프로덕션 6개 디렉터리 및 오케스트레이션 훅 무변경·기존 헬퍼 재사용 배치 IMPL 근거 기록), 테스트 충실도 3(파산 재충전 테스트 619-663이 잔고 150 시드 올인으로 실패 경로 재현→잔고 10,000 재충전·파산 횟수 1·재충전 후 루프 지속 화면 텍스트 단언, 인용 근거 driver.test.ts:86 역전 빈도 밴드·loop/particles/finishFx/effects/commentary 실재 확인), 운영 고려 3(의존성 무변경·build 번들 실측·IMPL.md:33-79 PRD 9번 4개 축 대조 표와 픽셀 미검증 항목 분리 기록). decisions 3주제 모두 evaluator.md 기존재로 신규 기록 없음. 다음 사이클 메모: T25 PASS로 M6 전 태스크 완료 — 다음 Planner가 PRD 전체 재대조 후 DONE 판정, 픽셀 미검증 항목(역전 체감·슬로모션·폭죽 밀도·이펙트 색/타이밍·자막·색 대비)은 npm run dev 수동 확인 몫(감점 아님). 참고(감점 아님): 2회 순환 테스트가 실황 emit·탭 일시정지 회귀를 자체 단언하지 않고 개별 테스트에 의존 — 종단 내 실황 피드 렌더 직접 단언 시 커버리지 강화.
2026-07-07T09:12:00Z planner: PRD 전체를 코드와 재대조 완료 — 4.1~4.9·5·6·8번 모든 요구가 실제 src/ 모듈에 대응함을 확인. TASKS/PLAN 미완료 없음, 직전 REVIEW(T25) PASS. 남은 간극 없어 docs/DONE 생성.
