# 집중/휴식 시간 커스터마이즈 여부 (Evaluator 독립 선택)

선택: A

## 근거

PRD 3.2~3.3은 25/5/15분을 확정 값으로 제시하며, 3절 핵심 기능 어디에도
설정 화면이나 시간 변경 기능이 없습니다. PRD 1절이 도구의 성격을 개인용·
단일 사용자·정적 웹 앱으로 좁게 규정하고, 6절은 v1 범위를 명시적으로
축소하고 있습니다. 이 방향에서 시간 커스터마이즈(후보 B)는 설정 화면,
검증 로직, localStorage 스키마 확장을 새로 요구하여 v1의 단순성 목표와
충돌합니다. 값은 상수 모듈(`src/constants.ts`)로 격리해 두면 향후 B로의
확장 비용도 낮으므로, v1에서는 고정(A)이 목표에 부합합니다.
