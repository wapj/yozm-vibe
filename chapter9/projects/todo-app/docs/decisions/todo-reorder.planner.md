선택: A

## 근거

- PRD 6절이 v1 범위 제외를 명시하는 방향과 일치하며, 순서 변경은 v1
  핵심 가치(뽀모도로 집중 기록)와 직접 관련이 없다.
- 최소 변경 원칙. A는 추가 UI와 `order` 필드가 필요 없어 데이터 모델을
  단순하게 유지한다.
- B·C는 `order` 필드 도입으로 저장 스키마가 커지고, 정렬·마이그레이션
  부담이 늘어난다. 후속 버전에서 도입해도 되돌리기 쉽다.
