# 결정: 할일 순서 변경 방식

## 배경

PRD 3.1은 할일 관리를 정의하나 순서 변경 방식은 미결이다. 기본 정렬은
생성 순서(`createdAt`)로 한다는 점만 확정되어 있다.

## 후보

- A: v1 제외. 생성 순서 고정. 순서 변경 UI 없음.
- B: 위/아래 이동 버튼. 할일마다 `order` 필드를 두고 버튼으로 교환.
- C: 드래그 앤 드롭. 라이브러리 또는 HTML5 DnD로 재정렬.

## 합의

(run.py가 채웁니다)
