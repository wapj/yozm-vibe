# RSS Wiki — JOURNAL

- 2026-04-24T14:59:45Z TASKS.md 작성 후 프로젝트 초기화(uv 패키지, 디렉토리, .gitignore, pytest) 완료
- 2026-04-24T15:01:19Z 런타임 의존성(fastapi/uvicorn/jinja2/pydantic/feedparser/trafilatura/apscheduler/python-multipart/markdown) 추가 및 import 스모크 테스트
- 2026-04-24T15:02:29Z 개발 의존성(pytest-asyncio, httpx) 추가 및 import 스모크 테스트
- 2026-04-24T15:03:39Z config.py에 PRD §14 운영 상수(경로/타임아웃/병렬도/LLM 백오프/위키 예산) 정의 및 값 검증 테스트 추가
- 2026-04-25T00:00:00Z db.py에 get_connection 팩토리(외래키 ON, Row factory) + feeds/categories 스키마와 idx_categories_parent 인덱스 생성 + 12개 테스트 추가
- 2026-04-24T15:07:17Z db.py에 articles 테이블·3개 인덱스·articles_fts 가상 테이블과 AI/AD/AU 트리거 추가, FTS 색인/업데이트/삭제 동기화 및 cascade/ON DELETE SET NULL 동작 검증 테스트 10건 추가
- 2026-04-25T06:00:00Z db.py에 wiki_pages(category_id UNIQUE, CASCADE)와 job_logs(+idx_joblogs_started) 테이블 추가 및 컬럼/기본값/제약 검증 테스트 8건 추가
- 2026-04-24T15:11:01Z models.py에 Feed/Category/Article/WikiPage/JobLog Pydantic 모델(status/job_type Literal, attempt_count ge=1) 추가 및 sqlite Row → model_validate 검증 테스트 14건 추가
- 2026-04-25T00:13:00Z prompts/article_summarize.txt, prompts/wiki_rebuild.txt를 PRD §15 그대로 작성하고 Jinja2 StrictUndefined 환경에서 카테고리 목록/입력 글/증분·최초 빌드 분기 렌더 스모크 테스트 5건 추가
- 2026-04-24T15:17:34Z pipeline/llm.py에 claude -p --output-format text subprocess 래퍼(stdin 전달, 120s wait_for 타임아웃, returncode/stderr rate-limit 감지) + 2/4/8s 최대 3회 지수 백오프 구현 및 성공·재시도·소진 경로 테스트 8건 추가
- 2026-04-24T15:19:59Z pipeline/llm.py에 JSON 파싱 폴백 체인(strip→코드펜스 제거→정규식 `\{[\s\S]*\}` 추출) 및 파싱 실패 시 "이전 응답이 JSON이 아니었다" 지시문을 덧붙여 1회만 재호출하는 call_claude_cli_json 래퍼 구현 및 테스트 10건 추가
- 2026-04-25T00:30:00Z pipeline/fetcher.py에 feedparser async 래퍼(to_thread + wait_for 30s), Semaphore(5) 병렬 fetch_feeds(실패는 FetchError로 격리, 입력 순서 유지), 신규 URL 판별 filter_new_entries(기존 URL 제외 + 배치 내 중복 제거) 구현 및 테스트 15건 추가
- 2026-04-25T00:45:00Z pipeline/extractor.py에 trafilatura async 래퍼(fetch_url+extract를 to_thread 로 offload, 전체 wait_for 20s), 실패·예외·빈 결과·타임아웃 시 raw_summary fallback, 둘 다 없으면 source="none" 리턴 구현 및 테스트 12건 추가
- 2026-04-25T15:30:18Z pipeline/summarizer.py에 기존 카테고리(merged_into_id IS NULL) 주입 프롬프트 렌더링, call_claude_cli_json 호출(주입 가능), 카테고리 upsert(name UNIQUE), articles INSERT(+commit), LLMError/LLMJSONParseError/summary·category 누락/공백/빈 본문 시 status='failed' 레코드 삽입 구현 및 테스트 16건 추가
- 2026-04-25T16:00:00Z pipeline/rebuilder.py에 카테고리 위키 재구성(call_claude_cli 텍스트 출력 사용, new_articles+existing_recent 제외 중복, previous 빈 문자열 시 초기 빌드 20→10→5→3→0, 증분 10→5→3→0 100k자 가드), wiki_pages upsert(has_unread_updates=1, last_seen_at 보존), status='failed'/skipped 분기 구현 및 테스트 20건 추가
- 2026-04-25T16:30:00Z pipeline/cycle.py에 전체 수집 사이클(run_cycle: 활성 피드→fetch_feeds→filter_new_entries→순차 extract/summarize→affected 카테고리당 1회 rebuild) 구현, FetchError/summarize failed/rebuild failed 를 job_logs 로 기록하고 feeds.last_fetched_at·consecutive_failures 갱신, 외부 의존 주입 가능하게 설계 및 테스트 9건 추가
- 2026-04-24T15:44:57Z scheduler.py에 BackgroundScheduler 빌더(cron hour='*' minute=0, FETCH_JOB_ID, max_instances=1, coalesce) + 전역 asyncio.Lock fetch_lock + run_fetch_cycle(lock/connection_factory/run_cycle_fn 주입 가능, 잠김 시 FetchBusyError, 예외/성공 모두 close & lock 해제) + 스레드 tick 진입점(_run_fetch_cycle_in_new_loop) 구현 및 테스트 10건 추가
- 2026-04-25T16:45:00Z src/rss_wiki/main.py에 create_app(connection_factory/init_schema/scheduler_factory/templates_dir/start_scheduler 주입 가능) 팩토리 구현 — lifespan에서 DB 스키마 초기화 후 커넥션 close, scheduler.start(), 종료 시 shutdown(wait=False) 호출; Jinja2Templates를 app.state.templates로 노출; GET / 은 HTMLResponse("<h1>RSS Wiki</h1>"); 모듈 수준 app 엔트리포인트와 테스트 10건 추가
- 2026-04-25T17:00:00Z src/rss_wiki/web/routes.py에 피드 라우트(GET /feeds 목록+추가 폼 HTML, POST /feeds/add INSERT·중복시 409·공백 URL 400, POST /feeds/{id}/toggle 1-is_active, POST /feeds/{id}/delete CASCADE, 모두 303 /feeds 리다이렉트) 구현, 비동기 get_conn 의존성으로 sqlite3 스레드 체크 회피, main.create_app에서 router include 및 app.state.connection_factory 노출, 12건 테스트 추가
- 2026-04-24T15:56:24Z src/rss_wiki/web/routes.py에 카테고리 라우트(GET / 최상위·미병합만 LEFT JOIN wiki_pages 후 has_unread DESC·name ASC 정렬, GET /categories/{id} Markdown→HTML 렌더·ok 글만 published_at DESC 나열·미병합 하위 노출·방문 시 has_unread_updates=0 & last_seen_at=datetime('now') UPDATE, 없으면 404) 구현, main.py 의 stub `/` 제거(라우터 충돌 방지), inline HTML 대체 렌더러 추가 및 테스트 16건 추가
- 2026-04-24T15:59:50Z src/rss_wiki/web/routes.py에 카테고리 관리 라우트(GET /categories/manage 미병합 전체 나열·부모 조인, POST /{id}/rename 공백제거·is_user_edited=1·UNIQUE 위반 409·빈 이름 400·미존재 404, POST /{id}/merge articles.primary_category_id 타겟 이동→소스 wiki_pages 삭제→merged_into_id 설정→타겟 wiki_pages upsert·has_unread_updates=1, 자기 자신/미존재 타겟 400·미존재 소스 404, POST /{id}/parent 빈값으로 해제·자기 참조 400·부모가 이미 부모 가짐 400·본인이 자식 가짐 400·미존재 타겟 400) 구현 및 테스트 20건 추가
- 2026-04-25T01:05:00Z src/rss_wiki/web/routes.py에 검색 라우트(GET /search?q=...) 구현 — 사용자 입력을 `\w`/한중일 유니코드 토큰으로 분해 후 각 토큰을 `""`-phrase 로 감싸 AND 결합한 FTS5 MATCH 식으로 변환(따옴표/AND/OR/콜론 등 특수 토큰에도 500 없이 동작), articles_fts JOIN articles LEFT JOIN categories 로 status='ok' 글을 bm25/published_at 순 100건 조회, 빈 쿼리/공백 쿼리는 폼만 렌더, 쿼리는 입력창에 에코; 테스트 11건 추가
- 2026-04-24T16:06:54Z src/rss_wiki/web/routes.py에 GET /logs(job_logs 최신 config.JOB_LOG_LIST_LIMIT=200건을 started_at DESC·id DESC 정렬해 테이블 렌더, 템플릿 없을 땐 inline HTML fallback)와 POST /api/fetch(scheduler.run_fetch_cycle 을 app.state.connection_factory 로 호출, 성공 시 202+CycleResult JSON, FetchBusyError는 409 HTTPException) 추가 및 테스트 7건 추가
- 2026-04-25T17:30:00Z src/rss_wiki/web/templates/{base.html,feeds.html} 작성(공통 네비·스타일·{% block title/content %}, feeds 는 extends base + 추가 폼/URL·제목·상태·마지막수집·연속실패·토글/삭제 폼), routes.py 의 템플릿 감지 로직을 Jinja2Templates.env.loader.searchpath 기반 _templates_searchpaths/_template_exists 로 교체(최신 FastAPI 에서 .directory 미노출 대응), 렌더 검증 테스트 6건 추가
- 2026-04-24T16:12:14Z src/rss_wiki/web/templates/{index.html,category.html} 작성(extends base, data-category-id·/categories/{id} 링크·badge-unread 뱃지·빈 상태 안내, category 는 title 블록을 카테고리명으로 치환·content_html|safe·하위 카테고리 리스트·원문 리스트·위키 미생성 시 placeholder), 기존 패키지 템플릿 경로로 렌더 검증 테스트 7건 추가
- 2026-04-25T17:45:00Z src/rss_wiki/web/templates/{categories_manage.html,search.html,logs.html} 작성(모두 extends base + block content, manage 는 data-category-id 행과 rename/merge/parent inline 폼, search 는 query input value 에코·카테고리 링크·data-article-id·빈쿼리는 결과 섹션 생략·매치 없을 때 "검색 결과 없음", logs 는 data-log-id/data-status·failed 는 badge-unread·수동 수집 POST /api/fetch 버튼), 기본 패키지 템플릿 경로로 렌더 검증 테스트 9건 추가
- 2026-04-25T18:00:00Z README.md 작성(요구사항/uv sync/PRD §14.2 개발·프로덕션 uvicorn 명령/수동 수집 curl/pytest/PRD §14.1 레이아웃) 및 tests/test_readme.py에 PRD §14.2 명령·uv sync·docs/PRD.md 참조 검증 5건 추가
- 2026-04-25T18:15:00Z tests/test_smoke_boot.py 추가 — `uv run uvicorn rss_wiki.main:app --host 127.0.0.1 --port <free>` 서브프로세스를 띄워 GET / 200 + "RSS Wiki" 바디 확인, 종료 후 stdout/stderr 병합 출력에서 "Application startup complete" 및 127.0.0.1:<port> 바인딩 로그를 검증; uv CLI 없으면 skip
